# 実装へ何を渡すか — 文書の仕分け

**作成日**: 2026-08-31
**目的**: **このリポジトリは仕様決定のためのリポジトリ**であり、実装は別の場所で行う。
実装場所には**実装に必要な情報だけ**を持っていく。その仕分けを1箇所に決める。

---

## 0. 結論を先に

**仕様書一式 `spec/` を 2026-08-31 に作成した。実装へ渡すのはこれだけである。**

```
spec/
  SPEC.md         全体・原則・前提・層・CLI・処理フロー・異常時・必須要件・順序   330行
  SCHEMA.md       列定義（docs/ から移設）                                       521行
  EXTRACTION.md   L0 の判定手順（実装の本体）                                    367行
  REPORTS.md      L3a のプラグイン仕様と5レポート                                229行
  TESTING.md      ゴールデンデータと受け入れ基準                                 158行
  golden/         正解データ（人手判定）
```

**`spec/` だけで実装に着手できることを条件にした。**
`DECISIONS.md` を読まないと実装できない仕様書は、実装場所に持っていけないため。

> 順序は **構想（済）→ 仕様書（済）→ 実装**。

---

## 1. 仕分けの原則

| 原則 | 内容 |
|---|---|
| **仕様書は自己完結させる** | **`DECISIONS.md` を参照しないと読めない仕様書は、実装場所に持っていけない。** 根拠が要る箇所は、結論を仕様書に書いたうえで出典を注記する |
| **経緯は持っていかない** | 「なぜ A ではなく B にしたか」は実装には不要。ただし**決定を蒸し返さないため**に本リポジトリに残す |
| **正本を二重に持たない** | `SCHEMA.md` は**移す**（コピーしない）。写しを作れば F-8（追随漏れ）が再発する |
| **測定スクリプトは残す** | 数字の出典。実装には不要だが、仕様を疑ったときに再現できる必要がある |

---

## 2. 仕分け表

### A. 実装へ渡す

| もの | 状態 | 扱い |
|---|---|---|
| **仕様書一式 `spec/`** | **作成済み（2026-08-31）** | → §3 |
| `spec/SCHEMA.md` | **完成** | **移設済み**（2026-08-31 に `docs/` から移した） |
| `spec/golden/` | **完成** | **移設済み**。テストの正解データ |
| `tools/schema-check/verify_schema.py` | **完成** | **移設**。DDL を実行して仕様どおりか確かめる |
| `tools/sample-golden.py` | **完成** | **移設**。ゴールデンデータの再生成 |

### B. 仕様検討にのみ必要（本リポジトリに残す）

| もの | 行数 | 残す理由 |
|---|---:|---|
| `docs/DECISIONS.md` | 3,474 | **なぜそう決めたか。** 実装には不要だが、**同じ議論を再演しないために要る** |
| `docs/SCHEMA_INDEX.md` | 250 | 列 × 決定の追跡表。`SCHEMA.md` を疑ったときに辿る |
| `docs/OPEN_DECISIONS.md` | 109 | 未決事項 |
| `docs/HANDOFF.md` | 265 | 引き継ぎ・案内 |
| `CLAUDE.md` | 77 | **本リポジトリの**作業規約（実装リポジトリの規約とは別物） |
| `tools/measure-*.py` / `emit-generic-schema.py` | — | 数字の出典。仕様を疑ったときの再現手段 |
| `tools/schema-check/record-*.py` | — | J 群 / K 群の検証の記録 |
| `tools/schema-check/verify_docs.py` | 101 | **本リポジトリ専用**（文書間の参照検査） |

### C. 一部だけ実装に要る — **切り出して仕様書へ書き下ろす**

**そのまま渡さない。** 実装に要る部分だけを仕様書の中に書き直す。

| もの | 行数 | 実装に要る部分 | 残りは |
|---|---:|---|---|
| `docs/SIMPLE_VERSION.md` | 511 | §2（前提）／§4（出力）／§5（構成）／§6（実装計画）／§7.1（天井） | §3 の実測の経緯は**仕様ではない** |
| `docs/CODE_ANALYSIS_CONCEPT.md` | 1,004 | **§2.3**（原則）／**§7.3**（層分離4ルール）／**§7.6**（L0 形式・増分ロード）／**§8**（テスト対象） | 残りはフル版 |

### D. 渡さない

| もの | 理由 |
|---|---|
| `docs/full-version/` | フル版専用（凍結中） |
| `tools/full-version/` | フル版の環境確認 |
| `docs/ANALYSIS_VIEWPOINTS.md` | 言語仕様の観点カタログ。**第2段**（L3a のレポートを増やすとき）に使う |

### E. 片付ける（成果物の誤コミット）

| もの | 実態 |
|---|---|
| `tools/schema-check/{f,s,full,simple}.db` | **`record-K-*.py` の生成物**。`.gitignore` に入れて削除する |

---

## 3. 仕様書の構成（提案）

**渡す先は `spec/` 一式。これだけで実装に着手できることを条件にする。**

> **`spec/` はこのリポジトリで作り、実装リポジトリへ丸ごと移す。**
> このリポジトリ側には残さない（残せば写しになり、追随漏れが起きる）。

| ファイル | 中身 | 状態 |
|---|---|---|
| `spec/SPEC.md` | 全体像・原則・前提・層・CLI・処理フロー・異常時・必須要件・実装の順序 | **作成済み** |
| `spec/SCHEMA.md` | 列定義 | 移設済み |
| `spec/EXTRACTION.md` | **L0 の判定手順（実装の本体）**、辞書、`import` 解決、言語ごとに変わる集合、ERROR の扱い | **作成済み** |
| `spec/REPORTS.md` | L3a のプラグイン仕様（受け口・`Artifact`・`requires`）と5レポートの定義 | **作成済み** |
| `spec/TESTING.md` | ゴールデンデータ、受け入れ基準（不変条件9件）、検証スクリプト | **作成済み** |
| `spec/golden/` | 正解データ | 移設済み |

### なぜ EXTRACTION.md を分けたか

**「どう解くか」がいまどこにも一箇所にまとまっていない。**
判定規則は `DECISIONS.md` の E-1 / E-3 / F-16 に分散し、実際の順序は測定スクリプトの中にしかない。

**実装者が読むべきものは「決定の並び」ではなく「判定の手順」である。** 収めたのは:

1. 呼び出し式の形の分類（識別子 / メンバ / 間接）
2. **判定の適用順序**（マクロ → ローカル束縛 → 同一 container → 宣言型 → 型名 static → 閉世界で一意 → 辞書 → 外部）
3. 各段での `status` / `reason` / `resolved_by` / `confidence` の埋め方
4. **オーバーロードの検出と降格**（F-16 決定2）
5. **`base.` は基底から探す**（F-16 決定3）
6. 言語ごとに変わるのは**集合の中身だけ**（F-12 決定2。C# は `{this, base}`、Python は `{self, cls}`、JS は `{this, super}`）

> **F-12 の教訓がここに効く。** 3つのスクリプトが同じ判定を別々に書き直した結果、
> 同じ指標が 53 / 55 / 56 と食い違った。**判定規則を1箇所に書き下ろすのが仕様書の役目**である。

---

## 4. 実装リポジトリ側で決めること（本リポジトリの範囲外）

仕様書に含めない。実装リポジトリの `CLAUDE.md` 相当で決める。

- パッケージ名・ディレクトリ構成の細部
- 依存管理（`pyproject.toml`）、lint / format の設定
- CI の構成

**ただし B-3（`core/` にロジック、CLI は薄いラッパー、ジョブ設定ファイル、構造化進捗出力）と
§7.3 ルール4（L1 を境界にプラグイン化）は仕様書に書く。** これらは構成の自由ではなく要件だから。
