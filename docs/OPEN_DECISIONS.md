# 未決事項リスト

**作成日**: 2026-08-25
**目的**: 3本の構想文書に散らばった「まだ決まっていないこと」を1箇所に集約する
**運用**: 作業開始時に必ず本ファイルを参照する。決まったものは「決定事項」へ移し、決定日と根拠（どの文書のどこに書いたか）を残す

**関連文書**:
- `CODE_ANALYSIS_CONCEPT.md` — 全体構想。以下 **[構想]**
- `EXTRACTABLE_DATA_MATRIX.md` — 言語別に「ツールで取れるか」。以下 **[現実]**
- `ANALYSIS_VIEWPOINTS.md` — 言語別に「その概念が存在するか」。以下 **[理想]**

---

## 進め方

`A → B → C` の順に片付ける。`D` は後続 Phase まで着手しない（忘れないための記録）。

| 群 | 内容 | 着手条件 |
|---|---|---|
| **A** | 文書間の矛盾 | 最優先。放置するとスキーマが二重定義になる |
| **B** | Phase 1 着手前に決めるもの | A と独立。作業量は小さい |
| **C** | 最大の宿題 | A の結論に依存する |
| **D** | 後続 Phase | 着手しない |

---

## A. 文書間の矛盾

### A-1. `resolution` の値セットが2文書で食い違っている

| 出典 | 値 |
|---|---|
| [構想] §3.1 `calls.csv` | `resolved` / `via_function_pointer` / `virtual_unresolved` / `macro_expanded` / `unknown` |
| [理想] §3.3 ④ | `resolved` / `unresolved` / `not_applicable` / `not_extracted` |

**論点**: 前者は「なぜ解決できなかったか」の理由分類、後者は「そもそもどの状態か」の状態分類で、**粒度の異なる軸が同じ列名で衝突している**。

[理想] §3.3 ④ の「『調べていない』『取れなかった』『そもそも存在しない』の3つは意味が全く違い、混ぜると解析結果を誤読する」という指摘自体は正しい。

**決めること**: 1列に統合するか、2列（状態＋理由）に分けるか。分ける場合の列名と値域。

- [ ] 未決

### A-2. `confidence`（信頼度）列がスキーマに無い

[構想] §7.4-2 が「**初版のスキーマに入れておくべきもの**」として「`resolution` / 信頼度列を最初から入れる」と明記しているが、§3.1 のテーブル定義に信頼度列が存在しない。

**決めること**: 列名（`confidence` か否か）、値域（0.0-1.0 の数値か、`high` / `medium` / `low` の enum か）、どのテーブルに持たせるか。

- [ ] 未決

### A-3. `lang` / `extractor` の適用範囲が不揃い

| テーブル | `lang` | `extractor` | `snapshot` |
|---|:--:|:--:|:--:|
| `symbols` | ○ | ○ | ○ |
| `calls` | ✕ | ✕ | ○ |
| `var_refs` | ✕ | ✕ | ○ |

[構想] §7.4-4 は「抽出器の識別子とバージョンを記録」を全体方針として挙げており、§7.6 のメタ情報行も全テーブルに `snapshot` / `extractor` / `lang` を持たせる前提で書かれている。§3.1 の定義だけが追随していない。

**決めること**: 全テーブルに揃えるか、`symbols` から JOIN で引く設計にするか。

- [ ] 未決

---

## B. Phase 1 着手前に決めるもの

A と独立に進められる。作業量は小さい。

### B-1. SCIP シンボルIDの具体的マッピング規則

[構想] §3.2 は「SCIP のシンボル文字列形式を借用する」と方針を書いているが、各言語のネイティブID（USR / DocumentationCommentId / `module.Class.method` 等）を SCIP 文字列へどう変換するかの規則が無い。§3.2 は名寄せの基盤であり、ここが曖昧だとデータセットが破綻する。

**決めること**: 変換規則。SCIP の公式仕様のどこまでに従うか。`scip-python` / `scip-typescript` の出力と実際に突き合わせられるか。

- [ ] 未決

### B-2. L1 のストア実体

SQLite はロードマップ Phase 1 のチェック項目に出てくるだけで、§3 のデータセット設計では明言していない。

**決めること**: SQLite で確定してよいか。ファイル配置（`facts/` 配下か）。

- [ ] 未決

### B-3. 実装言語とリポジトリ構成

抽出器は Python（libclang）前提だが、C# は .NET プロセスが必要で、TS/JS は Node が必要。§7.3 の層分離（`extractors/` / `facts/` / `derived/` / `views/` / `rules/`）はディレクトリ名だけが決まっている。

**決めること**: 各抽出器の起動インターフェース（CLI の形、引数、終了コード）。オーケストレータを何で書くか。

- [ ] 未決

### B-4. `snapshot` の粒度

[構想] §7.4-3 は「コミットハッシュ**等**」と書いたまま。

**決めること**: ワークツリーが dirty なときの表現。ブランチ名を併記するか。

- [ ] 未決

---

## C. 最大の宿題

### C-1. 「理想」×「現実」の4象限突き合わせ

[理想] の末尾 §3.3 ⑤「次のステップ」が、この作業を挙げたまま**未実施**になっている。

> - 理想 ○ ／ 現実 ○ → 初版で実装
> - 理想 ○ ／ 現実 △ → `confidence` 列つきで実装
> - 理想 ○ ／ 現実 × → スキーマには列を用意し、当面は空にする
> - 理想 − ／ 現実 − → 該当言語では `not_applicable`

[理想] の観点 A〜O（全100項目超）× 6言語について、[現実] のマトリクスと突き合わせて仕分ける。

**これが終わらないと §3.1 のスキーマを初版として確定できない。** また、4象限の分類結果がそのまま A-1 / A-2 の値設計になるため、**A の結論はこの作業から自動的に出る可能性が高い**。

**決めること**: 仕分け結果。初版スキーマの確定。

- [ ] 未決

### C-2. 可視化層（L3a）の実現手段

ロードマップ Phase 1 に「Mermaid または自作 HTML でコールグラフを出力」と併記されたまま。

**決めること**: どちらか、あるいは両方。規模が大きいときの描画戦略。

- [ ] 未決

---

## D. 後続 Phase（今は着手しない）

忘れないための記録。着手時期が来るまで触らない。

| ID | 項目 | 出典 | 備考 |
|---|---|---|---|
| D-1 | **`constraints` テーブル**（人が書いたルールの保持） | [理想] §3.3 ③ | 「検討すべき」で終わっている。[理想] 自身が「これが差別化になる」と書いている核心部分。抽出ではなく宣言なので L0 とは別系統 |
| D-2 | **5軸の拡張テーブル**（`memory_ops` 等） | [理想] §3.3 ② | 言語ごとではなく「手動メモリ管理／GC」等の5軸で拡張テーブルを切る方針 |
| D-3 | **`cfg_blocks` / `cfg_edges` のスキーマ** | [構想] §6 | 名前だけ登場し §3.1 に定義が無い |
| D-4 | **findings / SARIF 取り込み** | [構想] Phase 5 | 明示的に「未確定」 |
| D-5 | **依存グラフ**（増分更新時の陳腐化対策） | [構想] §7.6 | 「後日検討」と明記。ファイル横断の `resolution` が古くなる問題 |
| D-6 | **FFI マッチング**（言語をまたぐ呼び出しの解決） | [構想] §7.7 | 非スコープと明記 |

---

## 決定事項

決まったものはここへ移す。形式: `[決定日] 項目 — 結論（記載先）`

（まだ無し）
