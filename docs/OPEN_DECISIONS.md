# 未決事項リスト

**作成日**: 2026-08-25
**目的**: 構想文書に散らばった「まだ決まっていないこと」を1箇所に集約する
**運用**: 作業開始時に必ず本ファイルを参照する。決まったものは「決定事項」へ移し、決定日と根拠（どの文書のどこに書いたか）を残す

> ## ⚠ 現在の状況（2026-08-26 更新）
>
> **フル版の構想（CODE_ANALYSIS_CONCEPT.md）は凍結中。** §9.3 のインストール要件（Python / .NET SDK / Node / clang / cmake）が、そのまま利用者への要求になることが判明したため、**前提を問い直す段階に入った**。
>
> したがって **C 群・D 群の検討は中断する**。それらはフル版の詰めであり、簡易版の検討結果によって前提が変わる可能性があるため。
>
> **E 群（本ファイル）は 2026-08-26 に全件決着した。** 次は簡易版の構想文書を起こす段階だが、
> その前段の文書レビューで **F 群**（本ファイル末尾）が出ている。**F-1 〜 F-6 は列定義に関わるため先に決める。**

**関連文書**:
- `CODE_ANALYSIS_CONCEPT.md` — 全体構想。以下 **[構想]**
- `EXTRACTABLE_DATA_MATRIX.md` — 言語別に「ツールで取れるか」。以下 **[現実]**
- `ANALYSIS_VIEWPOINTS.md` — 言語別に「その概念が存在するか」。以下 **[理想]**

---

## 進め方

`A → B → C` の順に片付ける。`D` は後続 Phase まで着手しない（忘れないための記録）。

| 群 | 内容 | 状態 |
|---|---|---|
| ~~**A**~~ | ~~文書間の矛盾~~ | **解決済み（2026-08-25）** |
| ~~**B**~~ | ~~Phase 1 着手前に決めるもの~~ | **解決済み（2026-08-25）** |
| **C** | フル版の最大の宿題 | **中断**（凍結中。E の結論待ち） |
| **D** | フル版の後続 Phase | **中断**（同上） |
| ~~**E**~~ | ~~簡易版の再検討~~ | **解決済み（2026-08-26）**。E-1〜E-4 すべて決着 |
| **F** | 文書レビューで出た未決事項 | **F-1〜F-6 / F-13 は決定済み（2026-08-27）**。残りは記述の不整合 |
| ~~**G**~~ | ~~`SCHEMA.md` を書く前に埋める列の細目~~ | **解決済み（2026-08-27）**。13件すべて決着 |
| ~~**H**~~ | ~~G 群決着後の再突き合わせ~~ | **解決済み（2026-08-27）**。9件すべて決着 |
| ~~**I**~~ | ~~H 群決着後の再突き合わせ（運用に当てて検証）~~ | **解決済み（2026-08-27）**。8件すべて決着 |

---

## A. 文書間の矛盾 — **解決済み（2026-08-25）**

3件とも決着し、[構想] §3.1 に反映済み。詳細は「決定事項」を参照。

| ID | 項目 | 結論 |
|---|---|---|
| A-1 | `resolution` の値セット衝突 | `status`（状態4値）と `reason`（理由4値）の2列に分離 |
| A-2 | `confidence` 列が無い | `high` / `medium` / `low` の enum で追加 |
| A-3 | `lang` / `extractor` の適用範囲 | 全テーブルに `lang` / `extractor` / `snapshot` を持たせる |

---

## B. Phase 1 着手前に決めるもの — **解決済み（2026-08-25）**

4件とも決着し、[構想] に反映済み。詳細は「決定事項」を参照。

| ID | 項目 | 結論 | 記載先 |
|---|---|---|---|
| B-1 | SCIP シンボルIDのマッピング規則 | 自リポジトリは `local` スキーム + リポジトリ名 + スナップショット。外部ライブラリは `pypi` / `npm` 等の本来の座標をそのまま受け入れる | [構想] §3.2.1 |
| B-2 | L1 のストア実体 | SQLite（`facts/db.sqlite`）。追加依存ゼロ | [構想] §3.4.1 |
| B-3 | 実装言語とリポジトリ構成 | 抽出器は独立プロセス（API 呼び出しではない）。ロジックは `core/`、CLI は薄いラッパー、ジョブ設定ファイル、構造化進捗出力の3点で GUI 後付けを安くする | [構想] §9.1 / §9.2 |
| B-4 | `snapshot` の粒度 | `git describe --always --dirty` 相当。Git 管理外は `nogit-YYYYMMDDThhmmss` | [構想] §3.4.2 |

**副産物**: 必要なインストール一覧を Phase 別に [構想] §9.3 へ整理した。**SQLite と LLVM 本体はインストール不要**であることが実測で判明している。

---

## C. フル版の最大の宿題 — **中断中**

> 凍結中（E 群の結論待ち）。フル版を再開する場合の宿題として残す。

### C-1. 「理想」×「現実」の4象限突き合わせ

[理想] の末尾 §3.3 ⑤「次のステップ」が、この作業を挙げたまま**未実施**になっている。

> - 理想 ○ ／ 現実 ○ → 初版で実装
> - 理想 ○ ／ 現実 △ → `confidence` 列つきで実装
> - 理想 ○ ／ 現実 × → スキーマには列を用意し、当面は空にする
> - 理想 − ／ 現実 − → 該当言語では `not_applicable`

[理想] の観点 A〜O（全100項目超）× 6言語について、[現実] のマトリクスと突き合わせて仕分ける。

**これが終わらないと §3.1 のスキーマを初版として確定できない。** また、4象限の分類結果がそのまま A-1 / A-2 の値設計になるため、**A の結論はこの作業から自動的に出る可能性が高い**。

**決めること**: 仕分け結果。初版スキーマの確定。

- [ ] 未決

### C-2. 可視化層（L3a）の実現手段

ロードマップ Phase 1 に「Mermaid または自作 HTML でコールグラフを出力」と併記されたまま。

**決めること**: どちらか、あるいは両方。規模が大きいときの描画戦略。

- [ ] 未決

---

## D. フル版の後続 Phase — **中断中**

忘れないための記録。着手時期が来るまで触らない。

| ID | 項目 | 出典 | 備考 |
|---|---|---|---|
| D-1 | **`constraints` テーブル**（人が書いたルールの保持） | [理想] §3.3 ③ | 「検討すべき」で終わっている。[理想] 自身が「これが差別化になる」と書いている核心部分。抽出ではなく宣言なので L0 とは別系統 |
| D-2 | **5軸の拡張テーブル**（`memory_ops` 等） | [理想] §3.3 ② | 言語ごとではなく「手動メモリ管理／GC」等の5軸で拡張テーブルを切る方針 |
| D-3 | **`cfg_blocks` / `cfg_edges` のスキーマ** | [構想] §6 | 名前だけ登場し §3.1 に定義が無い |
| D-4 | **findings / SARIF 取り込み** | [構想] Phase 5 | 明示的に「未確定」 |
| D-5 | **依存グラフ**（増分更新時の陳腐化対策） | [構想] §7.6 | 「後日検討」と明記。ファイル横断の `resolution` が古くなる問題 |
| D-6 | **FFI マッチング**（言語をまたぐ呼び出しの解決） | [構想] §7.7 | 非スコープと明記 |

---

## E. 簡易版の再検討 — **全件決着（2026-08-26）**

フル版（CODE_ANALYSIS_CONCEPT.md）の凍結を受けて、原点に立ち返って検討する項目。

**進捗**: **E-2 → E-3 → E-1 → E-4 の順に決着し、2026-08-26 に全件完了した。**

| ID | 状態 |
|---|---|
| E-2 インストール水準 | **決定**（軸1 =「軽い」／軸2 = ゼロ） |
| E-3 名前解決を捨てた場合の線引き | **決定**（① / ②-a / ②-b / ③ の4段階。実測つき） |
| E-1 想定用途と最小の出力 | **決定**（4テーブル・5レポート・改良A〜D必須） |
| E-4 フル版との関係 | **決定**（スキーマ共有・DB は混ぜない。フル版に追随改訂6件） |

**E 群は完了した。** 次は簡易版の構想文書を起こし、本設計へ進む。

### E-1. 想定用途の再定義 — **決定済み（2026-08-26）**

**初見のソースコードの一次解析の補助**。知りたいのは全体像。

- どのフォルダに何の機能があるか
- どの辺りが詳細解析を要しそうか
- 規模感、構造の偏り

**決めること**: この用途を満たす最小の出力は何か。フル版 §1.1 の目的（正確なコール関係・データの所在）とどう関係するか（置き換えか、部分集合か、別物か）。

#### 初期対象言語 — **C と C#（2026-08-26 指示）**

E-3 の測定では C が最も価値が出たが、**C だけで進めるとデータセットが C に寄る**。マルチ言語ツールにしたいので、データは汎用的な形で管理する。

**この2つは C 系言語の中で互いに最も遠く、スキーマの一般性を検証するペアとして最適。**

| | C | C# |
|---|---|---|
| 名前の入れ物 | ファイル（翻訳単位） | 名前空間 → 型 |
| メソッドの所有者 | 無い | 型 |
| 可視性の仕組み | リンケージ（`static` / `extern`） | アクセシビリティ（`public` / `internal` / …） |
| 前処理 | マクロあり | 無し |
| 呼び出しの主形 | 直接（②-a 6割） | メンバ（③型解決 7割） |

**片方で通って片方で破綻する設計は、この2つを並べれば露呈する。** 実際に露呈した（下記）。

#### 汎用スキーマ案（言語固有概念を列に持ち込まない）

C の `static` / `extern` や C# の `namespace` / `type` を**そのまま列にすると C 専用・C# 専用の表になる**。共通の問いへ写す。

| 汎用概念 | 問い | C での対応 | C# での対応 |
|---|---|---|---|
| **`kind`** | 何であるか | `function` / `macro` / `file` | `method` / `type` / `namespace` / `file` |
| **`container_id`** | 何の中にあるか（入れ子の親） | ファイル | 名前空間 → 型 |
| **`visibility`** | 名前がどこまで見えるか | `extern`→`public` / `static`→`module` | `public`→`public` / `internal`→`module` / `private`・`protected`→`private` |
| **`refs.kind`** | どんな関係か | `call` / `import`(`#include`) | `call` / `import`(`using`) |
| **`refs.reason`** | なぜ解けないか | `external` / `ambiguous` / `macro_expanded` / `via_function_pointer` | `external` / `ambiguous` / **`needs_type`** |

**言語に無い概念は「行が出ない」だけで、列は共通のまま。** C に `private` が無く、C# に `macro` が無いのは、A-1 が `not_applicable` で扱おうとしていたものと同じ形。

##### `refs.reason` の値域（提案）

A-1 が定めた4値に、簡易版で必要になる3値を足す。**`reason` に値を追加しても `status` の値域が汚れない**という A-1 の設計意図どおりの拡張。

| 値 | 意味 | 出典 |
|---|---|---|
| `external` | 定義がリポジトリ外（閉世界の境界の外） | **新設**（E-3） |
| `ambiguous` | リポジトリ内の候補が複数。名前だけでは決まらない | **新設**（E-3） |
| **`needs_type`** | **メンバ呼び出しで、レシーバの型が分からないため解けない** | **新設**（E-1） |
| `via_function_pointer` | 関数ポインタ／デリゲート経由 | A-1 |
| `virtual_unresolved` | 仮想ディスパッチ | A-1 |
| `macro_expanded` | マクロ経由 | A-1 |
| `unknown` | 上記のいずれでもない | A-1 |

**`needs_type` を `unknown` に混ぜてはいけない理由**: C# の呼び出しの **69% がこれ**。混ぜると最大の分類が「理由不明」になり、「**型解決さえあれば解ける**」という情報が消える。`external`（境界を広げれば解ける）／`ambiguous`（名前の一意性の問題）とも性質が違い、**何を足せば解けるようになるか**が値ごとに違う。

```
refs: src_id=...  dst_id=NULL  dst_expr="_validator.Validate"
      status=unresolved  reason=needs_type  confidence=low
```

#### 実測: C と C# を同じ列に出した（2026-08-26）

cJSON（C）と FluentValidation（C#）を上記スキーマの SQLite へ実際に出力した。

| `kind` の分布 | C | C# |
|---|---:|---:|
| `function` | 154 | — |
| `method` | — | 436 |
| `type` | — | 181 |
| `macro` | 13 | — |
| `file` | 4 | 137 |

| `visibility` | C | C# |
|---|---:|---:|
| `public` | 93 | 286 |
| `module` | 61 | 20 |
| `private` | — | 130 |

| `refs`（呼び出し） | C | C# |
|---|---:|---:|
| `resolved` | 350 | 55 |
| `resolved` / `macro_expanded` | 65 | — |
| `unresolved` / `external` | 85 | 130 |
| `unresolved` / `ambiguous` | — | 126 |
| `unresolved` / `needs_type` | 26 | 705 |
| `unresolved` / `via_function_pointer` | — | 6 |

**列は1つも増やさずに両言語が載った。** 汎用スキーマ案は成立する。

#### ★ 発見1: シンボルID がシグネチャ無しでは衝突する（B-1 の要修正）

| プロジェクト | 生成した symbol | ユニーク ID | **衝突で消える** |
|---|---:|---:|---:|
| cJSON（C） | 175 | 171 | **4 件（2.3%）** |
| FluentValidation（C#） | 916 | 754 | **162 件（17.7%）** |

C# の消失例（いずれもオーバーロード）:

```
x6  DefaultValidatorExtensions#Matches().
x6  DefaultValidatorExtensions#LessThan().
x6  DefaultValidatorExtensions#GreaterThan().
```

**これは §2.3 の「静かな取りこぼし」そのもの。** 図は綺麗に出るが 17.7% のシンボルが存在しないことになる。

**C だけで進めていたら気づけなかった**（C は 2.3%、しかも中身は `#ifdef` の枝ごとに5回定義された同一マクロで、原因が別）。

> **B-1 への修正提案**: descriptor に**パラメータの型名リスト**を含める。B-1 は「要実測（Phase 0）: descriptor を機械的に生成できるか。特に C++ のオーバーロード」と書いていたが、**C# でも同じ問題が起きることが実測された**。
> 型名はソースに書いてあるので取得できる（C# の `var` は引数には現れない）。型注釈が無い言語（Python / JS）は**アリティ（引数の個数）**でフォールバックする。
> 出現順の連番は編集で変わるため採らない（B-4 が「ブランチ名は識別子として不安定」を退けたのと同じ理由）。

#### ★ 発見2: C# の file-scoped namespace を取りこぼしていた

FluentValidation は `namespace FluentValidation;`（C# 10 以降の file-scoped 形式）を使っており、`namespace_declaration`（ブロック形式）しか見ていない実装では**名前空間が ID から丸ごと欠落する**。

**言語仕様の新旧2形式は、tree-sitter では別ノード型になる。** §2.3 の「長い尻尾の言語仕様」が最初の1言語で早速出た。実装時のチェック項目とする。

#### ★ コメントは最小に含める（2026-08-26、当初案の訂正）

**当初案はコメントを「件数だけ」に切り詰めていた。これは誤りだった。** 一次解析ではコメントが重要な情報であるとの指摘を受けて実測した。

| 項目 | cJSON (C) | cmark (C) | FluentValidation (C#) |
|---|---:|---:|---:|
| コメント総数 | 403 | 620 | 4,804 |
| 定義の総数 | 184 | 394 | 999 |
| **直前に doc コメントを持つ定義** | **34.2%** | **22.6%** | **49.8%** |
| doc コメントの平均長 | 61 文字 | 117 文字 | **239 文字** |
| ファイル先頭コメント | 4 / 4 ファイル | 2 ファイル | **130 / 137 ファイル** |
| マーカー（TODO 等） | 12 | 11 | 10 |
| コメントアウト疑い | 0 | 5 | 146 |

**doc コメントは「この関数が何をするか」を人が書いた唯一の記述であり、名前解決を一切必要としない。** C# では定義の半分に付いていて平均 239 文字。一次解析の用途（どのフォルダに何の機能があるか）に対して、**命名の語彙集計よりも遥かに直接的な答え**になる。

> **さらに重要**: ここは**簡易版がフル版と同等以上になりうる唯一の領域**。コンパイラのフロントエンドはコメントを捨てる（libclang は保持に特別な指定が要り、Roslyn では trivia 扱い）。tree-sitter は comment を第一級ノードとして持つ。

**マーカー（TODO / FIXME）は思ったより少なかった**（10〜12件）。E-3 の①で「未整理そうな領域の当たり」として挙げていたが、**主役は doc コメントの方**だと分かった。

##### confidence の規則（実測に基づく）

| コメントの形 | cJSON | FluentValidation | `confidence` |
|---|---:|---:|---|
| `///`（C# XML doc） | — | **54.3%** | **`high`**（明示的な doc 記法） |
| `/**`（doc ブロック） | 0% | — | **`high`** |
| `/* */` 通常ブロック | **100%** | — | `medium`（隣接規則のみ） |
| `//` 通常行 | — | 45.7% | `medium` |

**明示的な doc 記法があれば `high`、隣接だけなら `medium`。** 「直前のコメントは doc である」は慣習に基づく判断なので、**位置は事実として必ず記録し、紐付けは confidence つきで別に持つ**（L3a で再計算できる）。

##### 汎用性の確認（C 専用にしない）

**doc の担い手は言語で違う。** C / C# ではコメントだが、**Python の docstring はコメントではなく文字列リテラル**（関数本体の最初の式）。`comments` テーブルは「コメント構文」ではなく「**人が書いた説明**」を入れる器として定義し、Python の docstring も同じテーブルに `kind = doc` で入れる。

#### ★ 他言語で空く軸の先行確認（2026-08-26）

**初期対象は C + C# のまま変えない**が、**列の定義**にだけ影響する軸が他言語にある。列はあとから変えるとデータの作り直しになる（A-3 と同じ構造）ので、**「そういう形がある」ことだけ先に確認**した。抽出器の実装は第2段でよい。

##### 実測: 3つの軸が空いている

| 軸 | C | C# | JS | Python | C+C# でカバー |
|---|---:|---:|---:|---:|---|
| **無名関数の中の呼び出し** | 0.0% | **9.1%** | **31.3%** | 0.0% | **部分的** |
| **doc を持つ定義**（docstring 含む） | 34.2%（comment） | 49.8%（comment） | — | **65.0%（文字列リテラル）** | **×** |
| **visibility の決まり方** | 宣言の修飾子 | 宣言の修飾子 | **修飾子型14 + リスト型3 の混在** | 慣習（`_foo`）+ `__all__` | **×** |

（requests はモジュール docstring も 19 ファイル中 15（79%）が持つ。comment ノード 532 件には **docstring は一切含まれない**。）

##### 軸1: 無名関数 — **C# で既に 9.1%。JS では 31.3%**

C# の `RuleFor(x => x.Surname)` のようなラムダの中にも呼び出しがある。**C だけを見ていたら 0% なので存在にすら気づかない。**

**設計判断**: 無名関数を `symbols` に登録すると **ID を位置ベースにするしかなく、B-4 が退けた「不安定な識別子」になる**。

> **採る案**: 無名関数は `symbols` に登録しない。`refs.src_id` は**囲む名前つき関数**にする。ただし `refs` に **`via_lambda`**（またはネスト深さ）を持たせ、「ラムダ内からの呼び出し」と分かるようにする。
> ID の不安定さを避けつつ情報も失わない。JS の 31.3% も「この関数の中のコールバックから呼ばれている」として残る。

##### 軸2: doc の担い手 — Python は**コメントではない**

requests の定義の **65.0%** が docstring を持つが、**`comment` ノードには一切入らない**（文字列リテラル）。

> **採る案**: `comments` テーブルに **`source_kind`** 列（`comment` / `string_literal`）を持たせる。テーブルは「コメント構文」ではなく「**人が書いた説明**」の器として定義する（E-1 で既に方針として書いたものを、列として具体化）。

##### 軸3: visibility の由来 — 3種類ある

C も C# も**宣言に修飾子が付く**形しかない。他言語には別の形がある。

| 由来 | 例 | `confidence` |
|---|---|---|
| `declaration` | C の `static`/`extern`、C# の `public`、JS の `export const` | `high` |
| `export_list` | JS の `export { A, B }`、Python の `__all__` | `high`（ただし**宣言箇所とは別の場所を読む必要がある**） |
| `convention` | Python の `_foo`、Go の先頭大文字 | **`medium` / `low`** |

**commander.js は同じファイル内に修飾子型14箇所とリスト型3箇所が混在していた。** 「言語ごとにどちらか」ではなく「**同じ言語に両方ある**」。

> **採る案**: `symbols` に **`visibility_source`** 列（`declaration` / `export_list` / `convention` / `linkage`）を持たせる。C と C# は常に `declaration`（または `linkage`）で `confidence = high`。慣習型が来たときに `confidence` を下げられる。

##### 今やらないこと

Go / Rust / Java は上記3軸の**亜種**にしかならない見込み（Go の先頭大文字＝`convention`、Rust の `pub`＝`declaration`、`mod`＝`container` の変種）。**第2段でよい。**
各言語の抽出器の設計も第2段。**今回は列定義への反映のみ。**

#### 最小の出力（提案・訂正版）

**4テーブル**（L1、SQLite）:

| テーブル | 内容 |
|---|---|
| `files` | パス、`lang`、行数、コメント行数、**`parse_errors`**（ERROR ノード数）、git メトリクス |
| `symbols` | `id` / `kind` / `name` / `container_id` / 位置 / `visibility` / **`visibility_source`** / `branch_count` / `confidence` |
| `refs` | `src_id` / `dst_id` / `dst_expr` / `kind` / 位置 / `status` / `reason` / `confidence` / **`via_lambda`** |
| **`comments`** | `file` / 位置 / `kind`（`doc` / `file_header` / `inline` / `block`）/ **`source_kind`**（`comment` / `string_literal`）/ `attached_id` / `marker` / `text` / `confidence` |

**太字の3列は他言語で空く軸への備え**（上記「他言語で空く軸の先行確認」）。C と C# では `visibility_source` は常に `declaration` / `linkage`、`source_kind` は常に `comment` になるが、**列を後から足すとデータの作り直しになる**ため初版から持つ。

**5レポート**（L3a）:

1. **概観** — ディレクトリ別のファイル数・行数・言語構成
2. **機能の見取り図** — ディレクトリ／名前空間ごとに**ファイル先頭コメント＋主要シンボルの doc を集約**したもの。**「どのフォルダに何の機能があるか」への直接の答え**
3. **ホットスポット** — 規模 × 変更頻度（git）× 分岐数 × **doc の欠落率**。「どこから読むか」の答え
4. **コンポーネント依存** — ディレクトリ／名前空間の単位に呼び出しを集約した図。**C も C# も出せる粒度**
5. **解決サマリ** — `status` / `reason` / `confidence` の分布。**最初に見せる**

**5 を最小に含めるのが最重要。** 「呼び出しの 66% が確定、12% がマクロ、16% が外部」を先に出すことで、§2.3 の「不確かさを黙って隠さない」が**出力の一部**になる。

**3 に「doc の欠落率」を入れた理由**: 規模が大きく変更が多いのに doc が無い領域は、まさに「詳細解析を要しそうな場所」。実測どおり C# でも半分の定義に doc が無い。

**最小から外すもの**（後から足せる）: 命名の語彙集計、型参照、継承関係、コメントアウトされたコードの判定（FluentValidation で 146 件の候補が出たが、判定はヒューリスティックなので L3a 側に置く）。

#### 実測: 最小構成から何が作れるか（2026-08-26）

`facts.sqlite`（cJSON + FluentValidation）に対して実際にクエリを流し、**成立するものと成立しないものを分けた**。

##### 成立したもの

| 出せるもの | 根拠 | 実測例 |
|---|---|---|
| **外部依存の棚卸し** | `refs.reason='external'` と `kind='import'` の集計 | C: `strcmp`(13) `strlen`(12) `sprintf`(11) `memcpy`(9) → **libc 依存の一覧＝移植性の見積もり**／C#: `System.Threading.Tasks`(20) `System.Reflection`(**18**) |
| **解析の限界の予告** | 上記のうち動的機構を示すもの | **`System.Reflection` が 18 ファイル**で使われている＝**コールグラフに原理的に見えない部分がある**。フル版でも解けない領域を事前に警告できる |
| **中核シンボルの特定**（C） | `refs` の被参照数 | `cJSON_Delete`(31) `buffer_at_offset`(28, macro) `cJSON_New_Item`(17) → **読む順序の根拠** |
| **未解決の集中箇所**（C#） | `reason` のファイル別分布 | `ValidationStrategy.cs` `RulesetValidatorSelector.cs` などが `needs_type` **94%** → 抽象化が効いている場所＝設計の中心 |

##### 成立しなかったもの（仮説が外れた）

| 仮説 | 実測 | なぜ外れたか |
|---|---|---|
| 「個々の解決率が低くても、**ディレクトリ粒度に集約すればノイズが消えて図になる**」 | **外れ**。C# は 6 ディレクトリ中 3 個しかエッジを持たず、エッジ 4 本（ほぼ自己ループ） | **集約はノイズを減らすが、欠落は埋めない。** 元が 55 本しかなければ集約しても増えない |
| 「call が使えないなら **`import` から内部依存図を描ける**」 | **外れ**。C# の `using` は 241 件すべて `System.*`。C の `#include` もリポジトリ内を指すのは 3 件のみ | FluentValidation は**単一名前空間**（`namespace FluentValidation;`）なので内部 `using` が存在しない。C も `#include` は主にシステムヘッダ |

> **帰結**: **C# の内部コンポーネント依存図は、この最小構成では描けない。** 描くには段階5（型解決）か、少なくともレシーバの型名からの近似が要る。E-3 の「出力の主役を言語で変える」がここでも裏付けられた。

##### ★ 発見3: 段階1（スコープ解析）が無いと誤分類する

C# の `external` 上位が `predicate`(28) `func`(11) `action`(11) だった。実体を見ると:

```csharp
AbstractValidator.cs:246   action();
AbstractValidator.cs:259   return When((x, _) => predicate(x), action);
```

**引数として渡されたデリゲートの呼び出し**であり、`external`（定義がリポジトリ外）は**誤り**。正しくは `via_function_pointer`。

段階1があれば「`action` はこのメソッドの引数として束縛されている」と分かるので正しく分類できる。

> **段階1は「あると精度が上がる」機能ではなく、「無いと嘘をつく」機能である。**
> §2.3 の「間違った答えが自信を持って出てくる」に該当するため、**最小構成に段階1は必須**。E-3 の「要実測: 段階1の実装コスト」の優先度が上がった。

#### ★ 初版に含める改良（2026-08-26、実測つき）

「取れなかったもの」には**バグ由来のもの**と**原理的に無理なもの**が混ざっていた。前者は安く直る。4つの改良を実装して before / after を実測した。

| 改良 | 内容 | 実装コスト |
|---|---|---|
| **A** | **同一 container 優先** — C# の `this.` 省略呼び出し。基底型チェーンも辿る | 小 |
| **B** | **レシーバの宣言型を使う** — 宣言に型が書いてあり、その型がリポジトリ内ならそのメソッドへ | 小〜中 |
| **C** | **レシーバが型名なら static 呼び出し**として解決 | 小 |
| **D** | **ローカル束縛の名前は `via_function_pointer`** — 引数／ローカル変数として束縛された名前の呼び出し | 小 |

**4つとも「1ファイルの構文木 ＋ リポジトリ内の定義表」で完結する。** 追加入力はゼロで、軸2 = ゼロを崩さない。実体は**段階1（スコープ解析）の最小版**。

> **ステータス: 初版の必須要件（2026-08-26 決定）。** 「追加機能」ではない。
> **理由**: 発見3 の通り、段階1 が無いと**関数ポインタ経由の呼び出しを「リポジトリ外の関数」と誤って報告する**。C の実測がそれを裏付けた——**解決率は 0.0 ポイントしか動かないのに、誤分類が 17〜18 件消えた**。§2.3 の「間違った答えが自信を持って出てくるツールは、答えが出ないツールより有害」に直撃するため、**無ければ初版として成立しない**。

##### 実測結果

| | cJSON (C) | cmark (C) | FluentValidation (C#) |
|---|---:|---:|---:|
| **`resolved` 率 before → after** | 66.5% → **66.5%** | 59.9% → **59.9%** | **5.5% → 31.8%（+26.3pt）** |
| `needs_type` | ±0 | ±0 | **704 → 408（−296）** |
| `ambiguous` | ±0 | ±0 | **126 → 36（−90）** |
| `external` | 85 → **68（−17）** | 157 → **139（−18）** | 130 → 138（+8） |
| `via_function_pointer` | 0 → **17（+17）** | 27 → **45（+18）** | 6 → **115（+109）** |

##### 読み取れること

**1. C では解決率は上がらないが、誤分類が消える。**

`external` が 17〜18 件減り、同数が `via_function_pointer` へ移った。これは**関数ポインタ経由の呼び出しを「リポジトリ外の関数」と誤って報告していた**のが直ったということ。

> **§2.3 の観点では、解決率の向上より価値が高い。** 「間違った答えが自信を持って出てくる」のを止めている。C の改良Dは**精度改善であって解決率改善ではない**。

**2. C# は解決率が 6 倍近くになる。** `needs_type` の 4 割強が消える。`var` はもともと 4.2% しかなく、**型はソースに書いてある**ため。

**3. C# の `external` が +8 増えたのは改善。** 改良Bで「レシーバの型は分かるが、その型はリポジトリ外（`System.*`）」と判定できるようになった分。以前は `needs_type`（理由不明の一種）に落ちていた。**より正確な理由が付いた。**

##### あわせて初版に入れるもの（バグ修正・低コスト）

| 項目 | 理由 | 出典 |
|---|---|---|
| **シンボルID にシグネチャを含める** | C# で **17.7%** のシンボルが衝突で消えている | 発見1 |
| **C# の file-scoped namespace 対応** | 名前空間が ID から丸ごと欠落する | 発見2 |
| **標準ライブラリの名前辞書** | `external` を「既知の標準ライブラリ」と「未知の外部」に分ける。C の `external` はほぼ libc | E-3 |
| C の `#include "..."` の相対解決 | リポジトリ内ヘッダを指すものだけ結線 | E-3 |

##### 初版に**入れない**もの（原理的に高コスト）

残った `needs_type` 408 件（C#）の中身は、**チェーン呼び出し**（`a.b().c()`）と **`var` 宣言**。これらは本物の型推論を要求し、段階5に踏み込む。**ここが簡易版の天井**であり、境界として明示する。

- チェーン呼び出しの戻り値型の推論
- `var` / ジェネリクスの実体化
- 仮想ディスパッチの実装先特定（インタフェース宣言までは届く）

##### 初版の目標値（改良込み）

| 言語 | `resolved` | 主な未解決の理由 |
|---|---:|---|
| **C** | **約 60〜67%** ＋ macro 12〜23% ＋ external（ほぼ libc、辞書でラベル可） | `needs_type` 3〜5%、`via_function_pointer` 3% |
| **C#** | **約 32%** | `needs_type` 40%（チェーン呼び出しと `var`）、`via_function_pointer` 11% |

#### 発展させられる資料（カタログ）

最小構成の4テーブルから作れる資料を、**読者と目的**で分けて整理する。

**A. 初見の人向け — 読解ガイド**

| 資料 | 作り方 | 前提 |
|---|---|---|
| 機能の見取り図 | ディレクトリ／名前空間ごとに `comments`(file_header + doc) を集約 | 言語非依存 |
| **読む順序** | 被参照数の多い順 × 依存の浅い順（トポロジカル） | ②-a が効く言語（C）で有効 |
| API 面の一覧 | `visibility='public'` のシンボル | 言語非依存。**C の `extern` と C# の `public` が同じ列で並ぶ** |

**B. 判断材料 — リスクと見積もり**

| 資料 | 作り方 | 用途 |
|---|---|---|
| **外部依存の棚卸し** | `external` と `import` の集計 | 移植性、ライセンス、依存脱却コスト |
| **解析の限界マップ** | リフレクション／動的機構の使用箇所 ＋ `needs_type` / `ambiguous` の集中 | **「ここは静的解析では分からない」の事前提示** |
| 触ると危ないマップ | 被参照数 × `branch_count` × 変更頻度(git) × doc 欠落率 | 改修計画、レビュー重点 |

**C. 変化を見る資料**

| 資料 | 作り方 | 前提 |
|---|---|---|
| スナップショット間の差分 | `snapshot` 列で2時点比較（B-4 が効く） | 同一リポジトリの2時点 |
| 公開 API の変更検出 | `visibility='public'` のシンボル増減 | 破壊的変更の候補出し |

**D. 他のツールへの入力**

| 資料 | 作り方 |
|---|---|
| LLM 用コンテキストパック | doc テキスト ＋ 構造 ＋ 規模を重要度順に、トークン予算で切り出す |
| **詳細解析への引き継ぎ書** | `needs_type` / `ambiguous` / リフレクションの集中箇所＝「フル版やデバッガで見るべき場所」の一覧 |

##### この資料群の独自性

**「分からない場所が分かる」こと。**

既製ツール（Doxygen / Understand）は「**分かる部分**」を出す。簡易版は `status` / `reason` / `confidence` を持つので、**解けなかったものの分布そのものを一級の出力にできる**。それが「次にどこを人手で見るか」の地図になる。

§2.3 の「不確かさを黙って隠さない」という思想が、そのまま製品としての差別化になっている。**D の「詳細解析への引き継ぎ書」は、簡易版とフル版を接続する成果物**でもあり、E-4 の判断材料になる。

#### フル版との関係（提案）

**部分集合 + α。** ①②-a ②-b ③ の仕分けの通り、簡易版が出すのはフル版の出力の部分集合。ただし **git メトリクスはフル版に無い軸**なので厳密には包含関係にない。→ E-4 の判断材料。

- [x] **決定**（2026-08-26）。結論は「決定事項」を参照

### E-2. 「インストール不要」の水準をどこに置くか — **決定済み（2026-08-26）**

**2軸に分解し、軸1 =「軽い」（Python + `pip install` 1コマンド）／軸2 = ゼロ（ビルドコンテキストを要求しない）。**

詳細と根拠は「決定事項」を参照。**この決定が E-3 の前提になる。**

- [x] 決定

### E-3. 名前解決を捨てた場合に何が失われ、何が残るか

フル版が重い根本原因は**厳密な名前解決**であり、それがビルドコンテキストとツールチェインを要求している（§4.1 の選定基準1）。

E-2 の決定（軸1 =「軽い」／軸2 = ゼロ）により、**使える手段は Python + pip で入るもの（tree-sitter、標準 `ast`、`sqlite3`、`git` コマンド）に確定した**。その前提での線引きを以下に置く。

> **ステータス**: 骨格は確定。**tree-sitter の能力と C のファイル横断解決は実測済み**（2026-08-26）。実プロジェクトでの取りこぼし率・誤結線率は未測定（末尾の「要実測」）。
> **構成**: まず「解決の階段と2軸」で**なぜ解けるか／解けないか**を示し、その上で ① / ②-a / ②-b / ③ の4段階に仕分ける。
> **確定後の移設先**: E-1 / E-4 が決まった時点で簡易版の構想文書へ移す。本ファイルは決定の記録のみを残す。

#### 解決の階段と、解けるかを決める2軸（2026-08-26 追記）

> **経緯**: 当初は「名前解決を持つかどうか」の二択で線を引いていたが、それが粗すぎた。
> 「意味を付ける」作業は6段階あり、**段階の中にも解けるものと解けないものが混在する**。

##### 意味を付ける6段階

| 段階 | やること | 必要な入力 | 軸2 = ゼロで可能か |
|---|---|---|---|
| 1 | **スコープ解析**（識別子がどの束縛を指すか） | そのファイルの木 ＋ 言語のスコープ規則 | **○ 完全に可能** |
| 2 | **モジュール解決**（import / include 先の特定） | 検索パス、パッケージ境界 | **△ 混在**（下記） |
| 3 | **エクスポート面の解析**（対象ファイルの定義表） | 対象ファイルの木（再エクスポートは推移的） | 2 次第 |
| 4 | **シンボルID の付与** | 1〜3 | 2 次第 |
| 5 | **型解決**（`obj.method()` の `obj` の型） | 全ファイル ＋ **外部ライブラリの型** | **× 不可** |
| 6 | **オーバーロード解決・仮想ディスパッチ** | 5 ＋ 言語規則 | **× 不可** |

段階1は**追加入力ゼロで到達できる**。Python は標準 `symtable` が無料で行う（実測）。他5言語は tree-sitter の木の上に自作する。

##### 解けるかを決めるのは段階ではなく、この2軸

| 軸 | 問い | 値 |
|---|---|---|
| **軸A: 情報の所在** | 答えを出すのに必要な情報はどこにあるか | 自ファイル内 / **リポジトリ内の他ファイル** / リポジトリ外 |
| **軸B: 名前の一意性** | その言語で、名前だけから一意に定まるか | 定まる / 定まらない（オーバーロード・仮想・動的） |

**両方が満たされたときだけ解ける。** 段階2の中に解けるものと解けないものが混在するのは、段階2が軸Aの3値すべてにまたがっているため。

| 例 | 軸A | 軸B | 結果 |
|---|---|---|---|
| C: `b.c` から `a.c` の `compute()` を呼ぶ | リポジトリ内 | ○ | **解ける** |
| C: `printf()` | リポジトリ外 | ○ | 解けない（`external`） |
| C++: オーバーロードされた `f(x)` | リポジトリ内 | **×** | 解けない（段階5が要る） |
| 任意: `obj.method()` | リポジトリ内 | × | 解けない（段階5が要る） |

##### 閉世界仮定 — 推測ではなく「仮定つきの演繹」にできる

**解析対象のソースツリーがプログラムの全体である**、という仮定を明示的に置く。この仮定の下では、リポジトリ内で候補が一意に定まる呼び出しは**推測ではなく演繹**になる。

そして**仮定が破れた箇所は検出できる**。候補数が答えを教える。

| リポジトリ内の候補数 | 意味 | `status` / `reason`（A-1） |
|---|---|---|
| **1個** | 閉世界仮定の下で一意 | `resolved` |
| **0個** | 定義がリポジトリ外にある（外部ライブラリ等） | `unresolved` / **`external`** |
| **2個以上** | 名前だけでは決まらない | `unresolved` / **`ambiguous`** |

**A-1 への拡張提案**: `reason` に **`external`** と **`ambiguous`** を追加する。A-1 は「`reason` に値を追加しても `status` の値域が汚れず既存クエリが壊れない」ことを採用理由に挙げており、この拡張はその設計意図どおりの使い方になる。

##### 実測: C の3ファイルを構文木の合成だけで解決した（2026-08-26）

`a.h`（宣言）/ `a.c`（`compute` と `static helper` を定義）/ `b.c`（`main` と別の `static helper` を定義し、`compute` / `printf` / `log_msg` を呼ぶ）を tree-sitter で解析し、定義表を合成して解決した結果。

| 呼び出し | 結果 |
|---|---|
| `a.c:3` `compute` → `helper` | **resolved** → `a.c:2`（同一ファイルの static が優先） |
| `b.c:5` `main` → `helper` | **resolved** → `b.c:3`（同上。**同名 static が2つあっても正しく分離できた**） |
| `b.c:5` `main` → `compute` | **resolved** → `a.c:3`（リポジトリ内で外部リンケージ定義が一意） |
| `b.c:5` `main` → `printf` | unresolved / `external` |
| `b.c:6` `main` → `log_msg` | unresolved / `external`（宣言だけあり定義なし） |

**`a.h` は解決に不要だった。** C は名前修飾が無く、外部リンケージのシンボルはプログラム全体で一意でなければならないため、**リポジトリ内の全定義を集めるだけで決まる**。ヘッダは裏付けにしかならない。

**破れる条件**（＝ `confidence` を下げる／`ambiguous` にする条件）: マクロで生成された関数名、条件コンパイルで定義が分岐している、ビルドから除外されたファイルに同名定義がある、関数ポインタ経由。

##### 閉世界の境界は二択ではなく「つまみ」（2026-08-26 追記）

手持ちに無いファイルを**外部から渡せる設計**にすれば、その分だけ解決が進む。処理は同じで、**渡されたソースを定義表に足すだけ**。新しい機構は要らない。

つまり **軸2 = ゼロは二択ではなく既定値**であり、閉世界の**境界がパラメータ**になる。

| レベル | 渡すもの | 得られるもの | 追加要求 |
|---|---|---|---|
| **0**（既定） | 何も渡さない | リポジトリ内で閉じるものだけ | なし |
| **1** | **追加のソースツリー**（別リポジトリの社内共通ライブラリ等） | **定義**まで到達。②-a がそのまま広がる | ソースツリーのパス1つ |
| **2** | システムヘッダ / SDK のヘッダ | **宣言**まで（定義は無い） | ヘッダのパス（実質インクルードパス） |
| **3** | ビルドコンテキスト | フル版 | フル版の要求 |

**レベル1と2は得られるものが質的に違う。** レベル2で得られる `printf` は `is_definition = false` の行き止まりになる。これは §7.7 の `[DllImport]` について既に設計済みの「切れた線」と同じ形で、コールグラフはそこで止まる（そしてそれが正しい）。シグネチャが分かり「これは自分たちのコードではない」と断定できる点に価値がある。

##### 実測: `stdio.h` を渡してみた（2026-08-26）— レベル2は割に合わない

| 項目 | 実測値 |
|---|---|
| ファイル | 34,649 バイト / 985 行 |
| **ERROR ノード** | **136** |
| `extern` 宣言の行数（grep） | **138** |
| **tree-sitter が抽出できた関数宣言** | **16**（ユニーク13） |
| `printf` | **取れた**（L363） |
| `fprintf` / `sprintf` / `snprintf` | **取れなかった** |

原因はマクロ。

```c
extern int printf (const char *__restrict __format, ...);              // 取れた
extern int fprintf (FILE *__restrict __stream, ...) __nonnull ((1));   // 取れない
extern int sprintf (char *__restrict __s, ...) __THROWNL;              // 取れない
```

`__THROW` / `__nonnull` / `__wur` / `__REDIRECT_NTH` を未知トークンとして扱い、宣言ごと ERROR に落とす。`printf` はたまたま属性マクロが付いていなかったから取れただけ。

さらに `stdio.h` 自身が **18個のヘッダを include** していた。1ファイル渡せば済む話ではなく、結局インクルードパスを渡すことになる＝軸2 = ゼロの放棄。

> **システムヘッダは tree-sitter が最も苦手とする入力である。** マクロ密度が実コードより遥かに高いため。

##### 代案: 標準ライブラリの関数名辞書

「`printf` は libc の関数だ」と言いたいだけなら、ヘッダをパースするより**名前の辞書を持つ**方が確実で安い。C89 / C99 / POSIX の関数名は数百件の静的データで済み、パース失敗の影響を受けない。同じ発想が他言語にも効く（Python の builtins と標準ライブラリ名、C# の BCL 主要名前空間、Node の組み込みモジュール）。

**`external` を「未知の外部」と「既知の標準ライブラリ」に分けられれば、一次解析には十分。**

##### 設計への反映

1. **境界をデータに持つ**。「解けなかった」ではなく「**この境界の外だから解けなかった**」と言えるようにする。境界を広げれば解けるものが増える、という関係が利用者から見える
2. **B-1 の `local <repo-name>` スキームがそのまま効く**。追加ソースツリーは別の repo-name を持てばよい
3. **新しい前提が1つ増える**: レベル2 を採る場合、**渡されたヘッダが実際のビルドで使われたものと同じ保証は無い**。バージョン違いのヘッダは誤った解決を生む。閉世界仮定と並べて明示・記録する
4. **境界を広げるのは既定にしない**。`external` のまま残った呼び出しの集計は「**このプロジェクトが依存する外部 API の一覧**」であり、一次解析では有用な出力。広げるとこの情報が失われるため、**目的に応じて利用者が選ぶつまみ**とする

##### 言語別: リポジトリ内解決の強さ

| 言語 | 強さ | 理由 |
|---|---|---|
| **C** | **強** | 名前修飾なし・オーバーロードなし。外部リンケージは全体で一意。`static` はファイル内で閉じる |
| **C#** | **強** | 名前空間 ＋ `using` が宣言されており、同一アセンブリ内は全ファイルの型宣言を集めれば決まる。ただしオーバーロードは段階5が要る |
| **Python** | 中 | import 先がパスで書いてある。動的要素（`getattr` / 条件 import / `sys.path` 操作）が破れ要因 |
| **TS/JS** | 中 | 相対パスは書いてあるが、拡張子省略・`index` 解決・`paths` / `baseUrl` が曖昧要因 |
| **C++** | **弱** | 情報がリポジトリ内に全部あっても、名前空間 ＋ オーバーロード ＋ ADL ＋ テンプレートで**名前だけでは決まらない**（軸B が × ） |

**C++ が弱いのは情報の所在ではなく言語規則の問題**。軸A と軸B が独立であることの実例。

#### ① 取れる（`confidence = high`）— 構文と位置だけで決まるもの

| 出力 | 中身 | 手段 |
|---|---|---|
| ディレクトリ構造マップ | パス階層、ファイル数、行数、拡張子別の言語構成 | ファイル走査のみ。tree-sitter すら不要 |
| 規模の偏り | フォルダ／ファイル別の行数分布、上位 N | 同上 |
| 関数・クラス・メソッドの一覧と位置 | `name` / `kind` / `file` / `start_line` / `end_line` | tree-sitter の構文ノード。[構想] §3.1 の `symbols` にそのまま入る |
| `import` / `include` / `using` の**記述** | 文そのものとその位置 | 構文ノード。※**解決先**は ② |
| 呼び出し**式**の一覧 | `caller_id`（囲っている関数）、`callee_expr`（式のテキスト）、位置 | `calls` の `callee_id` を空にした行（A-1 の `status = unresolved`） |
| コメントとその密度 | コメント行数／コード行数、`TODO` / `FIXME` / `HACK` の位置 | tree-sitter の comment ノード |
| 命名の語彙集計 | フォルダ名・ファイル名・識別子の語の頻度 | 文字列処理。機能推測の材料 |
| 分岐の数（複雑度の**近似**） | `if` / `for` / `while` / `case` / `catch` ノードの計数 | CFG 不要。厳密な循環的複雑度ではない |
| 変更のホットスポット | ファイル別の変更回数・最終更新日・著者数 | `git log`。**名前解決と無関係**に取れる。フル版には無かった軸（下記「副産物」） |
| テスト／生成コード／vendor の判別 | パス規約と生成物マーカーによる分類 | ヒューリスティック。誤分類は起きるので `confidence = medium` 相当 |

#### ②-a 決定的に解けるもの（`confidence = medium`）— 推測を含まない

> **2026-08-26 追記で新設。** 当初は②を一括で `low` にしていたが、**推測を含まないものが混ざっていた**。
> 段階1（スコープ解析）と、閉世界仮定の下で候補が一意に定まる解決は、**推測ではなく演繹**である。

| 出力 | 根拠 | 前提 |
|---|---|---|
| ファイル内の束縛（この識別子はどの宣言を指すか） | 段階1。木 ＋ 言語のスコープ規則で確定 | なし。**完全に確実** |
| リポジトリ内で候補が一意な呼び出し | 閉世界仮定 ＋ 軸B が ○ の言語（C / C#） | 閉世界仮定。破れは候補数で検出可 |
| `static` / ファイルローカルなシンボルの解決 | 同一ファイル内で閉じる | なし。**完全に確実** |
| import / include の解決先（相対パスで書かれているもの） | パス解決 | ファイルが実在すること |

**`high` ではなく `medium` にする理由**: 閉世界仮定に依存する行があるため。仮定が破れる条件（マクロ生成の名前、条件コンパイル、ビルド除外ファイルの同名定義）は列挙できるので、破れを検出した行だけを個別に下げる運用にする。

#### ②-b 名前一致による推測（`confidence = low`）

②-a に該当しない結線。**ここだけが推測。**

| 出力 | 何をするか | 何が解けないか |
|---|---|---|
| 粗いコールグラフ（軸B が × の言語） | `callee_expr` の識別子と関数名の一致で線を引く | オーバーロード、名前空間、同名メソッド（C++ が該当） |
| モジュール間の依存図（パスで書かれていないもの） | path alias / インクルードパス / 名前空間を名前で当てる | `-I` 追加パス、`paths` / `baseUrl`、外部パッケージ |
| 継承・実装関係 | `class Foo : Bar` の `Bar` を名前で当てる | どの `Bar` か（名前空間・同名クラス） |
| 宣言された型 | 型注釈・型名が**ソースに書いてあれば**文字列として拾う | 推論された型（`var` / `auto` / 注釈なし Python / TS の推論） |

**②-b の扱いに関する重要な帰結**（[構想] §2.3・§7.3 との整合）:

§2.3 は「**間違った答えが自信を持って出てくるツールは、答えが出ないツールより有害**」と決めている。名前一致による結線はまさにこのリスクの当事者であり、**L0（抽出層）では線を引かない**。L0 が出すのは事実（`callee_expr` の文字列）だけで、名前一致という**判断**は L2 / L3a 側に置き、利用者が明示的に有効化する「推測モード」として扱う。

**②-a はこの制約の対象外。** 演繹であって判断ではないため、L0 が `callee_id` を埋めてよい。「L0 に判断ロジックを入れない」に反しないのは、**閉世界仮定という明示された前提の下での確定**だから。前提が破れた行は `status = unresolved` と `reason`（`external` / `ambiguous`）で記録され、黙って消えることはない。

これは §7.3 のルール1「L0 に判断ロジックを一切入れない」と一致する。**フル版のために作った層分離が、簡易版でそのまま効く。**

#### ③ 取れない

- 正確なコールグラフ（仮想呼び出し、関数ポインタ、デリゲート、動的ディスパッチ）
- 「この関数の呼び元を漏れなく全部」
- 変数の読み書き箇所の網羅（データフロー）
- 未使用コードの断定
- 推論された型
- マクロ展開後のコード、条件コンパイルの有効枝（tree-sitter は `#ifdef` の内外を**両方**構文木に載せる。[構想] §4.7 の注意）
- 外部ライブラリのシンボル（依存を落とさせないので当然の帰結）
- リフレクション・動的呼び出し（`getattr` / `Type.GetMethod` / `eval`）※**フル版でも解けない**
- 言語をまたぐ呼び出し（FFI）※**フル版でも非スコープ**（D-6）

> 末尾2件は簡易版で失うものではない。**フル版との差分は ①②③ のうち ③ の上7件のみ。**

#### 副産物: Git 履歴という軸

「変更のホットスポット」は名前解決を一切必要としないのに、「どの辺りが詳細解析を要しそうか」（E-1 の用途）に直接効く。**フル版の構想には無い軸**であり、簡易版が単なる下位互換に留まらない可能性がある。E-4 の判断材料になる。

#### tree-sitter の実測（2026-08-26）

> **これは実測である。** 使用: `pip install tree-sitter tree-sitter-language-pack`（Python 3.11 の venv）。

| 項目 | 実測値 |
|---|---|
| バージョン | tree-sitter **0.26.0** / tree-sitter-language-pack **1.15.8** |
| 対応言語数 | **371** |
| 対象6言語（c / cpp / csharp / python / typescript / javascript） | **全て対応**（go / rust / java / tsx も） |
| インストール | **1コマンドで完了。追加依存なし。C コンパイラ不要**（プリビルド wheel） |
| 容量 | 計 約 **7.6 MB**（`tree_sitter` 2.0MB + `tree_sitter_language_pack` 5.6MB） |
| ライセンス | 両パッケージとも **MIT**。同梱依存の SBOM 198件も全て permissive（MIT / Apache-2.0 系が大半、MPL-2.0 が1件、**GPL 系なし**） |

**→ 軸1 =「軽い」は成立する。** 以下、実測で分かったことを2点記録する。

##### 実測1: エラー回復の実力と限界

C の3関数を含むソースを壊して測った。

| ケース | ERROR ノード数 | 検出できた関数 |
|---|---|---|
| 正常 | 0 | **3 / 3** |
| 式が壊れている（`int x = ;`） | 1 | **3 / 3** |
| 未知のマクロ修飾（`API_EXPORT int b(...)`） | 1 | **3 / 3** |
| **括弧の不均衡** | 2 | **0 / 3** ← 全滅 |
| ファイル途中で切れている | 1 | 1 / 2 |

**ローカルなエラーからは回復する。括弧の不均衡は後続を巻き込んで全滅する。**

**→ 設計項目（新規）: ERROR ノード数を数えて `confidence` に反映する。**
これを入れないと、木が壊れているのに綺麗な結果が出て §2.3 の「静かな取りこぼし」がそのまま起きる。A-2 の `confidence` enum の判定材料として、抽出器がファイル単位で ERROR ノード数を持つ。

##### 実測2: 抽出はクエリで書く（Python 側で木を走査しない）

1.7 MB / 20,000行の C で計測。

| 測定 | 結果 |
|---|---|
| パース | **0.77 秒**（2.3 MB/s） |
| 抽出：Python 側で木を全走査 | 2.37 秒 |
| 抽出：**tree-sitter のクエリ（C 実装）** | **0.26 秒**（約9倍速い） |

パースより Python 側の走査の方が遅い。**抽出は S 式クエリで書く**を方針とする。

##### 実測3: `#ifdef` の両枝が出る（§4.7 の注意書きの実証）

`#ifdef DEBUG` / `#else` で同名関数を2回定義した C を食わせたところ、**両方が `function_definition` として出た**（L3 と L5）。プリプロセッサを解決しないため、同名関数が2つ見える。③の「条件コンパイルの有効枝は取れない」の具体的な現れ方。

##### 言語ごとにノード名が違う（実測）

同じ「関数」でも `function_definition`（C / C++ / Python）、`method_declaration`（C#）、`function_declaration` / `arrow_function`（TS / JS）と異なる。**6言語を `symbols.kind` に正規化する対応表は自作**になる。文法は書かなくてよいが、**クエリと正規化は自分の仕事**で、言語ごとの作業は消えない。

#### 実測: ②-a / ②-b の比率（Phase 0 相当、2026-08-26）

> **これは実測である。** [構想] §8.3 の推奨セットを shallow clone し、**tree-sitter の構文木のみ**を入力として呼び出し1件ごとに分類した。ビルドは一切行っていない（＝軸2 = ゼロの条件そのもの）。

##### 方法

呼び出し式を**形**で分類し（ここは厳密。推測を含まない）、単純識別子の呼び出しだけを閉世界仮定の下で解決した。

| 分類 | 定義 |
|---|---|
| **②-a** | 単純識別子で候補が一意 ／ 同一ファイルの `static` が優先 ／ `self.` `this.` で囲むクラスに存在 |
| **②-b** | 単純識別子だが候補が複数 ／ `self.` だが自クラスに無い（継承先の可能性） |
| **macro** | C の関数形式マクロ呼び出し（`reason = macro_expanded` 相当） |
| **external** | 候補ゼロ（定義がリポジトリ外） |
| **③ 要型解決** | 他オブジェクトのメンバ呼び出し（`a.b()` / `p->b()`） |
| **③ 間接** | 呼び先が式（関数ポインタ等） |

**限界**: 「その形なら解けるはず」を数えたものであり、**正解データとの突合はしていない**。②-a の的中率そのものは別途検証が要る（下記「要実測」）。

##### 結果

| プロジェクト | 言語 | 呼び出し | **②-a** | ②-b | macro | external | ③型解決 | ③間接 |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| **cJSON** | C | 526 | **66.5%** | 0.0% | 12.4% | 16.2% | 4.9% | 0.0% |
| **cmark** | C | 1,381 | **59.9%** | 0.0% | 23.2% | 11.4% | 3.5% | 2.0% |
| **tinyxml2** | C++ | 909 | **22.2%** | 19.9% | 15.4% | 6.3% | 29.5% | 6.7% |
| **requests** | Python | 986 | **17.2%** | 2.9% | 0.0% | 40.5% | 39.2% | 0.1% |
| **commander.js** | JS | 758 | **27.3%** | 0.0% | 0.0% | 7.4% | 64.9% | 0.4% |
| **FluentValidation** | C# | 1,022 | **5.2%** | 12.3% | 0.0% | 12.8% | 69.1% | 0.6% |

パースは健全だった（Python / JS / C# は **ERROR ノード 0**。C / C++ で 12〜94 ノード）。

##### 読み取れること

**1. 言語で桁が違う。単一の答えにならない。**

C は約6割が決定的に解ける。C++ / Python / JS は1〜3割、C# は測定値上は5%。**簡易版は「言語によって出せるものが違うツール」になる。** A-3（全テーブルに `lang`）と A-2（`confidence`）がまさにこの非対称性の受け皿になる。

**2. C は実測値より実質的に強い。**

`macro` 12〜23% は**呼び先のマクロ定義が特定できている**（展開後は不明だが、どのマクロかは分かる）。`external` の中身は `strcmp` / `strlen` / `printf` / `assert` — **ほぼ libc**。標準ライブラリの関数名辞書を持てば「既知の外部」とラベルできる。

> **C は呼び出しの9割以上について「正体が分かる」。** 本当に不明なのは ③（型解決＋間接）の 5〜7% のみ。

**3. Python の `external` 40.5% は大半が builtins。**

`isinstance`(71) / `getattr`(20) / `int`(16) / `cast`(18)。辞書化すれば「不明」ではなくなる。ただし**②-a に昇格するわけではない**（外部だと分かるだけ）。

**4. `self.` / `this.` の解決は効く。**

commander.js の ②-a 27.3% のうち **24.3% が `this.`** 経由。型推論なしで囲むクラスから解決できる。実装すべき機能として費用対効果が高い。

**5. C# の 5.2% は過小評価。** メンバ呼び出し（706件）の内訳を追加測定した。

| レシーバの形 | 割合 |
|---|---:|
| **型名（static 呼び出し）** | **28.1%** |
| 式（チェーン等） | 26.0% |
| **型が明示 & 型がリポジトリ内** | **19.2%** |
| 型が明示だが型がリポジトリ外 | 16.8% |
| 宣言が同一ファイルに見つからない | 5.5% |
| `var` 宣言（型推論が要る） | 4.2% |

`var` はわずか 4.2%。**型はソースに書いてある**。static 呼び出しと明示型レシーバを実装すれば、C# の ②-a は **5.2% → 概ね 35〜40%** まで上がる見込み。

> **ただし注意**: 到達先の多くは `IRuleBuilder<T>` のような**インタフェース／抽象メソッドの宣言**であり、実装には届かない。これは §7.7 の「切れた線」と同じ形（フル版でも仮想ディスパッチは別途解決が要る）。

##### E-1 への含意（提案、未決定）

**着手順はフル版と逆にすべき。** [構想] §10.1 はフル版の Phase 1 を C# にしたが、その理由は「C/C++ は前提条件（cmake / clang）の準備が重いから」だった。**簡易版では軸2 = ゼロにより準備の重さが消えている**ため、この理由は成立しない。逆に**最も価値が出るのは C** であることが実測で分かったので、簡易版は **C から始める**のが筋。

**出力の主役も言語で変える必要がある。**

| 言語 | 主役にできるもの |
|---|---|
| **C** | **コールグラフ**（②-a 6割＋マクロ＋標準ライブラリ辞書で9割が判明） |
| C++ / C# | 構造マップと分布が主役。コールグラフは `confidence` つきの補助 |
| Python / JS | 同上。外部依存の一覧（`external` の集計）は有用 |

#### 要実測（残り）

- [x] ~~tree-sitter が対象言語のプリビルド wheel を提供しているか~~ → **確認済み（上記）**
- [ ] テンプレート・マクロを多用した**実際の** C++ で構文木がどこまで壊れるか（合成コードでは確認済み。実プロジェクトは未）
- [ ] 実プロジェクト（[構想] §8.3 の推奨セット）での関数一覧の取りこぼし率
- [x] ~~**②-a と ②-b の比率**~~ → **測定済み（上記）**。言語で桁が違い、単一の答えにならないことが判明した
- [ ] **②-a の的中率**（形の上では解けるとした行が、実際に正しい先を指しているか）。正解データとの突合が要る。[構想] §8.5 の通り **cJSON に対してのみ人手でゴールデンデータを作る**
- [ ] **②-b の誤結線率**（`FirstChild` / `Configurable` のような多重定義が実際どれだけ誤るか）
- [ ] **段階1（スコープ解析）の実装コスト**。6言語分の言語規則をどこまで書くか。簡易版の実装量の本体であり、着手順を決める材料
- [ ] **C# の追加測定の裏付け** — static 呼び出し・明示型レシーバを実装した場合に ②-a が本当に 35〜40% になるか

- [x] 線引きは確定（実測による補正は残る）

### E-4. 簡易版とフル版の関係 — **決定済み（2026-08-26）**

**スキーマは共有する。DB は混ぜない。** 詳細と根拠は「決定事項」を参照。

**この決定に伴い、フル版（`CODE_ANALYSIS_CONCEPT.md`）に追随改訂を入れた。** 凍結は維持したまま、§3.1 / §3.2.1 の欠陥を修正している。改訂は「簡易版に合わせる」ためではなく、**フル版単体でも成立していなかった箇所の修正**。

- [x] **決定**（2026-08-26）

## F. 文書レビューで出た未決事項 — **未決（2026-08-27）**

**経緯**: E 群の完了を受け、**簡易版の構想文書を起こす前に**全4文書と `tools/` を突き合わせた。
決定内容そのものの誤りは見つからなかったが、**決定を文書に落とす過程で埋まっていない穴**が出た。

**F-1 〜 F-6 は 2026-08-27 にすべて決着した**（結論は「決定事項」）。列定義に関わるため簡易版の構想文書より先に決めた。
**F-13 も決着した（2026-08-27）。残るは F-7 〜 F-12 / F-14 〜 F-16 の記述の不整合。**
後から変えるとデータの作り直しになる（E-1「他言語で空く軸の先行確認」・A-3 と同じ構造）。
F-7 以降は記述の不整合で、決定内容には影響しない（修正だけで済む）。

| ID | 項目 | 影響 |
|---|---|---|
| ~~**F-1**~~ | ~~フル版と簡易版でテーブル名・列名が揃っていない~~ | **決定済み（2026-08-27）** |
| ~~**F-2**~~ | ~~`reason` を `status = resolved` の行にも書いている~~ | **決定済み（2026-08-27）** |
| ~~**F-3**~~ | ~~主キーと位置情報に列が足りず、静かな衝突が起きる~~ | **決定済み（2026-08-27）** |
| ~~**F-4**~~ | ~~「閉世界の境界」「既知の標準ライブラリ」を記録する列が無い~~ | **決定済み（2026-08-27）** |
| ~~**F-5**~~ | ~~`files` テーブルの列が未定義。git メトリクス欠落時の挙動も未定~~ | **決定済み（2026-08-27）** |
| ~~**F-6**~~ | ~~`confidence` の決め方が未定~~ | **決定済み（2026-08-27）** |
| F-7 | E 群の進捗記述が古い（本ファイル・HANDOFF） | 記述のみ |
| F-8 | [構想] の凍結ボックス・§4.7・§7.6 が追随改訂に追いついていない | 記述のみ |
| F-9 | C-1 が「終わらないと §3.1 を確定できない」と書いたまま、§3.1 は「確定」 | 記述のみ |
| F-10 | 5レポートの「コンポーネント依存」は C# では描けないと実測されている | 決定の再確認 |
| F-11 | C# の見込み 35〜40% と実測 31.8% が突き合わされていない。要実測の消し込みも未 | 記述のみ |
| F-12 | 同じ数値が測定スクリプトごとに違う（53 / 55 / 56 件） | 記述のみ |
| ~~F-13~~ | ~~[現実] が挙げた宿題3件が未決リストに入っていない~~ | **決定済み（2026-08-27）** |
| F-14 | HANDOFF §3 の A-2 / B-1 の記述が訂正前のまま | 記述のみ |
| F-15 | 簡易版の Python 3.10 下限の根拠が jedi（フル版 Phase 3）由来のまま | 前提の根拠 |
| F-16 | E-3 の「要実測」5件が未消化のまま E 群を完了としている | スコープ |

---

### F-1. フル版と簡易版でテーブル名・列名が揃っていない — **決定済み（2026-08-27）**

> 結論は「決定事項」を参照。**`refs` に統合し、正本を `docs/SCHEMA.md` に一本化する。**
> 以下は決定に至った問題の記述として残す。

E-4 決定1 は「**スキーマは共有する**」だが、両文書が書いている表は一致していない。

| | フル版 [構想] §3.1 | 簡易版 E-1 |
|---|---|---|
| テーブル | `symbols` / **`calls`** / `var_refs` / `comments` | **`files`** / `symbols` / **`refs`** / `comments` |
| 呼び出しの列名 | `caller_id` / `callee_id` / `callee_expr` | `src_id` / `dst_id` / `dst_expr` |
| 関係の種別 | `call_kind`（`direct` / `virtual` / `implicit`） | `kind`（`call` / `import`） |
| 片方にしか無い列 | `qualified_name` / `is_definition` / `storage_class` | `branch_count` / `parse_errors` |

**`kind` は名前が衝突している。** 簡易版の `refs.kind` は「呼び出しか import か」、フル版の `call_kind` は「直接か仮想か」で、**別の問い**。共有スキーマなら両方が要る。

また簡易版の4テーブルは `lang` / `extractor` / `snapshot` を明記していない（A-3 は全テーブル必須）。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### F-2. `reason` を `resolved` の行にも書いている — **決定済み（2026-08-27）**

> 結論は「決定事項」を参照。**「呼び方」を `dispatch` へ切り出し、`reason` は障害専用に戻す。**

A-1 と [構想] §3.1.1 は「`reason` は `status = unresolved` のときのみ埋める」と決め、§7.6 の TSV 例にも「`resolved` の行では `reason` を空にする」と明記している。

しかし E-1 の実測表は **`resolved` / `macro_expanded` 65件**（cJSON）を出しており、`tools/emit-generic-schema.py` も
`status="resolved", reason="macro_expanded"` を書いている。

**どちらかが正しくない。** マクロ経由の呼び出しは「呼び先のマクロ定義は特定できている」ので `resolved` にする価値があり、
同時に「マクロを経由した」という事実も残したい。しかし今の `reason` は「**なぜ解けなかったか**」の列である。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照（③に近い形。ただし新列ではなく F-1 の `dispatch` に寄せた）

### F-3. 主キーと位置情報に列が足りない — **決定済み（2026-08-27）**

> 結論は「決定事項」を参照。**位置は範囲で持ち、ローダーは衝突を黙って捨てない。**

**発見1（シグネチャ無しのID衝突で 17.7% が消える）と同じ形の穴が、まだ2箇所ある。**

| 箇所 | 穴 | 起きること |
|---|---|---|
| 簡易版 `refs` | `col` が無い（`file` / `line` のみ） | `f(g(x))` のように**同じ行に2つの呼び出し**があると区別できない。フル版 `calls` の主キーは `(caller_id, file, line, col, extractor, snapshot)` なので、共有スキーマにするなら `col` が要る |
| `comments` 主キー | `(file, start_line, extractor, snapshot)` に `col` が無い | 同じ行に2つのコメントがあると1つ消える |
| 簡易版 `symbols` | 位置が `start_line` / `end_line` のみ | §7.4-1「位置情報は範囲で持つ（`line` / `col` / `end_line` / `end_col`）」を満たさない。§7.4 は「あとから追加するとデータの作り直しになる」ものとして挙げている |
| `files` | 主キーの記載が §3.1.5 に無い | — |

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### F-4. 閉世界の境界と「既知の標準ライブラリ」を記録する列が無い — **決定済み（2026-08-27）**

> 結論は「決定事項」を参照。**`analysis_run` を追加し、辞書は `dst_id` に座標で入れる（`status` は `unresolved` のまま）。**

E-3 は設計への反映として「**境界をデータに持つ**」「『解けなかった』ではなく『**この境界の外だから解けなかった**』と言えるようにする」と書き、
E-1 は「**標準ライブラリの名前辞書**」を初版必須に入れた（`external` を「既知の標準ライブラリ」と「未知の外部」に分ける）。

**しかし最小スキーマの4テーブルに、それを書く列が無い。** `reason = external` までしか言えない。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### F-5. `files` テーブルが未定義。git が無いツリーでの挙動も未定 — **決定済み（2026-08-27）**

> 結論は「決定事項」を参照。**`git_state` は3値（`full` / `shallow` / `none`）。縮退を明示する。**

E-1 の記述は「パス、`lang`、行数、コメント行数、`parse_errors`、**git メトリクス**」だけで、列名も値域も無い。

さらに **E-2 軸2 = ゼロは「zip を展開しただけのツリー」を対象に含めている**。この場合 git メトリクスは取れない。
B-4 は `snapshot` について `nogit-YYYYMMDDThhmmss` を用意しているが、**変更回数・著者数の欠落**については何も決めていない。

**これは5レポートの「ホットスポット」（規模 × 変更頻度 × 分岐数 × doc 欠落率）を直撃する。** 4軸のうち1軸が消える。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### F-6. `confidence` の決め方が未定 — **決定済み（2026-08-27）**

> 結論は「決定事項」を参照。**欠落は `files`、誤りは `confidence` の2段構え。**

決まっているのは値域（A-2、`high` / `medium` / `low`）と、**②-a は `medium`**、doc コメントの規則（明示的な doc 記法なら `high`）だけ。

| 未定の点 | 内容 |
|---|---|
| `unresolved` 行での意味 | §3.1.2 は `confidence` を「**解決できたと言っているが**どれだけ信じてよいか」と定義している。`unresolved` の行では意味が無いはずだが、`tools/emit-generic-schema.py` は `external` に `high`、`ambiguous` に `low` を入れている。「その理由がどれだけ確からしいか」に読み替えるなら定義を書き換える必要がある |
| ERROR ノードの反映規則 | E-3 は「**ERROR ノード数を数えて `confidence` に反映する**」を L0 の必須要件としたが、閾値も、ファイル単位の `parse_errors` から行単位の `confidence` へどう落とすかも決めていない。**括弧不均衡では関数が 0/3 になる**という実測がある以上、ここが空だと「静かな取りこぼし」がそのまま残る |
| ②-a の破れの反映 | 「破れを検出した行だけ個別に下げる」とあるが、下げ先（`low` か）と検出方法が未定 |

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

---

### F-7 〜 F-16（記述の不整合・取りこぼし）

| ID | 箇所 | 内容 |
|---|---|---|
| **F-7** | 本ファイル冒頭 ⚠ ／ E 群冒頭 ／ HANDOFF §0・§4・§6 | E 群の進捗が「残るは E-1 / E-3 / E-4」「残るは E-4 のみ」「次の一手は E-1」のまま。**2026-08-27 に修正済み**（各所を「全件決着」「次は簡易版の構想文書」に更新） |
| **F-8** | [構想] 冒頭の凍結ボックス | 「簡易版の抽出結果は **`confidence = low` として同じテーブルに入る**」が残っている。E-4 決定2（DB は混ぜない）・決定3（`confidence` で区別しない）と矛盾。直下の「追随改訂について」だけが更新されている |
| | [構想] §4.7 | 「tree-sitter を主力にはしない」のまま。E-2 が**この位置づけを覆した**ことが本文に無い（凍結中の本文なので、注記で足りるかは要判断） |
| | [構想] §7.6 | 「現状の**3テーブル**（`symbols` / `calls` / `var_refs`）」「`each table T in {symbols, calls, var_refs}`」が改訂4（`comments` 追加）に未追随。ファイル配置例にも `.comments.tsv` が無い |
| **F-9** | C-1 | 「**これが終わらないと §3.1 のスキーマを初版として確定できない**」と書いたまま中断。一方 §3.1 は「**ステータス: 確定**」。フル版凍結中なので実害は無いが、再開時に必ず衝突する |
| **F-10** | E-1 最小の出力 4番 | 「コンポーネント依存 — **C も C# も出せる粒度**」に対し、同じ E-1 の実測が「**C# の内部コンポーネント依存図は、この最小構成では描けない**」と結論している。実測は改良A〜D の**前**のDBで行っており（`resolved` 5.5%）、改良後（31.8%）で描けるかは未測定。**5レポートを最小要件と呼ぶ前に再測定が要る** |
| **F-11** | E-3 の C# 見込み | 「static 呼び出し＋明示型レシーバで **35〜40%** まで上がる見込み」に対し実測は **31.8%**。見込みが外れた理由が書かれていない。要実測「C# の追加測定の裏付け」も未チェックのまま |
| **F-12** | 実測値 | FluentValidation の解決済み件数が測定ごとに **53件（Phase 0、5.2%）/ 55件（`emit-generic-schema.py`）/ 56件（`measure-improvements.py`、5.5%）**。`needs_type` も 704 / 705 で違う。CLAUDE.md は「数字の出典はすべて再現できる」と書いているので、差の理由（分類規則の違い）を注記するか揃えるかが要る |
| **F-13** | [現実] §4.1 / §4.3 / §4.4 | 本ファイルの未決リストに載っていない宿題が3件ある。① `imports` テーブル（「§3.1 に含まれていない（未決事項。OPEN_DECISIONS.md 参照）」と書かれているが**本ファイルに項目が無い**。簡易版は `refs.kind = import` で解いており、フル版だけ宙に浮いている）② **`config` 列**（「初版から必要」「確定済み」とあるが §3.1 に列が無い）③「**正規シンボル1行＋宣言箇所の別テーブル**は全言語で必要」（§3.1 に無い。C/C++ の宣言/定義分離、C# の `partial` が該当） |
| | [現実] §4.1 の再掲スキーマ | `visibility` / `visibility_source` / `container_id` / `via_lambda` / `comments` が入っていない。「正はこれではない」と断ってあるが、追随したのは `cpp_linkage` の1行のみ |
| **F-14** | HANDOFF §3 | A-2 の行が「**簡易版の結果は `low`**」のまま（決定3 で訂正済み）。B-1 の行が「簡易版では過剰かもしれない（**要検討**）」のまま（発見1 でシグネチャ必須と決まり、本ファイルにこの「要検討」に対応する項目は無い）。本文も「同じテーブルに混在させられる／これを活かすかどうかが E-4」のままだった（E-4 決定2 は**混ぜない**）。**いずれも 2026-08-27 に修正済み** |
| **F-15** | E-2 軸1 | 「Python **3.10+**」の根拠が [構想] §9.3.1 の **jedi 0.20.0（`>= 3.10`）**。簡易版は jedi を使わないので、この根拠は効かない。tree-sitter / tree-sitter-language-pack の `Requires-Python` は**未実測**。§7 環境メモにも記載が無い |
| **F-16** | E-3「要実測（残り）」 | 5件が未チェックのまま E 群を完了としている。うち **②-a の的中率**（形の上で解けるとした行が実際に正しい先を指しているか）は**すべての比率がその上に載っている前提**であり、正解データとの突合が無い。§8.5 は「cJSON に対してのみ人手でゴールデンデータを作る」と決めているので、**簡易版の本設計に入る前に一度やるか、明示的に後回しと宣言するか**を決める必要がある |

**F-7 は 2026-08-27 に修正済み。F-13 は決定事項へ移した。F-8 〜 F-12 / F-14 〜 F-16 は未着手。**

- [ ] 未決（記述の不整合。`SCHEMA.md` の作成後に片付ける）


## G. `SCHEMA.md` を書く前に埋める列の細目 — **全件決着（2026-08-27）**

> 結論は「決定事項」を参照。**G-8 と G-10 は当初の提案を訂正した**（`(attached_id, file)` は一意にならない／`run.tsv` は縦持ち）。

**経緯**: F-1 〜 F-6 / F-13 の決定を `SCHEMA.md` に落とす前に、決定同士を突き合わせた。
**決定同士の矛盾は無かった**が、**直接の矛盾が1件（G-1）と、決定が新たに生んだ穴が12件**出た。

**列の値域・型・意味の粒度**なので、これを埋めないと `SCHEMA.md` が「曖昧なまま確定した仕様」になる。
F 群と同じ理由（後から変えるとデータの作り直し）で先に決める。

| ID | 項目 | 種別 |
|---|---|---|
| **G-1** | `container_id` — 「無ければ空」（[構想] §3.1）と「C はファイル」（E-1）が矛盾 | **直接の矛盾** |
| **G-2** | `files` と `symbols(kind='file')` の関係が未定義 | 決定が生んだ穴 |
| **G-3** | 追加ソースツリーの `symbols.file` が何からの相対パスか未定 | 同上 |
| **G-4** | `refs` / `symbols` の位置が**何の範囲**か未定 | 同上 |
| **G-5** | `via_lambda` が bool かネスト深さか未定 | 同上 |
| **G-6** | `refs.kind` の変数参照系に `unknown` が無い（旧 `access` にはあった） | 同上 |
| **G-7** | `guard` の非対象言語を「空」と書いたが §7.6 の規則では `not_applicable` | 同上 |
| **G-8** | `comments.attached_id` が `symbols` の N 行化で一意に定まらない | 同上 |
| **G-9** | `analysis_run` は `lang` を持てない（A-3 の例外が要る） | 同上 |
| **G-10** | 実行単位の情報を L0 → L1 へ運ぶ経路が無い | 同上 |
| **G-11** | `analysis_run` に `files` から導出できる列を作ろうとしている | 冗長 |
| **G-12** | [構想] §3.1 の `container_id` の参照先（§3.1.4）が内容と対応していない | 記述のみ |

---

### G-1. `container_id` — 直接の矛盾

| 出典 | 記述 |
|---|---|
| [構想] §3.1 | `container_id` — 入れ子の親シンボル。**無ければ空** |
| E-1 汎用スキーマ | `container_id`（何の中にあるか）— **C での対応: ファイル** |

C のトップレベル関数は「空」なのか「ファイルシンボル」なのか。`tools/emit-generic-schema.py` は**ファイルシンボルを立てて親にしている**。

> **提案**: **常に埋める**（トップレベルはファイルシンボルを親にする）。空になるのはファイルシンボル自身のみ。
> **理由**: レポート1「概観」とレポート4「コンポーネント依存」はディレクトリ／名前空間単位で集約するので、シンボル → ファイル → ディレクトリの経路が要る。`file` 列でも辿れるが、**C# の 名前空間 → 型 → メソッド の鎖と同じ機構で辿れる方が素直**。E-1 の実測も既にこの形。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### G-2. `files` と `symbols(kind='file')` の関係

ファイルが2箇所に現れる。二重に見えるが、消すと壊れる。

| テーブル | 役割 | 消せない理由 |
|---|---|---|
| `files` | ファイルという**実体の計測値**（行数・parse_errors・git） | レポート1 / 3 / 5 の土台 |
| `symbols(kind='file')` | **参照の対象になれる名前** | `refs.src_id`（`import` 行の起点）と `container_id` の終端がこれを要求する（G-1） |

> **提案**: 両方持つ。対応規則を明記する — `symbols.id = local <repo> <snapshot> <path>/`（末尾 `/` が file を表す）、`symbols.file = files.file`。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### G-3. 追加ソースツリーの `symbols.file`

F-5 決定5 で「追加ソースツリーは `symbols` にだけ行を立てる」と決めたため、**その行の `file` が何からの相対パスか**が宙に浮いた。解析対象のルートからでは表現できない。

> **提案**: `symbols.file` は「**その `repo-name` のルートからの相対パス**」とする。`repo-name` は `id` に入っているので `(id, file)` で一意。**`files` に対応行が無いのは正常**（追加ツリーは計測対象外）。`SCHEMA.md` に「`symbols.file` は `files.file` への外部キーではない」と明記する。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### G-4. 位置が「何の範囲」か

F-3 は `start_line` / `start_col` / `end_line` / `end_col` の**形式**を決めたが、**何を囲む範囲か**を決めていない。F-3 の主キー一意性の議論（`f(x)(y)` は終了位置で分離できる）も、F-6 の「ERROR ノードと同じ行／隣接行なら `low`」も、この定義に依存している。

> **提案**:
> - **`refs`** = **参照式全体**（`p->f(a,b)` 全体、`#include "x.h"` の行全体）。F-3 の一意性の根拠がこれ
> - **`symbols`** = **宣言／定義全体**（修飾子・属性を含む先頭から、本体の閉じ括弧まで）。doc コメントの隣接判定は `start_line - 1` を見る
> - **`comments`** = コメント（または docstring リテラル）全体
> - **名前トークンだけの位置は初版で持たない**。§7.4-1 が要求しているのは範囲であって名前位置ではない

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### G-5. `via_lambda` の型

E-1 は「`via_lambda`（**またはネスト深さ**）」と書いたまま決めていない。[構想] §3.1 は「無名関数の中からの呼び出しか」と bool 前提の説明。

> **提案**: **`lambda_depth`（整数）に改める。** 0 = ラムダの外、1 以上 = ネスト深さ。
> **理由**: bool は `lambda_depth > 0` で導出できるが、**逆はできない**。記録コストは同じ（抽出時に数えるだけ）。JS の 31.3% を扱うとき、コールバックの入れ子の深さは「読みにくさ」の指標になりうる。
> `via_lambda` という名前は bool を想起させるので、名前ごと変える。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### G-6. `refs.kind` の変数参照系に `unknown` が無い

F-1 で `var_refs` を畳んだとき、旧 `access` の値域（`read` / `write` / `readwrite` / `address_of` / **`unknown`**）のうち **`unknown` が落ちた**。

> **提案**: `var_unknown` を値域に加える。読みか書きか判定できない参照（複雑な左辺式など）を `var_read` に丸めると**嘘になる**（§2.3）。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### G-7. `guard` の非対象言語 — 「空」ではなく `not_applicable`

F-13 で「他言語は空」と書いたが、**§7.6 の規則に反している**。

> §7.6: 「空」と `not_applicable` は別物。前者は**列が不要**、後者は「**その言語に概念が無い**」という積極的な記録

> **提案**:
> - Python / JS / TS → **`not_applicable`**（条件コンパイルという概念が無い）
> - C / C++ / C# で条件の外にある行 → **空**（この行には該当しない）
>
> [構想] §3.1 が `storage_class` を「C/C++ 固有。他言語は `not_applicable`」と書いているのと同じ扱いに揃える。**F-13 の記述はこの点で訂正。**

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### G-8. `comments.attached_id` の一意性

F-3 決定4 / F-13③ で `symbols` が 1シンボル N 行になったため、**`attached_id`（id だけ）では対応する行が一意に定まらない**。

> **提案**: `attached_id` は id のみを持つ。**結合キーは `(attached_id, file)`** とし、doc コメントは常に同一ファイル内の隣接規則で付くため一意に決まる、と `SCHEMA.md` に明記する。列は増やさない。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### G-9. `analysis_run` は `lang` を持てない

A-3 は「`lang` / `extractor` / `snapshot` を**全テーブル**に」と決めたが、`analysis_run` の1行は**多言語にまたがる**。

> **提案**: **A-3 の明示的な例外**として `SCHEMA.md` に書く。`analysis_run` は `lang` を持たない。言語構成は `files.lang` の分布で分かる。
> 暗黙に落とすと、A-3 を読んだ人が「漏れ」と判断して足してしまう。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### G-10. 実行単位の情報を L0 → L1 へ運ぶ経路が無い

§7.6 の L0 は**ソースファイル単位**の TSV で、メタ情報行が持つのは `snapshot` / `extractor` / `lang` の3つだけ。`analysis_run` の中身（境界レベル・追加ツリー・辞書・`git_state`・`config`）を運ぶ経路が無い。

> **提案**: `facts/raw/run.tsv`（1行）を置く。**抽出器ではなくオーケストレータ（CLI）が書く**。取り込み時に `analysis_run` へ入れる。
> B-3 の「ジョブ設定ファイル（TOML）」とは別物（**設定**の記述 vs **実績**の記録）である点を明記する。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### G-11. `analysis_run` の冗長列

F-6 で「`analysis_run` に『ERROR を含むファイルが存在するか』を持つ」と書いたが、**`files` から導出できる**（`SELECT COUNT(*) FROM files WHERE parse_errors > 0 OR missing_nodes > 0`）。

> **提案**: 列を作らない。**`analysis_run` は入力（設定）だけを持ち、結果の要約は持たない。**
> ただし `load_errors`（F-4、主キー衝突件数）は `files` から導出できない実績値なので残す。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### G-12. [構想] §3.1 の参照先の誤り

`container_id -- 入れ子の親シンボル。無ければ空（§3.1.4）` とあるが、**§3.1.4 は `linkage` → `visibility` の節**で `container_id` を説明していない。G-1 の決定を書く先を新設するか、参照を外す。記述のみ。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照


## H. G 群決着後の再突き合わせ — **全件決着（2026-08-27）**

> 結論は「決定事項」を参照。**H-1（`analysis_run` の鍵）は設計として成立していなかったため作り直した。**

**経緯**: G 群を決めた直後にもう一度全決定を突き合わせた。**G 群自身が生んだ穴と、F 群の見落としが9件**出た。
**H-1 は設計として成立していない**（鍵が定義できない）。

| ID | 項目 | 種別 |
|---|---|---|
| **H-1** | **`analysis_run` の鍵が成立しない**（`extractor` は言語ごと ＋ 縦持ちで主キーが変わる） | **設計の破綻** |
| **H-2** | G-2 と G-3 が `symbols.file` について矛盾 | 矛盾 |
| **H-3** | `symbols.confidence` の対象列が F-1 の列を網羅せず、ERROR 判定の基準行も未定 | 未定 |
| **H-4** | `lambda_depth`（整数）は `not_applicable` を表せない | 型の衝突 |
| **H-5** | `dispatch` / `lambda_depth` / `attached_*` の非該当時の値が未定 | 未定 |
| **H-6** | `address_of` だけ `var_` 接頭辞が無い | 命名 |
| **H-7** | `run.tsv` が §7.6 のメタ情報行の形式に従っていない | 形式 |
| **H-8** | F-1 決定事項のテーブル一覧が古い（以降の決定が反映されていない） | 記述 |
| **H-9** | `attached_id` / `attached_line` が `kind = doc` 限定であることが明記されていない | 記述 |

---

### H-1. `analysis_run` の鍵が成立しない — **設計の破綻**

F-4 は「主キーは `(extractor, snapshot)`。**A-3 が全テーブルに持たせた2列がそのまま外部キーになる**」と決めたが、**2つの理由で成立しない。**

#### 原因1: `extractor` は「実行」ではなく「言語」の単位

| 出典 | 値 |
|---|---|
| [構想] §7.6 のメタ情報行 | `extractor=libclang-18.1.0 lang=c` |
| `tools/emit-generic-schema.py` | `tree-sitter-c` / `tree-sitter-c-sharp` |

**C と C# を1回の実行で解析すると `extractor` は2種類になる。** 一方 `boundary_level` / `root` / `git_state` は実行に1つ。
`(extractor, snapshot)` を鍵にすると、**1回の実行が2つの鍵に割れる**。

#### 原因2: 縦持ち（G-10）にすると `(extractor, snapshot)` は一意でなくなる

`analysis_run(extractor, snapshot, key, value, seq)` では1実行あたり十数行になる。
G-10 は「F-4 の決定内容（主キーは `(extractor, snapshot)`）は変わらない」と書いたが、**主キーは必ず変わる。**

#### 検討した案

| 案 | 内容 | 評価 |
|---|---|---|
| a | `snapshot` だけを鍵にする | §7.6 の増分取り込みでは**未変更ファイルが古い `snapshot` を保持する**ため、1 DB に複数の snapshot が同居する。鍵としては成立するが「実行」とは対応しない |
| b | `run_id` を新設し全テーブルに列追加 | F-4 の「列追加ゼロ」を放棄する。A-3 の3列に4つ目が加わる |
| **c** | **`analysis_run` の単位を「(言語, 実行)」にする** | 設定値が言語の数だけ重複するが、**A-3 が既に採った判断と同じ**（「冗長を承知で全テーブルに持たせる」） |

> **提案: c。** `analysis_run` の主キーを **`(extractor, snapshot, key, seq)`** とし、
> `(extractor, snapshot)` は**行グループを選ぶジョインキー**（外部キー制約ではない）と位置づける。
>
> **採用理由**: 列追加ゼロを保てる。同じ設定を言語ごとに書く冗長は生じるが、**A-3 が `lang` / `extractor` / `snapshot` を
> 全テーブルに持たせたときと同じ性質の冗長**であり、そこで受け入れた判断と一貫する。
> CLI は起動した抽出器の数だけ行グループを書く（どの抽出器を動かしたかは CLI が知っている）。
> 増分取り込みで `(tree-sitter-c, 旧snapshot)` と `(tree-sitter-c, 新snapshot)` が同居するのは正しい
> ——**それぞれが実際に起きた抽出イベント**だから。

**F-4 の「そのまま外部キーになる」という記述は「ジョインキーになる」へ訂正が要る。**

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### H-2. G-2 と G-3 が `symbols.file` について矛盾

| ID | 記述 |
|---|---|
| G-2 | 対応規則は …… **`symbols.file = files.file`** |
| G-3 | `symbols.file` は**その `repo-name` のルートからの相対パス**。**`files.file` への外部キーではない** |

G-2 は無条件の等号、G-3 は「外部キーではない」。**実装者が G-2 を読めば外部キー制約を張り、追加ソースツリー（F-5 決定5 で `files` に行を作らない）で壊れる。**

> **提案**: G-2 を「**解析対象ツリーのシンボルに限り** `symbols.file` は `files.file` と一致する。追加ソースツリーのシンボルは `files` に対応行を持たない」に限定する。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### H-3. `symbols.confidence` の対象列と ERROR 判定の基準行

F-6 決定2 は `symbols.confidence` を「このシンボルは**実在し、`visibility` / `container_id` は正しいか**」と定義したが、
F-1 が確定した `symbols` の列は他にもある（`qualified_name` / `is_definition` / `storage_class` / `branch_count` / `guard`）。

とくに **`branch_count` は本体から数える値**なので、**本体に ERROR があれば嘘になる**。
一方 `visibility` / `container_id` / `name` は**宣言ヘッダ**から取るので本体の破損に影響されない。

そして G-4 が `symbols` の位置を「**宣言・定義全体**」と定義したため、**F-6 の「ERROR ノードと同じ行／隣接行なら `low`」がどちらを指すかで結果が変わる。**

| 基準 | 結果 |
|---|---|
| 宣言ヘッダ（`start_line` 近傍） | 本体が壊れていても `high`。だが `branch_count` は嘘のまま |
| 範囲全体 | 本体に1つでも ERROR があれば `low`。`branch_count` の嘘は表現できるが、`visibility` まで巻き添えで下がる |

> **提案**: F-6 決定2 の「最も弱い環」の原則どおり**範囲全体**を基準にする（`branch_count` を含むため）。
> あわせて F-6 決定2 の列挙を「`name` / `visibility` / `container_id` / `branch_count` などこの行が主張する全項目」に改める。
> 個別の列の確からしさを分けたい場合は L3a で `files.error_lines` から再計算できる（§7.3 ルール2）。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### H-4. `lambda_depth`（整数）は `not_applicable` を表せない

G-5 で整数にしたが、G-7 が確立した規則（「その言語に概念が無い」＝ `not_applicable`）を**整数列では書けない**。
C にはラムダが無い。

> **提案**: `lambda_depth` は**常に整数**とし、`not_applicable` を使わない。
> 「この呼び出しはラムダの中か」への答えは、C でも「いいえ（0）」で**真**であり、概念の欠如ではない。
> §7.6 の空値規則の**明示的な例外**として `SCHEMA.md` に書く（書かないと `guard` と同じ扱いにされる）。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### H-5. 非該当時の値が `guard` 以外で未定

G-7 は `guard` についてだけ「空」と `not_applicable` を決めた。同じ判断が要る列が残っている。

| 列 | 場面 | 提案 |
|---|---|---|
| `dispatch` | `kind` が `call` 以外の行 | **空**（この行には該当しない） |
| `dispatch` | 仮想ディスパッチを持たない言語（C） | 値域から `virtual` が出ないだけ。**列としては該当する**ので `not_applicable` にしない |
| `attached_id` / `attached_line` | `kind` が `doc` 以外のコメント | **空**（H-9 と同じ話） |
| `storage_class` | C/C++ 以外 | **`not_applicable`**（[構想] §3.1 が既に規定済み） |
| `reason` | `status = resolved` の行 | **空**（§7.6 が既に規定済み） |

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### H-6. `address_of` だけ `var_` 接頭辞が無い

F-1 決定3 の `refs.kind` の値域は `call` / `import` / `var_read` / `var_write` / `var_readwrite` / **`address_of`**、
G-6 が `var_unknown` を追加した。**変数参照系5値のうち `address_of` だけ命名規則から外れている。**

> **提案**: **`var_address_of`** に揃える。`var_` 接頭辞で変数参照系をまとめて引ける（`kind LIKE 'var_%'`）ことに実益がある。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### H-7. `run.tsv` が §7.6 のメタ情報行の形式に従っていない

§7.6 は「1行目は `#` で始まるメタ情報行（`# snapshot=... extractor=... lang=...`）、2行目はヘッダ、3行目以降がデータ」と決めている。
G-10 の `run.tsv` の例は1行目がファイル名のコメントで、この形式ではない。

> **提案**: **`run.tsv` はメタ情報行を持たない**と明記する（**内容自体がメタ情報**であり、`snapshot` / `extractor` をデータ行として持つため）。§7.6 の明示的な例外として書く。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### H-8. F-1 決定事項のテーブル一覧が古い

F-1 の「確定後のテーブル」のコードブロックは F-2 以降の決定を含んでいない。
不足: `guard`（F-13）/ `lambda_depth`（G-5）/ `attached_line`（G-8）/ `error_lines`（F-6）/ `missing_nodes`・`extract_status`・`content_hash`（F-5）/ `dispatch` の値域（F-2）。

> **提案**: F-1 のコードブロックに「**列の正本は `SCHEMA.md`。本ブロックは F-1 時点のもの**」と注記する。
> `SCHEMA.md` を書けば解消するが、**それまでの間に誤読される**。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### H-9. `attached_id` / `attached_line` は `kind = doc` 限定

[構想] §3.1 は `attached_id` を「**`kind = doc` のとき**、説明対象のシンボルID」と定義している。
G-8 は `attached_line` を追加したが、同じ限定が付くことを書いていない。

`kind = file_header` のコメントは**シンボルではなくファイルに属する**ので、`attached_id` は空にし、
レポート2「機能の見取り図」は `comments.file` で結合する。

> **提案**: `attached_id` / `attached_line` はともに `kind = doc` のときのみ埋める、と `SCHEMA.md` に明記する。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照


## I. H 群決着後の再突き合わせ — **全件決着（2026-08-27）**

> 結論は「決定事項」を参照。**I-1（IDから `snapshot` を外す）は B-1 の改訂。**

**経緯**: H 群を決めた直後に3回目の突き合わせ。今回は**決定を運用（増分取り込み・2時点比較）に当てて**検証した。
**I-1 / I-2 はデータが壊れる。** どちらも B-1（2026-08-25 決定）と §7.6 の組み合わせに元からあった穴で、
簡易版の列を詰めたことで初めて表に出た。

| ID | 項目 | 種別 |
|---|---|---|
| **I-1** | **シンボルID に `snapshot` が入っているため、2時点比較と増分取り込みが成立しない** | **データが壊れる** |
| **I-2** | **追加ソースツリーの `symbols` が増分 `DELETE` で巻き添えになる** | **データが壊れる** |
| **I-3** | `refs.status` の `not_applicable` / `not_extracted` が付く行が存在しない | 値域 |
| **I-4** | `files.lang` の値域が抽出器を持つ言語に限られ、レポート1 の言語構成が出せない | 値域 |
| **I-5** | `symbols.kind` の値域が `...` で閉じていない | 値域 |
| **I-6** | 解析対象リポジトリの `repo-name` を記録する場所が無い | 未定 |
| **I-7** | `content_hash` のアルゴリズムと `started_at` のタイムゾーンが未定 | 未定 |
| **I-8** | 外部ライブラリ座標の欄数が B-1 の例と合っていない | 形式 |

---

### I-1. シンボルID に `snapshot` が入っている — **データが壊れる**

B-1 は自リポジトリのIDを次の形と決めた。

```
local  <repo-name>  <snapshot>  <descriptor...>
例: local  myrepo  8f3ac21  DefaultValidatorExtensions#Matches(IRuleBuilder,String).
```

**IDにコミットハッシュが入っているため、同じ関数が snapshot ごとに別のIDになる。**

#### 壊れるもの1: 2時点比較（資料カタログ C）

| 資料 | 作り方（E-1 の記述） |
|---|---|
| スナップショット間の差分 | `snapshot` 列で2時点比較（**B-4 が効く**） |
| 公開 API の変更検出 | `visibility='public'` のシンボル**増減** |

同じ関数が2時点で別IDになるので、**増減を取ると「全件消滅＋全件新規」になる**。差分が原理的に出せない。

#### 壊れるもの2: 増分取り込み（§7.6）

§7.6 の増分では**未変更ファイルの行が古い `snapshot` のまま残る**。
再抽出したファイルの `refs.dst_id` は**新しい snapshot** で組み立てられるので、
**未変更ファイルにある呼び先シンボル（古い snapshot のID）と結合できない。**

> **提案**: **`snapshot` をIDから外す。** `local <repo-name> <version欄> <descriptor...>` の version 欄には固定値を置く。
> - **理由1**: 2時点比較は「同じシンボルが2時点で同じID」でなければ成立しない
> - **理由2**: A-3 が `snapshot` を**全テーブルの列**に持たせているので、IDに入れるのは冗長。列で絞れば同じことができる
> - **理由3**: 増分取り込みの結合切れが消える
>
> **要確認**: version 欄に置く固定値（`.` が慣例と思われる）は **SCIP の仕様を実装時に確認する**。ここは未検証。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### I-2. 追加ソースツリーの `symbols` が増分 `DELETE` で巻き添えになる — **データが壊れる**

§7.6 の増分取り込みは `DELETE FROM T WHERE file = X` で回る。
一方 G-3 は「`symbols.file` は**その `repo-name` のルートからの相対パス**」と決めた。

```
解析対象ツリー   src/util.c   → symbols.file = 'src/util.c'
追加ソースツリー src/util.c   → symbols.file = 'src/util.c'   ← 同じ値
```

**解析対象の `src/util.c` を再抽出すると、`DELETE ... WHERE file = 'src/util.c'` が追加ツリー側の行まで消す。**
再挿入されるのは解析対象の分だけなので、**追加ツリーのシンボルが静かに消える**（§2.3 の当事者）。

G-3 は「`repo-name` は `id` に入っているので `(id, file)` で一意」としたが、
**`DELETE` は `file` だけで回るため、IDの中身では守れない。**

> **提案**: **`symbols` に `root` 列（`repo-name`）を足す。** 削除条件を `WHERE file = X AND root = '<解析対象>'` にする。
> 1列で解決し、G-3 の規則（どのルートからの相対パスか）が**暗黙のID埋め込みではなく列として明示される**副次効果もある。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### I-3. `refs.status` の `not_applicable` / `not_extracted` が付く行が存在しない

A-1 は `status` を4値（`resolved` / `unresolved` / `not_applicable` / `not_extracted`）と決めたが、
**F-1（`refs` への統合）と F-5（`files.extract_status` の新設）で後半2値の役割が移った。**

| 値 | `refs` で付く場面 | 判定 |
|---|---|---|
| `not_applicable`（その言語に概念が無い） | 行が存在する時点で参照は存在する | **無い** |
| `not_extracted`（まだ調べていない） | 抽出しなかったファイルは**行そのものが出ない**。ファイル単位の記録は `files.extract_status` が持つ | **無い** |

> **提案**: 値域は**4値のまま残す**（A-1 は決定済みで、`var_*` 相当を実装したときに要否が変わりうる）。
> ただし `SCHEMA.md` に「**現時点で `refs` に `not_applicable` / `not_extracted` が現れる条件は無い**」と明記する。
> 書かないと、抽出器の実装者が「埋めるべき場面があるはず」と探すことになる。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### I-4. `files.lang` の値域が狭すぎる

F-5 は `lang` を `c | cs | cpp | py | ts | js | unknown` と決めたが、同時に `extract_status` に
**`skipped_no_extractor`**（抽出器を持たない言語）を入れた。§7.7 が挙げる Ruby / Go / シェルスクリプトが該当する。

**この値域では Go のファイルが `unknown` になり、レポート1「概観 — ディレクトリ別のファイル数・行数・言語構成」で
「不明」に丸められる。** 言語構成が出せない。

> **提案**: `lang` は**検出した言語名をそのまま入れる**（`go` / `ruby` / `sh` …）。値域は
> 「tree-sitter-language-pack の言語名（371言語）＋ `unknown`」とする。
> **抽出器の有無は `extract_status` が持つので、`lang` が背負う必要は無い。**

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### I-5. `symbols.kind` の値域が閉じていない

[構想] §3.1 は `function | method | type | namespace | macro | variable | field | file | ...` と `...` で終わっている。
`SCHEMA.md` は正本なので閉じる必要がある。

> **提案**:
> - **初版（C + C#）で実際に出るもの**: `file` / `namespace` / `type` / `method` / `function` / `macro`
> - **値域には残すが初版では出ないもの**: `variable` / `field`（`var_*` 参照を出さないため）
> - `...` は閉じ、第2段の言語で必要になった時点で追加する（値の追加は A-1 と同じく既存クエリを壊さない）

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### I-6. 解析対象リポジトリの `repo-name` を記録する場所が無い

B-1 のIDは `local <repo-name> …` で始まるが、`run.tsv`（G-10）には `root`（パス）しか無い。
**追加ツリーは `extra_root common-lib=/home/me/common` で repo-name とパスの対応を記録するのに、
解析対象自身の repo-name はどこにも無い。**

> **提案**: `run.tsv` に **`repo_name`** キーを足す。I-2 の `symbols.root` の値もこれと一致する。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### I-7. `content_hash` と `started_at` の規定が無い

| 項目 | 問題 |
|---|---|
| `content_hash`（F-5） | アルゴリズム未定。抽出器ごとに違う値になれば増分の変更検出が成立しない |
| `started_at`（G-10 の例は `2026-08-27T10:32:11`） | タイムゾーン未定。B-4 の `nogit-20260825T143052` も同様 |

> **提案**: `content_hash` は **SHA-256 の先頭16桁（16進小文字）**。`started_at` と B-4 の `nogit-` は
> **UTC**（`started_at` は末尾 `Z` を付ける）。

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

### I-8. 外部ライブラリ座標の欄数が合っていない

SCIP の形式は5欄（`<scheme> <package-manager> <package-name> <version> <descriptor...>`）だが、
**B-1 の例も F-4 の例も欄数が足りていない。**

| 出典 | 例 | 欄数 |
|---|---|---|
| B-1 | `pypi requests 2.31.0` | **3**（scheme と descriptor が無い） |
| F-4 決定2 | `c-stdlib c99 - printf().` | **4**（`-` が何の欄か不明） |

> **提案**: 5欄を守る形に統一する。標準ライブラリは `stdlib <言語> <ライブラリ名> <規格> <descriptor>`
> （例: `stdlib c libc c99 printf().`）。B-1 の外部ライブラリの例も5欄で書き直す。
> **I-1 の version 欄の扱いと合わせて、SCIP 仕様の確認を1回で済ませる。**

- [x] **決定**（2026-08-27）。結論は「決定事項」を参照

---

## 決定事項

決まったものはここへ移す。形式: `[決定日] 項目 — 結論（記載先）`

### [2026-08-27] I 群. H 群決着後の再突き合わせ（運用に当てて検証）— **8件を決着。B-1 を改訂**

決定を**増分取り込みと2時点比較に当てて**検証した。**I-1 / I-2 はデータが壊れる。**
どちらも B-1（2026-08-25）と §7.6 の組み合わせに元からあった穴で、簡易版の列を詰めたことで初めて表に出た。

| ID | 決定 |
|---|---|
| **I-1** | **シンボルID から `snapshot` を外す**（B-1 の改訂） |
| **I-2** | **`symbols` に `root` 列（`repo-name`）を足す** |
| **I-3** | `refs.status` の値域は4値のまま。「`not_applicable` / `not_extracted` が現れる条件は現時点で無い」と明記 |
| **I-4** | `files.lang` は**検出した言語名をそのまま**入れる（tree-sitter-language-pack の言語名 ＋ `unknown`） |
| **I-5** | `symbols.kind` の値域を閉じる |
| **I-6** | `run.tsv` に **`repo_name`** キーを足す |
| **I-7** | `content_hash` = SHA-256 の先頭16桁（16進小文字）。時刻は **UTC** |
| **I-8** | 外部ライブラリ座標は **5欄**を守る |

#### I-1: シンボルID から `snapshot` を外す（B-1 の改訂）

B-1 は `local <repo-name> <snapshot> <descriptor...>` と決めたが、**IDにコミットハッシュが入るため
同じ関数が snapshot ごとに別のIDになる**。2つの運用が壊れる。

| 壊れるもの | 内容 |
|---|---|
| **2時点比較**（資料カタログ C） | E-1 は「公開 API の変更検出 — `visibility='public'` のシンボル**増減**」と書いたが、同じ関数が別IDになるので**「全件消滅＋全件新規」**になる。差分が原理的に出せない |
| **増分取り込み**（§7.6） | 未変更ファイルの行は**古い `snapshot` のまま残る**。再抽出したファイルの `dst_id` は新しい snapshot で組み立てられるため、**呼び先シンボルと結合できない** |

> **決定**: `snapshot` をIDから外し、version 欄には固定値を置く。
> **理由1**: 2時点比較は「同じシンボルが2時点で同じID」でなければ成立しない
> **理由2**: A-3 が `snapshot` を**全テーブルの列**に持たせているので、IDに入れるのは冗長。列で絞れば同じことができる
> **理由3**: 増分取り込みの結合切れが消える
>
> **要確認（実装時）**: version 欄に置く固定値（`.` が慣例と思われる）は **SCIP 仕様を確認する**。ここは未検証。I-8 と合わせて1回で済ませる。

#### I-2: `symbols` に `root` 列を足す

§7.6 の増分取り込みは `DELETE FROM T WHERE file = X` で回る。一方 G-3 は「`symbols.file` はその `repo-name` の
ルートからの相対パス」と決めた。**解析対象と追加ツリーに同名パスがあると、追加ツリー側の行まで消え、再挿入されない。**

G-3 は「`repo-name` は `id` に入っているので一意」としたが、**`DELETE` は `file` だけで回るためIDの中身では守れない。**

> **決定**: `symbols` に `root` 列（`repo-name`）を足し、削除条件を `WHERE file = X AND root = '<解析対象>'` にする。
> G-3 の規則が**暗黙のID埋め込みではなく列として明示される**副次効果もある。

#### I-3 〜 I-8

| ID | 決定と理由 |
|---|---|
| **I-3** | `refs.status` は4値のまま残す（A-1 は決定済みで、`var_*` 相当の実装時に要否が変わりうる）。ただし **F-1 の統合と F-5 の `extract_status` で後半2値の役割が移った**ため、「現時点で `refs` に `not_applicable` / `not_extracted` が現れる条件は無い」と `SCHEMA.md` に明記する。書かないと実装者が「埋めるべき場面があるはず」と探す |
| **I-4** | F-5 の値域（7値）では **Go / Ruby / シェルが `unknown` に丸められ**、レポート1「言語構成」が出せない。**抽出器の有無は `extract_status` が持つ**ので `lang` が背負う必要は無い |
| **I-5** | `symbols.kind` の `...` を閉じる。**初版（C + C#）で出るのは** `file` / `namespace` / `type` / `method` / `function` / `macro`。**値域には残すが初版で出ない**のが `variable` / `field`（`var_*` を出さないため）。値の追加は A-1 と同じく既存クエリを壊さない |
| **I-6** | 追加ツリーは `extra_root common-lib=/path` で repo-name を記録するのに、**解析対象自身の repo-name がどこにも無かった**。I-2 の `symbols.root` の値もこれと一致する |
| **I-7** | `content_hash` はアルゴリズムが違えば増分の変更検出が成立しない → **SHA-256 の先頭16桁（16進小文字）**。`started_at` と B-4 の `nogit-YYYYMMDDThhmmss` は **UTC**（`started_at` は末尾 `Z`） |
| **I-8** | SCIP は5欄（`<scheme> <package-manager> <package-name> <version> <descriptor...>`）だが、**B-1 の例は3欄（`pypi requests 2.31.0`）、F-4 の例は4欄（`c-stdlib c99 - printf().`）**だった。5欄に統一し、標準ライブラリは `stdlib <言語> <ライブラリ名> <規格> <descriptor>`（例: `stdlib c libc c99 printf().`）とする |

**記載先**: 本ファイル I 群。反映先は `SCHEMA.md`、[構想] §3.2.1（B-1 の改訂）/ §3.2.2 / §7.6


### [2026-08-27] H 群. G 群決着後の再突き合わせ — **9件を決着。`analysis_run` の鍵を作り直した**

| ID | 決定 |
|---|---|
| **H-1** | `analysis_run` の単位を「**(言語, 実行)**」にする。主キーは **`(extractor, snapshot, key, seq)`**、`(extractor, snapshot)` は**ジョインキー**（外部キー制約ではない） |
| **H-2** | G-2 を「**解析対象ツリーに限り** `symbols.file = files.file`」に限定する |
| **H-3** | `symbols.confidence` は**範囲全体**を基準にする。F-6 決定2 の列挙を「この行が主張する全項目」に改める |
| **H-4** | `lambda_depth` は**常に整数**。`not_applicable` を使わない（§7.6 の空値規則の明示的な例外） |
| **H-5** | 非該当時の値を全列で確定（下表） |
| **H-6** | `address_of` → **`var_address_of`** |
| **H-7** | `run.tsv` は**メタ情報行を持たない**（§7.6 の明示的な例外） |
| **H-8** | F-1 のテーブル一覧に「**列の正本は `SCHEMA.md`**」と注記する |
| **H-9** | `attached_id` / `attached_line` は **`kind = doc` のときのみ**埋める |

#### H-1: `analysis_run` の鍵が成立していなかった

F-4 は「主キーは `(extractor, snapshot)`。A-3 の2列がそのまま外部キーになる」と決めたが、**2つの理由で成立しない。**

**原因1: `extractor` は「実行」ではなく「言語」の単位。**

| 出典 | 値 |
|---|---|
| [構想] §7.6 のメタ情報行 | `extractor=libclang-18.1.0 lang=c` |
| `tools/emit-generic-schema.py` | `tree-sitter-c` / `tree-sitter-c-sharp` |

C と C# を1回の実行で解析すると `extractor` は2種類になる。一方 `boundary_level` / `root` / `git_state` は実行に1つ。
**1回の実行が2つの鍵に割れる。**

**原因2: 縦持ち（G-10）にすると `(extractor, snapshot)` は一意でなくなる。** 1実行あたり十数行になる。
G-10 は「主キーは変わらない」と書いたが**必ず変わる**。

##### 検討した案

| 案 | 内容 | 評価 |
|---|---|---|
| a | `snapshot` だけを鍵にする | §7.6 の増分取り込みでは未変更ファイルが古い `snapshot` を保持するため、1 DB に複数の snapshot が同居する。「実行」と対応しない |
| b | `run_id` を新設し全テーブルに列追加 | F-4 の「列追加ゼロ」を放棄する |
| **c** | **`analysis_run` の単位を「(言語, 実行)」にする** | **採用** |

**採用理由**: 列追加ゼロを保てる。設定値が言語の数だけ重複するが、**A-3 が `lang` / `extractor` / `snapshot` を
全テーブルに持たせたときと同じ性質の冗長**であり、そこで受け入れた判断と一貫する。
CLI は起動した抽出器の数だけ行グループを書く。増分取り込みで `(tree-sitter-c, 旧snapshot)` と
`(tree-sitter-c, 新snapshot)` が同居するのは正しい——**それぞれが実際に起きた抽出イベント**だから。

**F-4 の「そのまま外部キーになる」は「ジョインキーになる」へ訂正。**

#### H-2: `symbols.file` — G-2 と G-3 の矛盾

G-2 は無条件の等号（`symbols.file = files.file`）、G-3 は「`files.file` への外部キーではない」と書いていた。
実装者が G-2 を読めば外部キー制約を張り、**追加ソースツリー**（F-5 決定5 で `files` に行を作らない）で壊れる。

> **決定**: 「**解析対象ツリーのシンボルに限り** `symbols.file` は `files.file` と一致する。追加ソースツリーのシンボルは `files` に対応行を持たない」。

#### H-3: `symbols.confidence` の対象と ERROR 判定の基準行

F-6 決定2 は対象を「実在し、`visibility` / `container_id` は正しいか」と書いたが、F-1 が確定した列は他にもある
（`qualified_name` / `is_definition` / `storage_class` / `branch_count` / `guard`）。とくに **`branch_count` は本体から数える値**で、
**本体に ERROR があれば嘘になる**。一方 `visibility` / `container_id` / `name` は宣言ヘッダ由来。

G-4 が位置を「宣言・定義全体」と定義したため、F-6 の「ERROR ノードと同じ行／隣接行なら `low`」がどちらを指すかで結果が変わっていた。

> **決定**: F-6 決定2 の「**最も弱い環**」の原則どおり**範囲全体**を基準にする（`branch_count` を含むため）。
> F-6 決定2 の列挙を「`name` / `visibility` / `container_id` / `branch_count` など**この行が主張する全項目**」に改める。
> 個別の列の確からしさを分けたい場合は L3a が `files.error_lines` から再計算できる（§7.3 ルール2）。

#### H-4: `lambda_depth` は常に整数

G-5 で整数にしたが、G-7 の規則（「その言語に概念が無い」＝ `not_applicable`）を**整数列では書けない**。C にラムダは無い。

> **決定**: `not_applicable` を使わない。「この呼び出しはラムダの中か」への答えは C でも「いいえ（0）」で**真**であり、
> 概念の欠如ではない。§7.6 の空値規則の**明示的な例外**として `SCHEMA.md` に書く。

#### H-5: 非該当時の値（全列）

G-7 は `guard` についてだけ決めていた。同じ判断が要る列を確定する。

| 列 | 場面 | 値 |
|---|---|---|
| `dispatch` | `kind` が `call` 以外の行 | **空** |
| `dispatch` | 仮想ディスパッチを持たない言語（C） | **空にしない**。値域から `virtual` が出ないだけで、列としては該当する |
| `attached_id` / `attached_line` | `kind` が `doc` 以外のコメント | **空**（H-9） |
| `guard` | Python / JS / TS | **`not_applicable`**（G-7） |
| `guard` | C 系で条件の外 | **空**（G-7） |
| `storage_class` | C/C++ 以外 | **`not_applicable`**（[構想] §3.1 が既に規定） |
| `reason` | `status = resolved` の行 | **空**（§7.6 が既に規定） |
| `lambda_depth` | 全言語 | **常に整数**（H-4） |

#### H-6: `var_address_of`

F-1 / G-6 の値域は `var_read` / `var_write` / `var_readwrite` / `var_unknown` / **`address_of`** で、
**`address_of` だけ命名規則から外れていた**。`var_` 接頭辞に揃えることで `kind LIKE 'var_%'` で変数参照系をまとめて引ける。

#### H-7: `run.tsv` はメタ情報行を持たない

§7.6 は「1行目は `#` で始まるメタ情報行、2行目はヘッダ、3行目以降がデータ」と決めているが、
`run.tsv` は**内容自体がメタ情報**であり `snapshot` / `extractor` をデータ行として持つ。**§7.6 の明示的な例外**とする。

#### H-8 / H-9

- F-1 の「確定後のテーブル」に「**列の正本は `SCHEMA.md`。本ブロックは F-1 時点のもの**」と注記する（F-2 以降の列が入っていないため）
- `attached_id` / `attached_line` は **`kind = doc` のときのみ**。`kind = file_header` はシンボルではなくファイルに属するので空にし、レポート2 は `comments.file` で結合する

**記載先**: 本ファイル H 群。反映先は `SCHEMA.md`、[構想] §3.1 / §3.1.2 / §7.6


### [2026-08-27] G 群. `SCHEMA.md` を書く前の列の細目 — **13件を決着**

F-1 〜 F-6 / F-13 の決定同士を突き合わせた結果出た穴。決定同士の矛盾は無かったが、
直接の矛盾1件と、決定が新たに生んだ穴12件があった。

| ID | 決定 |
|---|---|
| **G-1** | `container_id` は**常に埋める**。トップレベルはファイルシンボルを親にする。空になるのはファイルシンボル自身のみ |
| **G-2** | `files` と `symbols(kind='file')` は**両方持つ**。対応規則は `symbols.id = local <repo> <snapshot> <path>/`、`symbols.file = files.file` |
| **G-3** | `symbols.file` は**その `repo-name` のルートからの相対パス**。`files.file` への外部キーではない |
| **G-4** | 位置の範囲: `refs` = 参照式全体／`symbols` = 宣言・定義全体／`comments` = コメント全体。**名前トークンの位置は初版で持たない** |
| **G-5** | `via_lambda`（bool）→ **`lambda_depth`（整数）**に改める |
| **G-6** | `refs.kind` に **`var_unknown`** を加える |
| **G-7** | `guard` は Python / JS / TS で **`not_applicable`**、C 系で条件の外なら**空** |
| **G-8** | `comments` に **`attached_line`** を追加（下記・訂正） |
| **G-9** | `analysis_run` は `lang` を持たない（**A-3 の明示的な例外**） |
| **G-10** | `facts/raw/run.tsv` を **key-value の縦持ち**にする（下記・訂正） |
| **G-11** | `analysis_run` は**入力（設定）だけ**を持つ。`files` から導出できる列は作らない。`load_errors` は残す |
| **G-12** | [構想] に **§3.1.7 を新設**し G-1 の決定を書く。`container_id` から `（§3.1.4）` の参照を外す |

#### G-1: `container_id` は常に埋める

[構想] §3.1「無ければ空」と E-1 汎用スキーマ「C での対応: ファイル」が矛盾していた。
`tools/emit-generic-schema.py` はファイルシンボルを立てて親にしている。

**採用理由**: レポート1「概観」とレポート4「コンポーネント依存」はディレクトリ／名前空間単位で集約するので、
シンボル → ファイル → ディレクトリの経路が要る。`file` 列でも辿れるが、**C# の 名前空間 → 型 → メソッド の鎖と
同じ機構で辿れる方が素直**。

#### G-4: 位置は何の範囲か

F-3 は形式（`start_*` / `end_*`）だけを決め、**何を囲むかを決めていなかった**。
F-3 の主キー一意性（`f(x)(y)` は終了位置で分離できる）も F-6 の「ERROR ノードと隣接なら `low`」も、この定義に依存する。

| テーブル | 範囲 |
|---|---|
| `refs` | **参照式全体**（`p->f(a,b)` 全体、`#include "x.h"` の行全体） |
| `symbols` | **宣言・定義全体**（修飾子・属性を含む先頭から本体の閉じ括弧まで）。doc の隣接判定は `start_line - 1` |
| `comments` | コメント（または docstring リテラル）全体 |

**名前トークンだけの位置は初版で持たない。** §7.4-1 が要求しているのは範囲であって名前位置ではない。

#### G-5: `via_lambda` → `lambda_depth`（整数）

E-1 が「`via_lambda`（またはネスト深さ）」と両論併記のままだった。
**bool は `lambda_depth > 0` で導出できるが、逆はできない。** 記録コストは同じ（抽出時に数えるだけ）。
JS の 31.3% を扱うとき、コールバックの入れ子の深さは「読みにくさ」の指標になりうる。
`via_lambda` という名前は bool を想起させるため、名前ごと変える。

#### G-6: `var_unknown`

F-1 で `var_refs` を畳んだとき、旧 `access` の値域から **`unknown` が落ちていた**。
読みか書きか判定できない参照を `var_read` に丸めると**嘘になる**（§2.3）。

#### G-7: `guard` の非対象言語は `not_applicable`

F-13 で「他言語は空」と書いたのは **§7.6 の規則に反していた**（「空」＝列が不要、`not_applicable`＝その言語に概念が無い）。
[構想] §3.1 が `storage_class` を「C/C++ 固有。他言語は `not_applicable`」と書いているのと同じ扱いに揃える。**F-13 の記述はこの点で訂正。**

#### ★ G-8: `(attached_id, file)` では一意にならない — **提案を訂正**

当初は「`(attached_id, file)` で一意に決まる」としたが、**誤りだった。** 反例が2つあり、どちらも実際に起きる。

**反例1: `#ifdef` の両枝**（cJSON の衝突4件がこの形）

```c
#ifdef _WIN32
/** Windows 版 */
void open_port(void) {...}   ← line 3
#else
/** POSIX 版 */
void open_port(void) {...}   ← line 7
#endif
```

**反例2: 同一ファイル内の前方宣言と定義**（C では常用）

```c
/** バッファを解放する */
static void free_buf(Buf *b);    ← line 5  宣言（doc 付き）
...
static void free_buf(Buf *b) {   ← line 40 定義
```

いずれも同じ id で `start_line` 違いの2行があり、`(attached_id, file)` は2行にマッチする。

**原因**: コメントが付くのは「シンボル」ではなく「**シンボルの出現箇所**」。
出現箇所を一意にする鍵は `symbols` の主キー `(id, file, start_line, …)` であって `(id, file)` ではない。

> **決定**: `comments` に **`attached_line`**（説明対象の `symbols.start_line`）を追加する。
> 結合は `comments.(attached_id, file, attached_line)` = `symbols.(id, file, start_line)`。
> 抽出器はどのノードの直前かを知っているので `start_line` は無料で取れる。

§3.1.6 が「位置は事実として必ず記録し、紐付けは信頼度つきで別に持つ」と決めているのに、**紐付け先が曖昧だった**のが穴だった。

#### ★ G-10: `run.tsv` は key-value の縦持ちにする — **F-4 の持ち方を訂正**

記録したい項目のうち3つ（`extra_roots` / `stdlib_dicts` / `tool_versions`）は**リスト**であり、
1行の TSV に詰めると区切り文字のエスケープが要る。**§7.6 が CSV を却下した理由をそのまま踏む。**

```
# facts/raw/run.tsv
key	value
extractor	tree-sitter-c@0.26.0
snapshot	8f3ac21-dirty
root	/home/me/proj
boundary_level	0
closed_world	true
config	all_branches
git_state	shallow
started_at	2026-08-27T10:32:11
extra_root	common-lib=/home/me/common
stdlib_dict	c99
stdlib_dict	posix
tool_version	tree-sitter=0.26.0
tool_version	tree-sitter-language-pack=1.15.8
```

繰り返しキーでリストを表す。**L1 の `analysis_run` も同じ縦持ち**（`extractor` / `snapshot` / `key` / `value` / `seq`）。

| 採用理由 | 内容 |
|---|---|
| **項目追加が無料** | フル版が再開すれば `compile_commands.json` のパスや resource-dir を足したくなる。横持ちだと**列追加＝データの作り直し**で、F 群でずっと避けてきた問題を1テーブルだけ抱える |
| **レポート5 の用途に合う** | 用途は「前提を冒頭に列挙する」だけ。**全キーをそのまま並べれば完成する** |
| **`load_errors` の書き込み時点がずれる** | ローダーが取り込み後に知る値。縦持ちなら**行を1本足すだけ**。横持ちだと UPDATE が要る |
| **クエリ性を失わない** | E-4 決定2 で **1 DB = 1 実行**と決めているので `WHERE boundary_level = 0` のような検索は要らない |

**F-4 の決定内容（何を記録するか、主キーは `(extractor, snapshot)`）は変わらない。持ち方だけが変わる。**
書き手は**抽出器ではなくオーケストレータ（CLI）**、置き場所は `facts/raw/` 直下（ソースツリーのミラーではない）。
B-3 の「ジョブ設定ファイル（TOML）」とは別物（**設定**の記述 vs **実績**の記録）。

**記載先**: 本ファイル G 群。反映先は `SCHEMA.md`、[構想] §3.1（`container_id` / `lambda_depth` / `guard` / `attached_line`）/ **§3.1.7 新設**（G-1・G-12）/ §7.6（`run.tsv`）


### [2026-08-27] F-13. [現実] が挙げたまま未決リストに無かった宿題 — **正規化は L2。`config` は2つに分ける**

3件のうち **① `imports` テーブル案は F-1 で解決済み**（`refs.kind = import` で吸収）。[現実] §4.1 の再掲スキーマが古い件も F-1 決定5（再掲をやめて参照に置換）で解消。残る2件を決める。

#### ③ 正規シンボル1行＋宣言箇所の別テーブル — **L1 は N 行のまま。正規化は L2 のビュー**

[現実] §4.4-5 は「宣言が複数ある問題は全言語で起きる」（C/C++ の宣言/定義分離、C# の `partial`、TS の宣言マージ、Python の再定義）として、正規化構造を「全言語で必要」としていた。F-3 決定4 で `symbols` が 1シンボル N 行になったため、これが未決のまま主キーが暫定で止まっていた。

##### 却下: `symbols`（正規1行）＋ `symbol_decls`（出現箇所 N 行）

正規行を1つ選ぶには規則が要る。定義があれば定義、無ければ宣言——では**定義が複数あったら**？（`partial` クラス、`#ifdef` の両枝、条件付き定義）。「最初の1つ」はファイル走査順に依存し、**B-4 が退けた「不安定な識別子」と同じ性質**になる。

**つまり L0 に判断ロジックを入れることになり §7.3 ルール1 に反する。** そして選ばれなかった方は静かに消える——**発見1（17.7% 消失）とまったく同じ失敗の形**。

##### 決定: L2 の派生ビュー `symbol_canonical` を作る

```
L0  抽出層     見た宣言・定義をすべて出す（判断しない）
L1  symbols    N 行。is_definition と guard で区別できる
      ↓ 派生（再生成可能）
L2  symbol_canonical   正規シンボルのビュー
```

| 状況 | 結果 |
|---|---|
| 定義が1つ | それを正規とする |
| 定義が0（宣言のみ＝外部） | 宣言を正規。`is_definition = false` |
| **定義が複数** | **畳まない。`multiple_definitions = true` として全部残す** |

**「畳めなかった」がデータとして出るのが要点。** 却下案なら黙って1つ選んでいた場面が、`partial` クラス・条件コンパイル・条件付き定義の一覧として表に出る（§3.3 と同じ形）。

**得られるもの**: F-3 の暫定主キーが**そのまま確定**になる（変更ゼロ）。D-3 と同じ「L2 の派生テーブル」の枠に収まり新概念を持ち込まない。C で doc がヘッダの宣言に付き定義が `.c` にある場合も、N 行なら `attached_id` がヘッダの行を指したまま残る。

#### ② `config` 列 — **2つの別物が1語に混ざっていた**

[現実] §4.3 は `config` を「初版から必要」「確定済み（§3.1）」と書いているが **§3.1 にその列は無い**。落ちなかった原因は、F-2 と同じ構造の混同にある。

| 軸 | 問い | 単位 | 抽出器での違い |
|---|---|---|---|
| **(a)** | どの構成で解析したか | **実行単位** | フル版はフラグで**1構成だけ**を見る |
| **(b)** | この行は条件付きの枝の中にあるか | **行単位** | 簡易版は**両枝が同時に出る**（実測3） |

##### 決定 (a): `analysis_run.config`

フル版は `compile_commands.json` のフラグで枝が決まるため、**同じリポジトリを2回解析すると別の事実が出る**。どの構成の結果かを記録しないと再現できない。簡易版は前処理をしないので `all_branches`。**「構成を選んでいない」ことも記録すべき事実。**

##### 決定 (b): `guard` 列を `symbols` / `refs` / `comments` に持つ

囲んでいる `#if` 条件式のテキストをそのまま入れる。ネストは `&&` で連結、条件の外なら空。

```
cJSON_Delete   guard=""
debug_print    guard="DEBUG"
win_open       guard="_WIN32 && !NDEBUG"
```

**簡易版では必須。** 実測3 のとおり `#ifdef` の両枝が構文木に載るため、**`guard` が無いと「同名関数が2つある」ようにしか見えない**。E-3 ③「条件コンパイルの有効枝は取れない」は正しいが、**どの条件の下にあるかはソースに書いてある**——①（構文と位置だけで決まるもの）に属する。

> **★ これは簡易版がフル版より強い2つ目の領域**（1つ目はコメント）。
>
> | | フル版（libclang） | 簡易版（tree-sitter） |
> |---|---|---|
> | `#ifdef _WIN32` の中身 | **Linux 上では見えない**（1構成しかパースしない） | **見える**（両枝が木に載る） |
>
> HANDOFF §7 は「Windows 専用コードは WSL では解析できない」と記録していたが、簡易版なら
> 「**どのプラットフォーム向けのコードがどれだけあるか**」の棚卸しが出せる。
> E-4 の「簡易版は単なる下位互換ではない」の裏付けが1つ増えた。

「どちらの枝が有効か」は分からない。棚卸しには十分だが、コールグラフでは `confidence` を下げる材料になる（**F-6 決定5 の「条件コンパイルによる分岐は `low`」の検出手段が `guard`**）。

**対象**: C / C++ / C# のみ。他言語は空（[理想] §3.2 の「条件コンパイル｜C / C++ / C#」と一致）。Python の `if TYPE_CHECKING:` は実行時分岐なので対象外。

**後から足せない理由**: E-1 の「他言語で空く軸の先行確認」（`via_lambda` / `source_kind` / `visibility_source`）と同じ構造。初期対象が C と C# である今のうちに入れる。

**記載先**: 本ファイル F-13。反映先は `SCHEMA.md`、[現実] §4.3（`config` 行への注記）/ §4.4-5（「正規化は L2」の注記）。F-3 の `symbols` 主キーは**暫定から確定**へ

### [2026-08-27] F-6. `confidence` の決め方 — **欠落は `files`、誤りは `confidence`。2段構えにする**

#### ★ E-3 の「ERROR ノード数を `confidence` に反映する」はそのままでは実行できない

E-3 自身の実測1 を数字で見ると分かる。

| ケース | ERROR ノード数 | 検出できた関数 |
|---|---|---|
| 式が壊れている | 1 | **3 / 3** |
| 未知のマクロ修飾 | 1 | **3 / 3** |
| **括弧の不均衡** | **2** | **0 / 3** |
| ファイル途中で切れている | 1 | 1 / 2 |

**ERROR ノード数と被害は相関していない。** 1個で無傷、2個で全滅。数を閾値で切る設計は成立しない。

さらに根本的な問題がある。

> **括弧不均衡のとき、行は「間違って出る」のではなく「出ない」。**
> `confidence` は**出た行にしか付けられない**ので、0/3 のケースでは**付ける先が無い。**

E-3 が防ごうとした「黙って痩せた結果が出る」は、**`confidence` では表現できない種類の欠陥**だった。

#### 決定1: 2段構えにする

| 層 | 何で表現するか |
|---|---|
| **欠落**（行が出ない） | **`files` の壊れ具合**。レポート5 でファイル単位に列挙する |
| **誤り**（出た行が怪しい） | 行の `confidence` |

`files` に **`error_lines`**（ERROR / MISSING ノードが覆う行数、重複除く）を追加する（F-5 への追加）。
括弧不均衡は後続を全部巻き込むので span が巨大になり、式の破損は1行で収まる。**被害の大きさと相関するのは数ではなく行数。**

レポート5 に「構文木が壊れており、シンボルを取りこぼしている可能性が高いファイル」を `error_ratio`（= `error_lines / lines`）降順で出す。**これが「静かな取りこぼし」の可視化の本体**であり、`confidence` ではない。

**閾値は初版で決めない。** A-2 は「数値は採らない。根拠のない小数を書くことになる」と決めた。閾値も同じで、5% と 10% のどちらが正しいかの実測が無い。初版は **0 か非0 か**だけで切り、生データは `files` に残す。**L3a で閾値を後から変えられる**（§7.3 ルール2）。

#### 決定2: `confidence` の対象を「その行の主張のうち最も弱い判断」と定義する

現状 `confidence` は暗黙に複数のものを指していた。`comments` の規則は `attached_id` の紐付けの話、`visibility_source = convention` で下げるのは `visibility` の話、`refs` は解決の話。書いておかないと抽出器ごとに別の意味で埋める。

| テーブル | `confidence` が答えている問い |
|---|---|
| `files` | `lang` 判定は正しいか |
| `symbols` | このシンボルは実在し、`visibility` / `container_id` は正しいか |
| `refs` | `status` / `dst_id` / `dispatch` の判定は正しいか |
| `comments` | `kind` と `attached_id`（誰の説明か）の紐付けは正しいか |

列ごとの `confidence` を作ると列が爆発し、A-2 が enum を選んだ精神（過剰な精密さを避ける）に反する。

#### 決定3: `unresolved` 行の `confidence` は「`reason` の判定の確からしさ」

決定2 から自動的に決まる。**直感に反する結論が出る。**

| `reason` | `confidence` | 根拠 |
|---|---|---|
| `ambiguous` | **`high`** | 候補が2個以上あるのは**数えた事実** |
| `needs_type` | **`high`** | レシーバの型が書かれていないのは事実 |
| `needs_dataflow` | `high` | 段階1（スコープ解析）の演繹 |
| **`external`** | **`high` / `medium`** | 「リポジトリ内に定義が無い」の根拠は**定義表の網羅性**（下記） |
| `unknown` | `low` | 定義上 |

`emit-generic-schema.py` は `external` → `high`、`ambiguous` → `low` と入れていたが、**`ambiguous` は逆**だった。F-2 で `status` と分離した以上、`confidence` は `reason` の確からしさを言う。

##### ここで ERROR ノードが効く

`external` だけが**リポジトリ全体の定義表**に依存する。**パースに失敗したファイルが1つでもあれば、そこに定義があったかもしれない。**

> `analysis_run` に「ERROR を含むファイルが存在するか」を持ち、**存在すれば `external` の `confidence` は最大 `medium`**。

E-3 が求めた「ERROR ノードを `confidence` に反映する」は、**この1箇所でだけ意味を持つ**。他の `reason` は自ファイル内で完結する。

##### 実務上の注意

**`confidence` 単独でフィルタしてはいけない。** `WHERE confidence = 'high'` は未解決の行を大量に拾う。§7.3 ルール2 のとおり `WHERE status = 'resolved' AND confidence = 'high'` と組にする。§3.3 の SQL 例に注記する。

#### 決定4: 全テーブルの一覧

| テーブル | `high` | `medium` | `low` |
|---|---|---|---|
| `files` | 拡張子で `lang` が一意 | `.h` など複数言語がありうる拡張子 | 内容から推測 |
| `symbols` | ERROR 無しのファイル、かつ `visibility_source` が `declaration` / `export_list` / `linkage` | `visibility_source = convention`、または同じファイルに ERROR がある | ERROR ノードと同じ行／隣接行 |
| `refs`（`resolved`） | フル版の名前解決由来 | **②-a**（閉世界仮定つきの演繹）、`dispatch = macro` | ②-b（**L0 では出さない**。L2 / L3a の推測モードのみ） |
| `refs`（`unresolved`） | `ambiguous` / `needs_type` / `needs_dataflow` | `external` かつリポジトリ内に ERROR を含むファイルがある | `unknown` |
| `comments` | 明示的な doc 記法（`///` / `/**` / docstring） | 隣接しているだけ | ERROR ノードと同じ行／隣接行 |

**簡易版の `resolved` に `high` は出ない。** E-4 決定3（`confidence` でフル版と簡易版を区別しない）と矛盾しない — 区別は `extractor` 列で行い、`confidence` はたまたま分布が違うだけ。

#### 決定5: ②-a の破れは、検出できるものだけ下げる

| 破れの条件 | 検出できるか | 扱い |
|---|---|---|
| **条件コンパイルで定義が分岐** | **できる**（F-3 決定4 で同じ id が複数行になり、`dst_id` が複数にマッチする） | その `refs` 行を **`low`** |
| **マクロ生成の関数名** | できる（マクロ定義に `##` が含まれる） | そのファイル由来の解決を **`low`** |
| **ビルドから除外されたファイルの同名定義** | **できない**（ビルドコンテキストが無い＝軸2 = ゼロの帰結） | **閉世界仮定そのものの限界**として `analysis_run.closed_world` の注記で扱う。個別の行では表現しない |

**3つ目を「できない」と明記することが重要。** 書かないと「②-a は medium だから大丈夫」と読まれる。

**記載先**: 本ファイル F-6。反映先は `SCHEMA.md`、[構想] §3.1.2 / §3.3、E-3 の「ERROR ノード数を `confidence` に反映」の記述訂正（**数ではなく行数。反映先は `external` のみ。欠落は `files` 側**）

### [2026-08-27] F-5. `files` テーブルと git 欠落時の挙動 — **`git_state` は3値。縮退を明示する**

E-1 の記述は「パス、`lang`、行数、コメント行数、`parse_errors`、git メトリクス」だけで、列名も値域も無かった。

#### ★ 実測データそのものが罠を踏んでいた

**§8.4 の推奨セットは全部 `--depth 1` の shallow clone** で取得している（tools/README のコマンドもそう）。
shallow clone では `git log` が1件しか返らないため、**`git_commits` は全ファイルで一律 1** になり、
ホットスポットの「変更頻度」軸が**黙って無意味になる**。エラーも警告も出ない。§2.3 そのもの。

したがって git の状態は「ある / ない」の2値では足りない。

#### 決定1: `analysis_run.git_state` を3値で持つ（F-4 のテーブルに1列）

| 値 | 意味 | 変更頻度の軸 |
|---|---|---|
| `full` | 完全な履歴がある | **使える** |
| `shallow` | shallow clone。履歴が切れている | **使えない**（値は出るが嘘） |
| `none` | Git 管理外（zip 展開など） | 取れない |

判定は `git rev-parse --is-shallow-repository` 一発。B-4 が `nogit-` で「Git 管理外」を既に扱っているので、その隣に置く。

#### 決定2: `files` の列

```
files
  file              -- ルートからの相対パス。区切りは "/" に正規化
  lang              -- c | cs | cpp | py | ts | js | unknown
  extract_status    -- extracted | skipped_no_extractor | skipped_binary
                    -- | skipped_ignored | failed
  bytes
  lines             -- 物理行数
  comment_lines     -- コメントだけの行
  blank_lines       -- 空白だけの行
                    -- ※ code_lines は導出（lines - comment_lines - blank_lines）
  parse_errors      -- ERROR ノード数
  missing_nodes     -- MISSING ノード数（tree-sitter。libclang は 0）
  content_hash      -- 増分取り込みの変更検出用
  git_commits       -- git_state = none なら空
  git_authors
  git_last_modified
  confidence        -- lang 判定の確からしさ
  extractor, snapshot
  --> 主キー (file, extractor, snapshot)   ※F-3 決定3
```

| 列 | 理由 |
|---|---|
| **パス区切りの正規化** | Windows で `\` が入ると**同じファイルが別行になる**。`tools/check-env.ps1` があり Windows が対象環境に入っているので必ず踏む |
| **`extract_status`** | §7.7 は「抽出器を持たない言語はスキップ」と決めたが、それだと **`symbols` が0行なのが「本当に0個」なのか「見ていない」のか区別できない**。A-1 が `not_extracted` と `not_applicable` を分けたのと同じ問題がファイル単位で起きる |
| **`missing_nodes`** | tree-sitter は ERROR と MISSING の2種類を返し、**括弧の不均衡は MISSING でも出る**。実測2 は ERROR だけを数えていた |
| **`content_hash`** | §7.6 の増分取り込みは「変更されたファイルの分だけ再生成」だが、**変更検出の手段が書かれていない**。git 無しのツリーでは mtime しかなく不安定。読むついでに計算できるので安い |
| `blank_lines` | コメント密度（E-3 ①）は「コメント行 / コード行」。`code_lines` の導出に要る |
| `confidence` | `lang` 判定の確からしさ。`.h` は C か C++ かが拡張子で決まらないので `medium` |

**数え方も決める**（決めないと抽出器ごとにずれる）:
- `lines` は物理行数。最終行に改行が無くても1行。空ファイルは 0。CRLF / LF で同じ値になること
- 同じ行にコードとコメントが両方ある場合は**コード行**。`comment_lines` は**コメントだけの行**

#### 決定3: git が取れないときの縮退を明示する

ホットスポットは4軸（規模 × 変更頻度 × 分岐数 × doc 欠落率）。

> **`git_state` が `full` でないとき、レポート3 は変更頻度の軸を落として3軸で出し、落としたことを明記する。**

**軸を黙って落とすことも、意味のない値で埋めることも、どちらも §2.3 の当事者。** B-4 が `-dirty` で「このデータは再現不能」を明示すると決めたのと同じ扱いにする。

#### 決定4: テスト / 生成コード / vendor の判別は `files` に列を作らない

E-3 は ① に「テスト／生成コード／vendor の判別 — ヒューリスティック」を入れていたが、**§7.3 ルール1「L0 に判断ロジックを一切入れない」に反する。**

| 層 | 何を持つか |
|---|---|
| **L0（事実）** | パス（`files.file`）、自動生成マーカー（`comments.marker` に `AUTO_GENERATED` を追加） |
| **L3a（判断）** | 「このパス規約ならテスト」「このマーカーがあれば生成コード」の判定 |

**②-b を L0 で線を引かないと決めたのと同じ構造。** E-3 ① の記述に「導出は L3a」と注記する。

#### 決定5: 追加ソースツリー（レベル1）は `files` に載せない

`files.file` は相対パスなので、**追加ツリーに `src/util.c` があると解析対象の同名ファイルと主キーが衝突する**。

> **`files` / `refs` / `comments` は解析対象ツリーのみ。追加ソースツリーは `symbols` にだけ行を立てる**（repo-name 違いの ID ＋ `is_definition`）。

E-3 のレベル1 の位置づけは「**定義まで到達する**」ためであって、追加ツリー自体を分析したいわけではない。**分析の対象と、解決を助ける参照物を分ける。** 概観・ホットスポット・コメントの集計が、渡したライブラリで薄まることも防げる。

**記載先**: 本ファイル F-5。反映先は `SCHEMA.md`（F-6 決定後）、[構想] §3.1 / §7.6（ファイル配置に `.files.tsv`）、E-3 ①（「導出は L3a」の注記）、tools/README（shallow clone では変更頻度が測れないことの注記）

### [2026-08-27] F-4. 閉世界の境界と標準ライブラリ辞書 — **`analysis_run` を追加し、辞書は `dst_id` に座標で入れる**

E-3（「境界をデータに持つ」「レベル2のヘッダは明示・記録する」）と E-1（標準ライブラリ辞書を初版必須）が
どちらも「記録する」と決めていたのに、**4テーブルのどこにも書く場所が無かった。**

#### まず、穴は思っていたより狭い

E-3 決定2 が半分解いていた。「**B-1 の `local <repo-name>` スキームがそのまま効く。追加ソースツリーは別の repo-name を持てばよい**」。

つまり**レベル1の由来は `dst_id` に既に入っている**（`local common-lib <snapshot> …`）。「追加ツリーのおかげで解けた行」は `WHERE dst_id LIKE 'local common-lib %'` で引ける。**新しい列は要らない。**

本当に無いのは2つだけだった。

| 無いもの | 性質 |
|---|---|
| どの境界で走ったか（レベル・追加ツリー・辞書・閉世界仮定） | **実行単位**のメタ情報 |
| `external` が既知の標準ライブラリか未知か | 行ごとの情報 |

#### 決定1: `analysis_run` テーブルを追加する

```
analysis_run
  extractor, snapshot   -- 主キー
  root                  -- 解析対象のルート
  boundary_level        -- 0 | 1 | 2 | 3（E-3 の「つまみ」）
  extra_roots           -- レベル1で渡した追加ツリー（repo-name : パス）
  closed_world          -- 閉世界仮定を置いたか（既定 true）
  stdlib_dicts          -- 使った辞書とバージョン（c99 / posix / python-3.12-builtins …）
  tool_versions         -- tree-sitter 0.26.0 / language-pack 1.15.8 等
  started_at
  load_errors           -- F-3 決定5 の主キー衝突件数
```

**主キーが `(extractor, snapshot)` で足りる**。A-3 が全テーブルに持たせた2列がそのまま外部キーになり、**全テーブルへの列追加はゼロ**。同じコミット・同じ抽出器を境界レベル違いで2回流せば衝突するが、E-4 決定2「DB は混ぜない」により別 DB になる。仮に混ざれば F-3 決定5 でエラーとして表に出る。

**必要な理由**: `external` の意味が実行ごとに変わる。レベル0 なら「このリポジトリの外」、レベル1 で3ツリー渡していれば「4ツリーすべての外」。記録が無いと同じ語が別のことを指す。

**B-4 の `-dirty` と同じ役割**。B-4 は「このファクトデータは再現不能」を明示するために `-dirty` を付けると決めた。境界設定も「**この数字は何を前提にした数字か**」を隠さないための記録である。E-3 が「レベル2のヘッダはビルドで使われたものと同じ保証が無い。明示・記録する」と書いた宛先もここ。

#### 決定2: 標準ライブラリは `dst_id` に座標を入れ、`status` は `unresolved` のまま

| 状態 | `dst_id` | `status` | `reason` |
|---|---|---|---|
| リポジトリ内で解決 | `local myrepo 8f3ac21 helper().` | `resolved` | 空 |
| 追加ツリーで解決 | `local common-lib a1b2c3d encode().` | `resolved` | 空 |
| **既知の標準ライブラリ** | **`c-stdlib c99 - printf().`** | **`unresolved`** | **`external`** |
| 未知の外部 | 空 | `unresolved` | `external` |

**B-1 の設計意図どおり**（「外部ライブラリは本来の座標をそのまま受け入れる。両者は同じ名前空間に矛盾なく共存する」）。辞書のバージョンが座標に入るので再現性も付く。

##### `resolved` に格上げしない理由

E-3 / E-1 自身がそう書いている。「`external` を『未知の外部』と『既知の標準ライブラリ』に分けられれば、一次解析には**十分**」「`external` のまま残った呼び出しの集計は『**このプロジェクトが依存する外部 API の一覧**』であり、一次解析では有用な出力」。

格上げすると C の解決率が 66.5% → 8割超に跳ね上がるが、**コードについて分かったことは1つも増えていない**。名前が libc のリストに載っていた、というだけ。数字だけが良くなるのは §2.3 の当事者。

> **§7.7 の `[DllImport]`（宣言を指して `resolved`、`is_definition = false`）と扱いが違うのは意図的。** あちらは**宣言の実体をパースしている**（シグネチャがある）が、辞書は**名前の一致**しかない。

##### A-1 の文言を1箇所修正する

A-1 は `callee_id` を「**解決できた場合のみ**」と書いている。上の表では `unresolved` の行に `dst_id` が入るため、「**呼び先の座標が同定できた場合**」へ読み替える。

`status` は「**実体（定義または宣言）に到達したか**」、`dst_id` は「**呼び先をどこまで名指しできたか**」で役割が別。F-2 で `reason` から「呼び方」を切り出したのと同じ整理。

##### 辞書が誤爆しない理由

辞書を引くのは**リポジトリ内に候補が0個のときだけ**。自前で `open()` を定義していればそちらが先に解決されるので発火しない。`confidence = medium`（②-a と同じく閉世界仮定に依存するため）。

##### 却下した案

| 案 | 却下理由 |
|---|---|
| `reason` に `external_known` / `external_unknown` を足す | `reason` は「何を足せば解けるか」の軸（F-2）。両方とも「解析範囲を広げる」で同じ値になるべきで、軸が汚れる |
| `origin` 列（`repo` / `extra_tree` / `stdlib` / `unknown`） | `repo` / `extra_tree` は `dst_id` の repo-name から導ける。冗長な列が増える |
| 辞書のシンボルを `symbols` の行として立てる | 名前しか無いのに `kind` / 位置 / `visibility` を埋めることになり、実体のある行と混ざる |

#### 決定3: レポート5「解決サマリ」に境界を併記する

「呼び出しの 66% が確定」は「**レベル0・閉世界仮定あり・C99辞書使用**」という前提つきの数字。前提を書かずに数字だけ出すのは §2.3 の当事者なので、`analysis_run` の内容をレポート5 の冒頭に出す。

E-3 が言っていた「**境界を広げれば解けるものが増える、という関係が利用者から見える**」は、これで初めて成立する。

**記載先**: 本ファイル F-4。反映先は `SCHEMA.md`（F-6 決定後）、[構想] §3.1（`callee_id` の説明）/ §3.4.2。辞書そのものはデータであってコードではないため `SCHEMA.md` とは別ファイルで持つ

### [2026-08-27] F-3. 主キーと位置情報 — **範囲で持ち、衝突は黙って捨てない**

発見1（ID衝突で 17.7% 消失）と同じ形の穴が4箇所あった。**フル版自身も §7.4-1 を満たしていなかった**（`calls` / `var_refs` の位置に `end_line` / `end_col` が無い）。

#### 決定1: 位置は全テーブルで `start_line` / `start_col` / `end_line` / `end_col`

現状は表ごとにバラバラ（`line, col, end_line, end_col` ／ `start_line, end_line` ／ `file, line`）。`start_` で揃えると §7.4-1 の「範囲で持つ」がひと目で分かる。

**抽出コストはゼロ。** tree-sitter は `start_point` / `end_point`、libclang は `extent` を無料で返す。

#### 決定2: 起点・端点・単位を明記する

**決めておかないと抽出器ごとに1〜2ずれた値が入り、後から直せない。**

| 項目 | 決定 | 理由 |
|---|---|---|
| 行・列の起点 | **1 起点** | §7.6 の TSV 例が既に 1 起点。SARIF も 1 起点。**tree-sitter は 0 起点なので +1 が要る** |
| `end` の端点 | **排他**（最後の文字の次） | SARIF の `endColumn` が排他。tree-sitter の `end_point` も排他。**libclang は包含なので +1 が要る** |
| `col` の単位 | **文字数（Unicode コードポイント）** | **tree-sitter の column は行内のバイトオフセット**。日本語コメントを含む C ソースで列がずれる。§7.6 は「人間が見てその場で直せる素直さ」を基準に TSV を選んでおり、バイト位置は手で数えられない |

`col` の単位だけは実装で変換が要る（行頭からのデコード1回）。安いが、明記しないと確実に食い違う。

#### 決定3: 主キー

| テーブル | 主キー |
|---|---|
| `files` | `(file, extractor, snapshot)` |
| `symbols` | `(id, file, start_line, extractor, snapshot)` ※**暫定**（決定4） |
| `refs` | `(file, start_line, start_col, end_line, end_col, kind, extractor, snapshot)` |
| `comments` | `(file, start_line, start_col, extractor, snapshot)` |

- **`refs` に `kind` が要る理由**: `p->f()` は呼び出しの開始位置と `p` の変数読み取り位置が**同じ**。F-1 で `var_read` を同じ表に畳んだため、`kind` が無いと衝突する
- **終了位置まで要る理由**: `f(x)(y)`（カリー化呼び出し）は外側と内側が**同じ位置から始まる**。C / C# では稀だが JS では普通に出る
- **`src_id` を入れない理由**: 位置から導けるので冗長。§7.6 の増分取り込みが `DELETE FROM T WHERE file = X` で回るため `file` が先頭にある方が素直

#### 決定4: `symbols` の主キーは暫定 — `#ifdef` の両枝で必ず衝突する

`(id, extractor, snapshot)` のままだと**実測3で確認済みの問題**が残る。tree-sitter は `#ifdef` / `#else` の両枝を構文木に載せるため、**シグネチャを入れても同じIDが2行できる**。cJSON の衝突4件はまさにこれだった（「`#ifdef` の枝ごとに5回定義された同一マクロ」）。

`(id, file, start_line, …)` にすれば両方が残る。副作用としてヘッダの宣言と `.c` の定義も別行になるが、`is_definition` で区別でき、[現実] §4.4-5 が「宣言が複数ある問題は全言語で起きる」と書いているとおり、むしろ正しい姿。

> **`refs.dst_id` が複数行にマッチしうる点は仕様として受け入れる。** 「呼び先の定義が条件コンパイルで分岐している」は**事実であって不具合ではない**。`GROUP BY id HAVING COUNT(*) > 1` で検出できる（§3.3 の思想どおり）。
>
> ~~ただし「正規シンボル1行＋宣言箇所の別テーブル」（[現実] §4.4-5、F-13③ で未決）を採ると主キーは変わる。そのため暫定とする。~~
> **→ F-13③ で決着（2026-08-27）。正規化は L2 の派生ビューに置くため、この主キーは暫定ではなく確定。**

#### 決定5: ★ ローダーは `INSERT OR IGNORE` / `INSERT OR REPLACE` を使わない

**これが F-3 の本体。**

発見1（17.7% 消失）が**件数を数えるまで気づけなかった**のは、主キーが足りなかったからではなく、**衝突が黙って捨てられていた**から。`emit-generic-schema.py` は `id TEXT PRIMARY KEY` に静かに詰めており、「175 生成 → 171 ユニーク」と引き算して初めて分かった。

主キーをどれだけ厳密にしても、ローダーが `OR IGNORE` なら穴は残る。

> **決定**: L0 → L1 の取り込みは素の `INSERT` で行い、**主キー衝突はエラーにする**。件数を取り込み結果に記録し、レポート5「解決サマリ」に出す。
>
> **主キーの役割は衝突を防ぐことではなく、検出して報せることである。**

§2.3「間違った答えが自信を持って出てくるツールは、答えが出ないツールより有害」に対する、スキーマ側の直接の実装になる。

#### 決定6: 列名 `path` → `file` に統一

`emit-generic-schema.py` は `files.path` を使っているが、他の全テーブルは `file`。**`file` に統一する。**

**記載先**: 本ファイル F-3。反映先は `SCHEMA.md`（F-6 決定後）、[構想] §3.1 / §3.1.5 / §7.6（TSV 例のヘッダ・取り込み擬似コードへ「衝突時はエラー」を追記）。実測値への影響なし

### [2026-08-27] F-2. `resolved` の行の `reason` — **「呼び方」を `dispatch` へ切り出す**

`resolved` / `macro_expanded` が出ていたのは書き間違いではない。**A-1 の分割が一段足りていなかった。**

#### 診断: `reason` に2つの軸が同居している

A-1 が定めた4値を分類すると、3つは「解けない理由」ではなく**呼び方**を言っている。

| A-1 の値 | 実際に何を言っているか | 軸 |
|---|---|---|
| `via_function_pointer` | 関数ポインタ**経由で呼んでいる** | **呼び方**（事実） |
| `virtual_unresolved` | 仮想ディスパッチ**で呼んでいる** | **呼び方**（事実） |
| `macro_expanded` | マクロ**を経由している** | **呼び方**（事実） |
| `unknown` | 理由が分からない | 解けない理由 |

一方 E-3 / E-1 が追加した3値（`external` / `ambiguous` / `needs_type`）はすべて「解けない理由」側。
**A-1 は `resolution` を割ったとき `status` は切り出したが、「呼び方」の軸を `reason` の中に残していた。**

#### 二義性の実害: `macro_expanded` は抽出器によって意味が逆になる

| | 簡易版（tree-sitter） | フル版（libclang） |
|---|---|---|
| プリプロセッサ | 解決しない | 既に展開済み |
| マクロ呼び出しで分かること | **どのマクロかは特定できる**（＝成功） | 展開後の呼び出しは見えるが**どのマクロ由来か辿れない**（＝失敗） |
| `macro_expanded` の意味 | 解決できた**経路** | 解決できなかった**理由** |

**§3.1.4 で `linkage: internal`（翻訳単位内）と C# の `internal`（アセンブリ内）を廃止したのと、同じ形の欠陥。**

#### 「`unresolved` に戻す」は採れない

A-1 は `callee_id` を「解決できた場合のみ」と決めているため、`unresolved` に戻すと `dst_id` に入っているマクロのシンボルIDを捨てることになる。**§3.3「抽出層では絶対に情報を捨てない」に正面から反する。**

#### 決定: 3列に分ける（F-1 で作った `dispatch` に「呼び方」を寄せる）

| 列 | 問い | 値 | いつ埋めるか |
|---|---|---|---|
| **`dispatch`** | **どうやって呼ぶか**（事実） | `direct` / `virtual` / `macro` / `function_pointer` / `implicit` | `kind = call` なら**常に**。`status` と独立 |
| `status` | 解けたか（事実） | `resolved` / `unresolved` / `not_applicable` / `not_extracted` | 常に |
| **`reason`** | **なぜ解けなかったか**（事実） | `external` / `ambiguous` / `needs_type` / `needs_dataflow` / `unknown` | **`unresolved` のときのみ**（A-1 の規則が復活する） |

##### 旧値の移し替え

| A-1 の旧値 | 移し先 |
|---|---|
| `macro_expanded` | `dispatch = macro`。`status` は解決状況どおり（C の 65件は `resolved` のまま、`reason` は空） |
| `via_function_pointer` | `dispatch = function_pointer` ＋ `reason = needs_dataflow` |
| `virtual_unresolved` | `dispatch = virtual` ＋ `reason = needs_type` |

##### `needs_dataflow` を新設する理由

§3.1.1 は `reason` を「**値ごとに何を足せば解けるようになるかが違う**」という軸で分けると決めたが、関数ポインタだけがその軸に乗っていなかった。

| `reason` | 何を足せば解けるか |
|---|---|
| `external` | 解析範囲を広げる（F-4 の境界レベル） |
| `ambiguous` | シグネチャ等の識別子 |
| `needs_type` | 型解決（段階5） |
| **`needs_dataflow`** | **データフロー解析**（どの関数が代入されたか） |
| `unknown` | 不明 |

これで `reason` の全値が「引き継ぎ書に書ける指示」になる。**E-4 決定5 の「詳細解析への引き継ぎ書」はこの列の集計そのもの。**

#### 効果

1. **数字は変わらず、読み方だけが正確になる。** レポート5「解決サマリ」は2つの表になる。今は「66% が確定、12% がマクロ、16% が外部」と**軸の違うものを1列に並べて**読ませていた。分離後は「マクロ経由だが解決済み」がそのまま言える
2. **E-1 改良D の成果が強くなる。** 「関数ポインタ経由を `external` と誤報告していた 17〜18件が消えた」は、分離後は `dispatch = function_pointer` として **`status` に関係なく数えられる**
3. **§3.3 の SQL が意味を持つ。** `GROUP BY reason` が純粋に「何を足せば解けるか」の内訳になる

#### 注意: `dispatch = macro` 自体が閉世界仮定つきの演繹

tree-sitter からは `foo(x)` が関数呼び出しかマクロ呼び出しか区別できず、**リポジトリ内のマクロ定義表に当たって初めて分かる**。外部ヘッダのマクロは `dispatch = direct`（推定）＋ `reason = external` に落ちる。

これは ②-a と同じ性質なので行の `confidence = medium` でカバーされる（§7.3 が「②-a は演繹であって判断ではない」として L0 での確定を許しているのと同じ根拠）。**新しい抜け道は作らない。**

**記載先**: 本ファイル F-2。反映先は `SCHEMA.md`（F-6 決定後に起こす）、[構想] §3.1 / §3.1.1 / §7.6。E-1 / E-3 の実測表はラベルのみ書き換え（**数字は1件も動かない**）

### [2026-08-27] F-1. テーブル名・列名の統一 — **`refs` に統合。正本は `docs/SCHEMA.md`**

E-4 決定1「スキーマは共有する」は**語彙の再利用までしか実現しておらず**、表の形が一致していなかった。共有する方向のまま、正本の形を確定させる。

#### 決定1: `calls` / `var_refs` /（未採用の）`imports` を **`refs` に統合する**

| 根拠 | 内容 |
|---|---|
| **import は呼び出しと同型** | E-3 は「相対パスで書かれた import/include の解決先」を ②-a に分類した。つまり import にも `status` / `reason` / `confidence` が要る。別テーブルにすると3列を複製することになる |
| **レポート4 が両方を同じ粒度で畳む** | ディレクトリ／名前空間単位のコンポーネント依存図は `call` と `import` を同じ `GROUP BY` で集約する。別テーブルだと `UNION` が要り、**列名が違えば `UNION` すら書けない** |
| **§2.1 が挙げた主流アーキテクチャそのもの** | Understand の「Entity / Reference の2モデル」、SCIP の「シンボルと参照の永続インデックス」。参照を種別で1表に持つ構造は既に文書が支持している |
| **宙に浮いた `imports` 案が片付く** | [現実] §4.1 が提案し「§3.1 には含まれていない（未決事項）」と書いたまま、本ファイルに項目が無かった（F-13①）。`kind = import` で吸収される |

`var_refs` も畳む。`refs` との違いは `access`（`read` / `write` / `readwrite` / `address_of`）だけであり、**排他的な種別なので `kind` に畳める**。簡易版は `var_*` の行を出さないだけで済み、「言語に無い概念は行が出ないだけ」という E-1 の原則がそのまま効く。

#### 決定2: 列名は `src_id` / `dst_id` / `dst_expr`

表が `call` 専用でなくなる以上、`caller` / `callee` は名前として成立しない。import の行に `caller_id` と書いてあれば読み手は誤読する。**§3.1.4 で `linkage` を廃止したのと同じ理由**（同じ語が別概念を指す状態を作らない）。

#### 決定3: `kind` の衝突を `kind` と `dispatch` に分ける

| 列 | 問い | 値 | いつ埋めるか |
|---|---|---|---|
| **`kind`** | どんな関係か | `call` / `import` / `var_read` / `var_write` / `var_readwrite` / `address_of` … | 常に |
| **`dispatch`**（旧 `call_kind`） | その呼び出しがどう解決されるか | F-2 で確定 | `kind = call` のときのみ |

`kind` の隣に `call_kind` が並ぶのは読みにくく、簡易版は `virtual` を判定できない（③に分類済み）ため、名前を分ける。

#### 決定4: 片方にしか無かった列はすべて残し、両方が埋める

| 列 | 判断 | 理由 |
|---|---|---|
| `qualified_name` | 残す。簡易版も埋める | `container_id` を辿れば作れる。レポート2「機能の見取り図」の名前空間別集約に効く |
| **`is_definition`** | 残す。**簡易版も必須** | E-3 レベル2 の「宣言までしか届かない」と §7.7 の「切れた線」がこの列で表現される。F-4（境界の記録）の土台 |
| `storage_class` | 残す | 簡易版の C 抽出でも `static` / `extern` は読める。他言語は `not_applicable` |
| `branch_count` | **フル版にも追加** | 分岐ノードの計数は事実であり判断ではない（L0 で出してよい）。ホットスポットの4軸の1つ。[理想] J-02 に対応 |
| `parse_errors` | **フル版にも追加** | tree-sitter 固有ではない。libclang にも診断がある（[現実] §2.8）。フル版は致命的エラー数を入れる |
| **`files` テーブル** | **共有テーブルとして追加** | §7.6 は既にファイル単位で動いているのに、ファイルの行を持つ表が無かった。レポート1「概観」も出せない。列定義は F-5 |

**A-3 の徹底**: `lang` / `extractor` / `snapshot` を **`files` を含む全テーブル**に持たせる。簡易版の記述はこの3列が抜けていた。

#### 決定5: 正本は `docs/SCHEMA.md` に一本化する

現在の正本は**凍結中のフル版 §3.1**。簡易版の構想文書にも列を書けば**3つ目の写し**ができ、[現実] §4.1 の再掲が古びたのと同じことが繰り返される。実際、追随改訂6件のうち §7.6 は「現状の**3テーブル**」のまま取り残された（F-8）。

**[構想] §3.1 / 簡易版構想文書 / [現実] §4.1 は再掲をやめ、`SCHEMA.md` への参照に置き換える。**

> **着手時期**: F-2 〜 F-6 がすべて列に影響するため、**F-6 まで決まった時点で `SCHEMA.md` を起こす**。

#### 確定後のテーブル（列は F-2 〜 F-6 で確定）

```
files     ← 新設（列は F-5）
symbols   ← id, kind, name, qualified_name, container_id, 位置,
             is_definition, visibility, visibility_source, storage_class,
             branch_count, confidence, lang, extractor, snapshot
refs      ← src_id, dst_id, dst_expr, kind, dispatch, 位置,
             status, reason, confidence, via_lambda, lang, extractor, snapshot
comments  ← file, 位置, kind, source_kind, attached_id, marker, text,
             confidence, lang, extractor, snapshot
```

`cfg_*` / `findings` / `constraints` は別系統のまま（D-1 / D-3 / D-4）。

#### 後続への引き渡し

`refs` 統合により **F-3（主キー）が変わる**。同じ位置に `call` と `var_read` が並びうるため、主キーに `kind` を含める必要が出る。

**記載先**: 本ファイル F-1。フル版への反映先は [構想] §3.1 / §3.1.5 / §3.3（SQL 例4本）/ §7.6（TSV 例・ファイル配置・取り込み擬似コード）、[現実] §4.1。**`SCHEMA.md` 起こしと同時に実施する**

### [2026-08-26] E-4. 簡易版とフル版の関係 — **スキーマは共有する。DB は混ぜない**

当初の設問は「独立した別ツールか、フル版の下位互換（同じテーブルに `confidence = low` で入れる）か」だったが、**この二択の立て方が誤りだった**。「スキーマを共有するか」と「同じ DB に混ぜるか」は別の決定である。

#### 決定1: スキーマは共有する

E-1〜E-3 の結果がすべて共有側を指していた。

| 根拠 | 内容 |
|---|---|
| 層分離 | §7.3 の L0〜L3 が簡易版でそのまま成立（②-b を L0 で線を引かない、がルール1 と一致） |
| A-1 / A-2 / A-3 | `status` / `reason` / `confidence` / `lang` / `extractor` / `snapshot` をそのまま使えた |
| B-2 / B-4 | SQLite と `snapshot` は共通 |
| 実測 | C と C# を同じ列に出力でき、列を1つも増やさずに載った |

#### 決定2: DB は混ぜない（同じスキーマ定義・別ファイル）

**混ぜることを既定にしない。** 必要になれば SQLite の `ATTACH` で後から結合できるため、既定で混ぜる理由が無い。

混ぜると起きること（混ぜない判断の根拠）:

1. **同じ呼び出しが2行になる** — 簡易版が `unresolved / needs_type`、フル版が `resolved` と言った同じ箇所が併存する。§7.3 のフィルタ原則 `WHERE status='resolved'` 自体は書けるが、**件数を数えると二重計上**する
2. **どちらを正とするかのルールが要る** — これは L2 / L3a の話であり、L1 に持ち込むと層分離が濁る

**ただし主キーは混在に耐える形にする**（[構想] §3.1.5）。運用として混ぜないことと、混ざったときに壊れないことは別物で、後者を捨てる理由が無いため。

#### 決定3: `confidence` ではフル版と簡易版を区別しない

A-2 は「簡易版の結果は `low`」を前提にしていたが、**E-3 で ②-a を `medium` に格上げした時点でこの前提は崩れている**。閉世界仮定の下で候補が一意に定まる解決は推測ではなく演繹だから。

**区別は `extractor` 列で行う**（A-3 がこの列を全テーブルに持たせているのがここでも効く）。

#### 決定4: フル版への追随改訂（凍結は維持）

**改訂は「簡易版に合わせる」ためではない。** 簡易版の検討で**フル版自身の欠陥が実測により判明した**ため。放置するとフル版の再開時に同じことを再発見することになる。

| # | 改訂 | 理由 | 記載先 |
|---|---|---|---|
| 1 | **`linkage` を廃止し `visibility` / `visibility_source` へ** | `linkage: internal`（翻訳単位内）と C# の `internal`（アセンブリ内）は**同じ語で別概念**。フル版は Phase 1 が C# なのに C 由来の列しか持っていなかった | [構想] §3.1.4 |
| 2 | **descriptor にシグネチャを含める** | C# で **17.7%** のシンボルが ID 衝突で消えた。C++ でも同じ問題が起きる | [構想] §3.2.2 |
| 3 | `reason` に `external` / `ambiguous` / `needs_type` を追加 | 値ごとに「何を足せば解けるか」が違う | [構想] §3.1.1 |
| 4 | `comments` テーブルと `via_lambda` / `visibility_source` / `source_kind` を追加 | doc は名前解決不要で取れる一次解析の主材料。3列は他言語で空く軸への備え | [構想] §3.1.4 / §3.1.6 |
| 5 | **主キーを明記** | `id` 単独では抽出器を変えて再抽出したときに衝突する | [構想] §3.1.5 |
| 6 | `confidence` の解釈を訂正 | 上記「決定3」 | [構想] §3.1.2 |

あわせて [構想] §7.6 の L0 TSV 例と [現実] の `cpp_linkage` 記述も追随させた。

#### 決定5: 引き継ぎ書を接続の成果物とする

`needs_type` / `ambiguous` / リフレクションの集中箇所を「**フル版やデバッガで見るべき場所**」として出す。**簡易版とフル版を接続する唯一の具体物**であり、DB を混ぜなくても両者が連携できる形になる。

**記載先**: [構想] §3.1.4 / §3.1.5 / §3.1.6 / §3.1.1 / §3.1.2 / §3.2.2 および冒頭の「追随改訂について」

### [2026-08-26] E-1. 想定用途と最小の出力 — **4テーブル・5レポート。段階1の最小版は必須要件**

初期対象言語は **C と C#**。C が最も価値が出るという E-3 の実測を受けつつ、**C だけではデータセットが C に寄る**ため、最も遠い言語と組ませて汎用性を担保する。実際、この選択により **C だけでは気づけない欠陥が2件露呈した**（本ファイル E-1 の発見1・発見2）。

#### 最小の出力

**4テーブル**（L1、SQLite）: `files` / `symbols` / `refs` / `comments`

**5レポート**（L3a）: 概観 ／ **機能の見取り図** ／ ホットスポット ／ コンポーネント依存 ／ **解決サマリ**

- **`comments` を最小に含める**。doc コメントは「この関数が何をするか」を人が書いた唯一の記述で、名前解決を一切必要としない。実測で C# の定義の **49.8%**（平均239文字）、cJSON の **34.2%** に付いていた。**簡易版がフル版と同等以上になりうる唯一の領域**でもある（コンパイラのフロントエンドはコメントを捨てる）
- **「解決サマリ」を最小に含める**。§2.3 の「不確かさを黙って隠さない」を**出力の一部**にするため

#### 言語固有概念を列に持ち込まない

`kind` / `container_id` / `visibility` / `refs.kind` / `refs.reason` の汎用語彙へ写す。**言語に無い概念は「行が出ない」だけで、列は共通のまま。** C と C# を同じ列の SQLite へ実際に出力して確認済み（列を1つも増やさずに載った）。

#### 段階1（スコープ解析）の最小版は**必須要件**

改良A（同一 container 優先）／B（レシーバの宣言型を使う）／C（型名なら static 呼び出し）／D（ローカル束縛は `via_function_pointer`）。

**「あると精度が上がる」機能ではなく「無いと嘘をつく」機能である。** C の実測では `resolved` 率が 0.0 ポイントしか動かないのに、**関数ポインタ経由の呼び出しを「リポジトリ外の関数」と誤報告していた 17〜18 件が消えた**。C# では `resolved` が **5.5% → 31.8%**。

4つとも「1ファイルの構文木 ＋ リポジトリ内の定義表」で完結し、**軸2 = ゼロを崩さない**。

#### 初版の天井（明示する境界）

チェーン呼び出し（`a.b().c()`）と `var` の型推論は**入れない**。本物の型推論＝段階5の領域。C# に残る `needs_type` 40% がこれ。

#### フル版との関係

**部分集合 ＋ α。** 出すものはフル版の部分集合だが、**git メトリクスはフル版に無い軸**なので厳密な包含関係ではない。→ E-4 で扱う。

**記載先**: 本ファイル E-1（実測と根拠の全文）。E-4 決定後に簡易版の構想文書へ移す

### [2026-08-26] E-2. インストール水準 — **2軸に分解。軸1 =「軽い」／軸2 = ゼロ**

当初の E-2 は「利用者のマシンに何を入れさせるか」という**1軸しか持っていなかった**。実際にはもう1軸ある。

| 軸 | 問い | 対象 |
|---|---|---|
| **軸1** | 利用者の**マシン**に何を入れさせるか | ツール側 |
| **軸2** | 利用者に**解析対象**について何を用意させるか | 対象リポジトリ側 |

#### 軸1 = 「軽い」（Python 3.10+ ＋ `pip install` 1コマンド）

**採用理由**: Python と pip が使えない前提を置くと、解析するための言語処理系から用意する話になり本末転倒。開発者のマシンに Python が無いケースは想定しない。

「完全ゼロ」（単一実行ファイル）を採らない理由: Go / Rust での実装を意味し、**B-2**（ストアは SQLite ＝ Python 本体同梱だから追加依存ゼロ）と **B-3**（ローダーは Python）の根拠がまるごと無効になる。tree-sitter 本体は C なので Go / Rust からも使えるが、実装言語の選び直しは既存の決定を再利用できなくする。配布の手間に対して失うものが大きい。

**受け入れる制約**: オフラインの閉じた環境では wheel の持ち込みが必要。これは既知の穴として受け入れる。

#### 軸2 = ゼロ（ビルドコンテキストを要求しない）

要求するのは「**ソースツリーが読める状態にあること**」だけ。[構想] §7.7 が抽出器の起動条件として挙げていたもの（`compile_commands.json` / `.sln`・`.csproj` / `tsconfig.json` ＋ `node_modules` / 依存パッケージの入った実行環境）を**一つも課さない**。

**採用理由**: 想定用途は「初見のソースコードの一次解析」であり、**その状況でビルドが通る状態にあることの方が稀**。ビルドが通らないリポジトリ、依存を落とせない環境、zip を展開しただけのツリー、受け取った一部だけのソース — これらが全部そのまま対象になる。

> **補正（2026-08-26）**: 軸2 = ゼロは**二択ではなく既定値**である。追加のソースツリーを外部から渡せる設計にすれば、閉世界の**境界はパラメータになる**（E-3 の「閉世界の境界はつまみ」）。要求を課さないのは既定動作の話であって、広げる手段を持たないという意味ではない。

#### この2軸のうち、実現手段を確定させるのは軸2

名前解決を行うツール（libclang / Roslyn / TypeScript Compiler API）は**ビルドコンテキスト無しでは機能しない**（プロセスは動くが型が解決できない）。したがって軸2 = ゼロは、**[構想] §4.1 の選定基準1（意味解析を持っているか）を放棄する**ことと同義であり、使える手段は構文解析までに絞られる。

→ **tree-sitter が主力になる。** [構想] §4.7 はこれを「まだ抽出器を書いていない言語の暫定対応」と格下げしているが、**簡易版ではこの位置づけを覆す**。

#### 効果の非対称性（軸1から詰めてはいけない理由）

軸1を「完全ゼロ」にしても、軸2を課せば「まず cmake を通してください」が残るので利用者の負担は消えない。逆に軸1が「軽い」でも軸2がゼロなら、体験は **clone → 1コマンド → 結果が出る** になる。**効果の大半は軸2にある。**

軸1から議論すると実装言語の選定に入ってしまい、用途の話に戻れなくなる。順序として軸2を先に確定させた。

**記載先**: 本ファイル E-3（この決定を前提とした線引き）。E-1 / E-4 の決定後、簡易版の構想文書へ移す

### [2026-08-25] A-1. `resolution` の値セット — **`status` と `reason` の2列に分離**

1列に「解決できたか」と「なぜできなかったか」という粒度の異なる2軸が同居していたのが原因。分離して両方を残す。

| 列 | 問い | 値 |
|---|---|---|
| `status` | どの状態か | `resolved` / `unresolved` / `not_applicable` / `not_extracted` |
| `reason` | なぜ解決できなかったか（`status = unresolved` のときのみ） | `via_function_pointer` / `virtual_unresolved` / `macro_expanded` / `unknown` |

**採用理由**: §7.3 のフィルタ原則が `WHERE status = 'resolved'` と素直に書ける。`reason` に値を追加しても `status` の値域が汚れず既存クエリが壊れない。「未対応の言語だから空」と「その言語に概念が無いから空」が区別できる。

**記載先**: [構想] §3.1 / §3.1.1 / §3.3、[理想] §3.3 ④（採用済みと追記）、[現実] §「初版スキーマ」

> **改訂（2026-08-26、E-4 で確定）**: `reason` に **`external`**（定義が解析対象の外）／**`ambiguous`**（対象内の候補が複数）／**`needs_type`**（メンバ呼び出しでレシーバの型が不明）の3値を**追加した**。本決定が採用理由に挙げた「`reason` に値を追加しても `status` の値域が汚れない」性質の、想定どおりの使い方。[構想] §3.1.1 に反映済み。

### [2026-08-25] A-2. `confidence` 列 — **`high` / `medium` / `low` の enum で追加**

§7.4-2 が必須としていた信頼度列を全テーブルに追加。数値（0.0〜1.0）は採らない。

**採用理由**: libclang も Jedi も確率値を返さないため、数値にすると根拠のない小数を書くことになり、§2.3 の「間違った答えが自信を持って出てくるツールは有害」に逆行する。enum なら抽出器が根拠を持って言える粒度に一致する。

`status`（解決できたか＝事実）/ `reason`（できなかった理由＝事実）/ `confidence`（どれだけ信じてよいか＝判断）は役割が重複しない。Python の解決結果は `status = resolved` かつ `confidence = low` になる。

> **訂正（2026-08-26、E-4）**: 当初「簡易版（構文解析のみ）の結果は一律 `low`」と考えていたが誤り。閉世界仮定の下で候補が一意に定まる解決は**推測ではなく演繹**で `medium` に相当する（E-3 の ②-a）。**`confidence` でフル版と簡易版を区別することはできず、区別は `extractor` 列で行う。**

**記載先**: [構想] §3.1 / §3.1.2

### [2026-08-25] A-3. `lang` / `extractor` / `snapshot` の適用範囲 — **全テーブルに持たせる**

`symbols` から JOIN で引く案は、未解決の行（`callee_id` が空）で対応先が無く、抽出器のバージョンだけを上げて再抽出したケースでも破綻するため却下。

L0 の中間ファイルではメタ情報行に1回だけ書き、L1 取り込み時に各行へ展開する。**冗長になるのは L1 のみ**で、手書きする中間ファイル側は冗長にならない。

**記載先**: [構想] §3.1 / §3.1.3 / §7.6

### [2026-08-25] B-1. SCIP シンボルIDのマッピング規則 — **`local` スキームを定義**

SCIP の `<scheme> <package-manager> <package-name> <version> <descriptor...>` のうち、C/C++ にはパッケージ座標に相当する概念が無いことが問題だった。

自リポジトリのコードは `local <repo-name> <snapshot> <descriptor...>`、外部ライブラリは `scip-python` / `scip-typescript` が出す本来の座標（`pypi requests 2.31.0` 等）をそのまま受け入れる。両者は同じ名前空間に共存する。

**要実測（Phase 0）**: descriptor を各言語のネイティブIDから機械的に生成できるか。特に C++ のオーバーロード・テンプレート。

> **⚠ 再改訂（2026-08-27、I-1）**: **ID から `snapshot` を外した。** ID にコミットハッシュが入ると同じ関数が snapshot ごとに別IDになり、2時点比較が「全件消滅＋全件新規」になる／増分取り込みで結合が切れる。`snapshot` は A-3 により全テーブルの列にあるので、ID に入れるのは冗長だった。詳細は I 群。

> **⚠ 改訂済み（2026-08-26、E-4 で確定）**: **C# で 17.7% のシンボルが ID 衝突で消えた**（オーバーロード）。C は 2.3%。descriptor に**パラメータの型名リスト**を含めることを必須とした。型注釈が無い言語はアリティでフォールバック。出現順の連番は編集で変わるため採らない。C++ を待たずに C# で顕在化した。詳細は E-1「発見1」、[構想] §3.2.2。

**記載先**: [構想] §3.2.1

### [2026-08-25] B-2. L1 のストア実体 — **SQLite（`facts/db.sqlite`）**

追加依存ゼロ（Python 本体に同梱）、単一ファイルで可搬、中身を直接覗ける、DuckDB / Parquet への移行も容易、という理由。TSV のみ・DuckDB・Parquet・グラフDB は却下。

**記載先**: [構想] §3.4.1

### [2026-08-25] B-3. 実装構成 — **独立プロセス + GUI 後付けのための3つの約束**

Roslyn（.NET）も ts-morph（Node）も別ランタイムでしか動かないため、抽出器は subprocess として起動する。接点は TSV ファイルのみ。終了コードで「対象ファイルなし（正常）」と「ビルドコンテキスト不足」「異常」を区別し、§7.7 の言語ごと独立を成立させる。

GUI は初版で作らないが、① ロジックを `core/` に置き CLI を薄くする ② ジョブ設定ファイル（TOML）を用意する ③ 進捗を構造化出力する、の3点を初版から守る。将来の GUI はローカル Web UI を想定（L3a 可視化層と同じ土俵に乗るため）。

**記載先**: [構想] §9.1 / §9.2

### [2026-08-25] B-4. `snapshot` の粒度 — **`git describe --always --dirty` 相当**

`8f3ac21` / `8f3ac21-dirty` / `nogit-20260825T143052`。`-dirty` は「このファクトデータは再現不能」を明示するためのもので、§2.3 の「不確かさを黙って隠さない」思想に沿う。ブランチ名は識別子として不安定なため含めない。

**記載先**: [構想] §3.4.2
