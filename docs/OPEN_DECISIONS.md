# 未決事項リスト

**運用**: 作業開始時に本ファイルを参照する。決まったものは `DECISIONS.md` へ移し、決定日と根拠を残す。

> ## 現在の状況（2026-08-27）
>
> **L1 の列定義は `SCHEMA.md` として確定した。** A 〜 L 群の決定はすべて `DECISIONS.md` にある。
> **測定が要る未決はすべて片付いた（2026-08-31）。残るはフル版の凍結解除待ち 2 件だけ。**
>
> | ID | 内容 | 状態 |
> |---|---|---|
> | ~~F-10~~ | ~~レポート4（コンポーネント依存）が改良後の C# で描けるか~~ | **決着（2026-08-31）。描ける** → `DECISIONS.md` |
> | ~~F-16~~ | ~~②-a の的中率~~ | **決着（2026-08-31）。C は 100%、C# は 66%** → `DECISIONS.md` |
> | C-1 | [理想] × [現実] の4象限突き合わせ | フル版の凍結解除待ち |
> | C-2 | 可視化層（L3a）の実現手段 | フル版の凍結解除待ち |
>
> **次にやること**: **簡易版の構想文書を起こす** → 本設計。詳細は `HANDOFF.md`。

**関連文書**: `SCHEMA.md`（列の正本）／ `SCHEMA_INDEX.md`（列 × 決定）／ `DECISIONS.md`（なぜそう決めたか）

---

## 測定が必要な未決 — **すべて決着（2026-08-31）**

> **F-10**: 改良後は**エッジを持つコンポーネントが 3/6 → 6/6、コンポーネント間エッジ 6 本 97 件**で、**描ける**。
> ただし乗ったのは全呼び出しの 9.5% なので、図には集約前の `status` 分布を必ず添える。
> 再現手順は `tools/measure-component-deps.py`。
>
> **F-16**: **C は 50/50（100%）、C# は 44/66（66.7%）。** C# は `resolved` 31.8% のうち
> 実際に正しいのは約 21.1% で、**名乗りが実態の 1.5 倍に膨らんでいた**。
> 誤り 22 件の内訳は**オーバーロードの取り違え 20 件**と **`base.` を自分自身に向けた 2 件**。
> 決定は4つ——C は現状維持／C# はオーバーロードを `ambiguous` に降格／`base.` は基底から探す／
> **`refs.resolved_by` 列を追加**（的中率が規則ごとに 25〜100% とばらけたため、`confidence` を規則ごとに決める）。
> 判定結果は `docs/golden/*.tsv`、再現手順は `tools/sample-golden.py`。結論は `DECISIONS.md`。
>
> **F-16 は「スコープ」に分類されていたが、測った結果「列」の話だった。**
> 後回しを選んでいたら `resolved_by` は初版に入らず、後から足せばデータの作り直しになっていた。

---

## C. フル版の最大の宿題 — **中断中**

> 凍結中（E 群の結論待ち）。フル版を再開する場合の宿題として残す。

### C-1. 「理想」×「現実」の4象限突き合わせ

[理想] の末尾 §3.3 ⑤「次のステップ」が、この作業を挙げたまま**未実施**になっている。

> - 理想 ○ ／ 現実 ○ → 初版で実装
> - 理想 ○ ／ 現実 △ → `confidence` 列つきで実装
> - 理想 ○ ／ 現実 × → スキーマには列を用意し、当面は空にする
> - 理想 − ／ 現実 − → 該当言語では `not_applicable`

[理想] の観点 A〜O（全100項目超）× 6言語について、[現実] のマトリクスと突き合わせて仕分ける。

~~**これが終わらないと §3.1 のスキーマを初版として確定できない。**~~ また、4象限の分類結果がそのまま A-1 / A-2 の値設計になるため、**A の結論はこの作業から自動的に出る可能性が高い**。

> **訂正（2026-08-27、F-9）**: **L1 の列定義は `SCHEMA.md` として確定した**（本作業を待たずに、簡易版の実測と F〜L 群の突き合わせで固めた）。
> したがって本項目は「スキーマ確定の前提」ではなく、**フル版を再開するときに『理想のうち何がまだ空か』を棚卸しする作業**として残る。

**決めること**: 仕分け結果。初版スキーマの確定。

- [ ] 未決

### C-2. 可視化層（L3a）の実現手段

ロードマップ Phase 1 に「Mermaid または自作 HTML でコールグラフを出力」と併記されたまま。

**決めること**: どちらか、あるいは両方。規模が大きいときの描画戦略。

- [ ] 未決

---

---

## D. フル版の後続 Phase — **中断中**

忘れないための記録。着手時期が来るまで触らない。

| ID | 項目 | 出典 | 備考 |
|---|---|---|---|
| D-1 | **`constraints` テーブル**（人が書いたルールの保持） | [理想] §3.3 ③ | 「検討すべき」で終わっている。[理想] 自身が「これが差別化になる」と書いている核心部分。抽出ではなく宣言なので L0 とは別系統 |
| D-2 | **5軸の拡張テーブル**（`memory_ops` 等） | [理想] §3.3 ② | 言語ごとではなく「手動メモリ管理／GC」等の5軸で拡張テーブルを切る方針 |
| D-3 | **`cfg_blocks` / `cfg_edges` のスキーマ** | [構想] §6 | 名前だけ登場し §3.1 に定義が無い |
| D-4 | **findings / SARIF 取り込み** | [構想] Phase 5 | 明示的に「未確定」 |
| D-5 | **依存グラフ**（増分更新時の陳腐化対策） | [構想] §7.6 | 「後日検討」と明記。ファイル横断の `resolution` が古くなる問題 |
| D-6 | **FFI マッチング**（言語をまたぐ呼び出しの解決） | [構想] §7.7 | 非スコープと明記 |

---
