# code_analysys_spec

ソースコード解析データセットの**構想・仕様リポジトリ**。実装コードはまだ無く、設計判断とその根拠を文書として残すことが目的。

## 現在の状況（2026-08-26）

**フル版の構想は凍結中。** インストール要件（Python / .NET SDK / Node / clang / cmake）が
そのまま利用者への要求になることが判明したため、前提を問い直す段階にある。

いま考えているのは **簡易版**（インストール負担を最小にした場合に何が実現できるか）。
用途は「初見のソースコードの一次解析の補助」で、全体像の把握が主目的。
検討項目は `docs/OPEN_DECISIONS.md` の **E 群**。C 群・D 群は中断中。

**E-1 〜 E-4 すべて決定済み（2026-08-26）。E 群は完了。**

- **E-2**: 前提を2軸に分けた。**軸1**（利用者のマシンへの要求）= Python 3.10+ ＋ `pip install` 1コマンド。
  **軸2**（解析対象への要求）= **ゼロ**（ただし境界を広げる「つまみ」は持つ）。主力ツールは **tree-sitter に確定**。
- **E-3**: できること／できないことを ① / ②-a / ②-b / ③ の4段階で線引き。**実測つき**。
  解けるかを決めるのは「情報の所在」と「名前の一意性」の2軸で、**段階の中にも解けるものと解けないものが混在する**。
- **E-1**: 初期対象は **C と C#**（C だけだとデータセットが C に寄るため）。
  最小の出力は **4テーブル**（`files` / `symbols` / `refs` / `comments`）と **5レポート**。
  **段階1（スコープ解析）の最小版は必須要件**（無いと誤分類する）。

- **E-4**: **スキーマは共有する。DB は混ぜない。** 区別は `confidence` ではなく `extractor` 列で行う。
  これに伴い**フル版に追随改訂6件**を入れた（凍結は維持）。`linkage` 廃止 → `visibility`、
  descriptor にシグネチャ、`reason` 3値追加、`comments` テーブル追加、主キー明記、`confidence` の解釈訂正。
  **改訂は簡易版に合わせるためではなく、フル版自身の欠陥が実測で判明したため。**

**E 群は完了。** 次は**簡易版の構想文書を起こし**、本設計へ進む。

**実測スクリプトは `tools/measure-*.py` / `tools/emit-generic-schema.py`。** 数字の出典はすべて再現できる。

## 作業開始時に必ず読むもの

**`docs/HANDOFF.md`** — 引き継ぎ資料。セッションを跨いだ直後は**まずこれを読む**。
経緯・決定事項・次に議論することが単独で完結してまとまっている。

**`docs/OPEN_DECISIONS.md`** — 未決事項リスト。何がまだ決まっていないかが集約されている。
作業の前にこれを参照し、決定したものは「決定事項」へ移して本体文書にも反映すること。

## 文書構成

| ファイル | 役割 |
|---|---|
| `docs/CODE_ANALYSIS_CONCEPT.md` | 全体構想。スキーマ・ツール選定・層分離・ロードマップ |
| `docs/EXTRACTABLE_DATA_MATRIX.md` | 言語別「ツールで**取れるか**」（ツールの能力） |
| `docs/ANALYSIS_VIEWPOINTS.md` | 言語別「その**概念が存在するか**」（言語仕様） |
| `docs/HANDOFF.md` | 引き継ぎ資料。セッション再開時の入口 |
| `docs/OPEN_DECISIONS.md` | 未決事項リスト |
| `tools/check-env.{sh,ps1}` | 実行環境の確認スクリプト（Linux/WSL・Windows）。判定根拠は構想 §9.3 |

`EXTRACTABLE_DATA_MATRIX.md`（現実）と `ANALYSIS_VIEWPOINTS.md`（理想）を突き合わせて
`CODE_ANALYSIS_CONCEPT.md` のスキーマを確定する、という関係になっている。

## 執筆上の約束

- 文書は日本語。既存の見出し階層と表形式に合わせる
- **「なぜその選択をしたのか」を必ず書く**。結論だけの記述にしない
- 推測と実測を区別する。実測していないものは「想定」と明記し、確認方法を添える
- 決定済みのものには「ステータス: 確定」と日付を入れる
