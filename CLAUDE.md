# code_analysys_spec

ソースコード解析データセットの**構想・仕様リポジトリ**。実装コードはまだ無く、設計判断とその根拠を文書として残すことが目的。

## 現在の状況（2026-08-27）

**フル版の構想は凍結中。** インストール要件（Python / .NET SDK / Node / clang / cmake）がそのまま利用者への
要求になることが判明したため、前提を問い直した結果、**簡易版**（インストール負担を最小にした版）に軸足を移した。

**簡易版の L1 スキーマは `docs/SCHEMA.md` として確定した。** 未決は4件のみ（測定2件、フル版の凍結解除待ち2件）。

## 作業開始時に必ず読むもの

**`docs/HANDOFF.md`** — 引き継ぎ資料。**§1「どの資料のどこを見れば何が分かるか」を先に読むこと。**
全部を読む必要はなく、目的別に読む場所が決まっている。

## 文書の役割

| ファイル | 役割 | いつ見るか |
|---|---|---|
| **`docs/SCHEMA.md`** | **L1 の列定義の正本** | **列のことを知りたいときは、ここだけ** |
| `docs/SCHEMA_INDEX.md` | 列 × 決定の追跡表 | ある列がどう決まってきたかを追うとき |
| `docs/DECISIONS.md` | **なぜそう決めたか**（A〜L 群） ＋ 付録に検討の全記録 | 根拠を確かめるとき。冒頭に主題別の索引がある |
| `docs/OPEN_DECISIONS.md` | 未決事項（4件） | 作業前 |
| `docs/HANDOFF.md` | 引き継ぎ・案内 | **最初** |
| `docs/CODE_ANALYSIS_CONCEPT.md` | 両版の共通土台。§2.3（原則）／§7.3（層分離）／§7.6（L0・増分）／§8（テスト対象） | 原則と L0 形式を見るとき。**§3.1 は `SCHEMA.md` に置き換わった** |
| `docs/ANALYSIS_VIEWPOINTS.md` | 言語別「その**概念が存在するか**」（言語仕様） | L3a のレポート設計のとき |
| `docs/full-version/` | フル版専用（ツール選定・ライセンス・インストール要件・ロードマップ・[現実]） | フル版を再開するとき |

## ツール

| コマンド | 何を保証するか |
|---|---|
| `python3 tools/schema-check/verify_schema.py` | **`SCHEMA.md` の DDL を実行**し、文書に書いた性質が実際に成り立つか |
| `python3 tools/schema-check/verify_docs.py` | 決定ID・`§N.M`・ファイル名の**参照がすべて解決するか** |

**文書を編集したら両方を走らせること。**

`tools/measure-*.py` / `emit-generic-schema.py` は簡易版の実測スクリプト（数字の出典）。
`tools/schema-check/record-*.py` は J 群 / K 群の検証の記録で、実装の雛形ではない。

## 執筆上の約束

- 文書は日本語。既存の見出し階層と表形式に合わせる
- **「なぜその選択をしたのか」を必ず書く**。結論だけの記述にしない
- 推測と実測を区別する。実測していないものは「想定」と明記し、確認方法を添える
- 決定済みのものには「ステータス: 確定」と日付を入れる
- **同じ情報を2箇所に書かない。** 列定義の再掲が5箇所にあった結果、追随漏れが構造的に起きた。
  正本を1つ決め、他は参照する
- **決定の記号（A-1 〜 L-7）は振り直さない。** 相互参照が 1,170 箇所ある
- **一括置換をしない。** 編集は節に限定する（`- [ ] 未決` の置換で無関係な項目を巻き込んだ実績がある）
